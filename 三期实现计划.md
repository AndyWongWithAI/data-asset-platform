# 数据资源管理平台 · 三期实现计划

> 上游依据：`三期技术设计.md`。执行方式：subagent-driven-development（每任务独立分支 → 实现者 subagent → reviewer subagent → merge 到 main）。
> 约定：每任务 TDD（先写失败测试 → 实现 → 通过 → commit）；开发完成后必跑 `npm test` + `npm run test:page`。

---

## 任务总表

| # | 任务 | 分支 | 改动 | 依赖 |
|---|------|------|------|------|
| 1 | 实现计划（本文件） | — | 文档 | — |
| 2 | 状态机重构 + 三看板拆分 | `task/phase3-state` | state.js / TabWorkspace / QualityBoardModule / StandardBoardModule / 删 GovernanceModule / PlaceholderModule / state.test.mjs | — |
| 3 | 数据契约 | `task/phase3-data` | data.js（lineage/batchFiles/services + meta.stats）/ data.test.mjs | — |
| 4 | 血缘看板 | `task/phase3-lineage` | LineageModule / index.css | 2, 3 |
| 5 | 批次文件 | `task/phase3-batch` | BatchFileModule | 2, 3 |
| 6 | 数据服务 | `task/phase3-service` | DataServiceModule | 2, 3 |
| 7 | e2e + 收尾 | `task/phase3-e2e` | page_test.py / CLAUDE.md / 全量验证 | 2–6 |

---

## Task 2 · 状态机重构 + 三看板拆分

**分支** `task/phase3-state`

### 改动

**`src/state.js`** — `MODULE_GROUPS` 重写为 3 分组 10 菜单（见技术设计 §1）：

```js
export const MODULE_GROUPS = [
  { name: '生产态·治理看板', items: [
    { key: 'qualityBoard', title: '数据质量看板', implemented: true },
    { key: 'lineageBoard', title: '数据血缘看板', implemented: true },
    { key: 'standardBoard', title: '数据标准看板', implemented: true },
  ]},
  { name: '设计态·定义', items: [
    { key: 'catalog', title: '数据资产目录', implemented: true },
    { key: 'quality', title: '数据质量', implemented: true },
    { key: 'standard', title: '数据标准', implemented: true },
    { key: 'security', title: '数据安全', implemented: true },
    { key: 'masterdata', title: '主数据', implemented: true },
  ]},
  { name: '数据交换', items: [
    { key: 'batchFile', title: '批次文件', implemented: true },
    { key: 'dataService', title: '数据服务', implemented: true },
  ]},
];
```

`TABLE_DETAIL`、`MODULES`、`createInitialState/openTab/closeTab/activateTab/navigate` 逻辑不变（仅 MODULES 内容随 MODULE_GROUPS 变化）。

**`src/modules/QualityBoardModule.jsx`** — 从 GovernanceModule 抽出 `QualityBoard`（内容不变，改名为默认导出）：

```jsx
import data from '../data.js';
import ScoreGauge from '../components/ScoreGauge.jsx';
import Tag from '../components/Tag.jsx';

export default function QualityBoardModule({ onNavigate }) {
  return (
    <div>
      <h3>企业整体数据质量评分</h3>
      {data.qualityResults.map((r) => {
        const app = data.applications.find((a) => a.id === r.appId);
        return ( /* 原 QualityBoard 的 score-row 渲染，不变 */ );
      })}
    </div>
  );
}
```

**`src/modules/StandardBoardModule.jsx`** — 从 GovernanceModule 抽出 `StandardBoard`（内容不变）。

**`src/modules/GovernanceModule.jsx`** — 删除。

**`src/components/TabWorkspace.jsx`** — 路由分支更新：

```jsx
import QualityBoardModule from '../modules/QualityBoardModule.jsx';
import StandardBoardModule from '../modules/StandardBoardModule.jsx';
// import LineageModule/BatchFileModule/DataServiceModule 在 Task 4/5/6 补上，本任务先不 import
```

移除 `GovernanceModule` import 与 `governance` 分支，加：

```jsx
: activeTab.moduleKey === 'qualityBoard'
  ? <QualityBoardModule onNavigate={...} />
  : activeTab.moduleKey === 'standardBoard'
    ? <StandardBoardModule onNavigate={...} />
    : <PlaceholderModule moduleKey={activeTab.moduleKey} />
```

> 注：`lineageBoard`/`batchFile`/`dataService` 三个分支在 Task 4/5/6 逐个补上（本任务先落到 PlaceholderModule 兜底，保证状态机先跑通）。

**`src/modules/PlaceholderModule.jsx`** — `POSITIONING` 更新：

```js
const POSITIONING = {
  lineageBoard: '生产态·血缘：展示数据资产表→表血缘关系（跨应用交换方式 + 应用内加工），卡片式血缘图。',
  batchFile: '数据交换·批次文件：管理离线批次文件交换信息（源表→目标系统/文件格式/调度/审批链）。',
  dataService: '数据交换·数据服务：管理实时/准实时数据服务（API/订阅/数据包/审批链/调用计量）。',
};
```

移除 `service` 条目；`quality/standard/security/masterdata` 保留。

### TDD

**`test/state.test.mjs`** — 改写第一个测试（其余不变）：

```js
test('MODULE_GROUPS 含 10 模块且全部 implemented', () => {
  const items = MODULE_GROUPS.flatMap((g) => g.items);
  assert.equal(items.length, 10);
  const m = Object.fromEntries(items.map((i) => [i.key, i]));
  for (const k of ['catalog','qualityBoard','lineageBoard','standardBoard','quality','standard','security','masterdata','batchFile','dataService'])
    assert.equal(m[k].implemented, true, `${k} 应 implemented`);
  assert.equal(m.governance, undefined);
  assert.equal(m.service, undefined);
  // 分组名断言
  assert.deepEqual(MODULE_GROUPS.map((g) => g.name), ['生产态·治理看板', '设计态·定义', '数据交换']);
});
```

> `closeTab`/`navigate` 等测试引用了 `quality`（设计态），仍有效，不需改。若引用了 `governance` 需同步改（当前测试无 governance 引用）。

### 验收

- `npm test` 通过（state 测试改写后全绿）。
- `npm run build` 成功。
- 手动/浏览器：侧边栏 3 分组 10 菜单；点「数据质量看板」「数据标准看板」正常渲染；「数据血缘看板」「批次文件」「数据服务」落到占位提示（Task 4/5/6 前）。

---

## Task 3 · 数据契约

**分支** `task/phase3-data`

### 改动

**`src/data.js`** — 新增 3 数组（内容见技术设计 §3.5/§4），置于 `maskExamples` 之后、`};` 之前：

1. `lineage[]`（9 条，`up/down/relation/mode/desc`，mode 三值）
2. `batchFiles[]`（5 条，`id/name/sourceTableId/targetSystem/fileFormat/schedule/securityLevel/status/applyFlow[]`）
3. `services[]`（5 条，`id/name/type/latency/tableIds[]/desc/securityLevel/status/metrics{}/applyFlow[]`）

**`meta.stats`** 补充：

```js
stats: { applications: 5, databases: 5, tables: 10, fields: 51, rules: 8, standards: 6, masterData: 5, maskExamples: 4, lineage: 9, batchFiles: 5, services: 5 },
```

数据要点：
- `lineage[].mode ∈ {'离线批次','数据服务','应用内'}`；`up`/`down` 均指向现有 `tables[].id`。
- `batchFiles[].sourceTableId` 指向现有 `tables[].id`；5 条 sourceTableId 建议对应 t_wind/t_geo/t_topo/t_spare/t_progress。
- `services[].tableIds[]` 每项指向现有 `tables[].id`；5 条对应 t_wind/t_scada/t_forecast/t_topo/t_turbine。
- `services[]` 中 L4 服务（svc_004 海域限制因素共享）`applyFlow` 含 4 步（申请/安全合规审批/**二次审批**/授权）；其余 L2 服务 3 步。
- `applyFlow[]` 每步 `{ step, actor, time, result }`。

### TDD

**`test/data.test.mjs`** — 追加 3 个测试：

```js
test('lineage 引用完整 + mode 合法', () => {
  const tableIds = ids(D.tables);
  const modes = new Set(['离线批次', '数据服务', '应用内']);
  assert.ok(D.lineage.length >= 9);
  for (const l of D.lineage) {
    assert.ok(tableIds.has(l.up), `lineage ${l.id} up ${l.up} 不存在`);
    assert.ok(tableIds.has(l.down), `lineage ${l.id} down ${l.down} 不存在`);
    assert.ok(modes.has(l.mode), `lineage ${l.id} mode ${l.mode} 非法`);
  }
});

test('batchFiles 引用完整 + 审批链完整', () => {
  const tableIds = ids(D.tables);
  assert.ok(D.batchFiles.length >= 5);
  for (const b of D.batchFiles) {
    assert.ok(tableIds.has(b.sourceTableId), `batchFile ${b.id} sourceTableId 不存在`);
    for (const s of b.applyFlow) {
      assert.ok(s.step && s.result, `batchFile ${b.id} applyFlow 缺 step/result`);
    }
  }
});

test('services 引用完整 + securityLevel 合法 + 审批链完整', () => {
  const tableIds = ids(D.tables);
  const secLevels = new Set(D.security.map((s) => s.level));
  assert.ok(D.services.length >= 5);
  for (const s of D.services) {
    assert.ok(secLevels.has(s.securityLevel), `service ${s.id} securityLevel 非法`);
    for (const tid of s.tableIds) assert.ok(tableIds.has(tid), `service ${s.id} tableId ${tid} 不存在`);
    for (const step of s.applyFlow) assert.ok(step.step && step.result, `service ${s.id} applyFlow 缺 step/result`);
  }
});
```

### 验收

- `npm test` 通过（data 测试追加后全绿，含既有引用完整性测试）。

---

## Task 4 · 血缘看板（LineageModule）

**分支** `task/phase3-lineage`

### 改动

**`src/modules/LineageModule.jsx`** — 两段式（列表 + SVG 血缘图），模块内 `useState`：

- `selectedTableId`（null=列表 / 表id=血缘图）
- 列表视图：
  - 顶检索：`exchangeFilter`（全部/离线批次/数据服务/应用内）+ `keyword`（表名子串）
  - 结果清单：所有表（`data.tables`），每行计算 `上游数`（`lineage.filter(l => l.down === t.id).length`）、`下游数`（`l.up === t.id`）；交换方式按「该表作为下游时的 mode」聚合（若表无下游边则显示「—」）
  - 「批量导出血缘」= `ComingSoonAction label="批量导出血缘"`
  - 行内「查看血缘图」`onClick` → `setSelectedTableId(t.id)`
- 血缘图视图：`<LineageGraph tableId={selectedTableId} onNavigate={onNavigate} onBack={() => setSelectedTableId(null)} />`

**`LineageGraph`（同文件内组件）** — 纯 SVG 手绘：

布局算法（实现者据此落地）：
1. 中心节点 = 选中表；`ups = lineage.filter(l => l.down === tableId)`（前驱），`downs = lineage.filter(l => l.up === tableId)`（后继）。
2. 节点尺寸 `W=170 H=50`，纵向间距 `gapY=20`；左列（上游）x=0，中心 x=`W+160`，右列（下游）x=`2*(W+160)`。
3. 画布宽 = `3*W + 2*160`（约 830）；高 = `max(ups.length, downs.length, 1) * (H+gapY)`，中心节点垂直居中。
4. 每节点画 `<rect>`（圆角）+ 表名 `<text>` + 交换方式 tag（`mode !== '应用内'` 才画小色块/文字）。
5. 边：上游节点右缘中点 → 中心节点左缘中点（若多上游，各连到中心左缘不同 y）；中心右缘 → 下游左缘。用 `<path>` 画折线 + `<marker>` 箭头，边旁 `<text>` 标 `relation`。
6. 节点 `onClick` → `onNavigate('tableDetail', { tableId: node.tableId, title: node.nameCn })`。
7. 无上游列显示「无上游」、无下游列显示「无下游」占位文案。

**`src/components/TabWorkspace.jsx`** — 补 `lineageBoard` 分支 → `<LineageModule onNavigate={...} />`。

**`src/index.css`** — 新增：

```css
.lineage-svg { width: 100%; height: auto; }
.lineage-node { cursor: pointer; }
.lineage-node rect { fill: #fff; stroke: #165dff; stroke-width: 1.5; rx: 6; }
.lineage-node text { font-size: 12px; fill: #1f2329; }
.lineage-edge { stroke: #86909c; stroke-width: 1.2; fill: none; }
.lineage-edge-label { font-size: 11px; fill: #86909c; }
.lineage-empty-col { color: #c9cdd4; font-size: 12px; }
.flow-step { display: flex; align-items: center; gap: 8px; padding: 8px 12px; border-left: 3px solid #165dff; background: #fff; margin-bottom: 8px; font-size: 13px; }
.flow-step .step-name { font-weight: 600; }
.flow-step .step-actor { color: #86909c; }
.flow-step .step-result { margin-left: auto; }
```

### TDD

**e2e（`test/page_test.py`）** 暂不在本任务写，Task 7 统一补。本任务以「组件渲染 + 节点转跳」手工/单测确认：

- 打开「数据血缘看板」→ 列表渲染 → 点「查看血缘图」→ `.lineage-node` 数量 = 1 + 上游数 + 下游数 → 点任一上游/下游节点 → `.field-table` 出现 + `.tab.active` 表名正确。

### 验收

- `npm run build` 成功；浏览器验证血缘图渲染与节点转跳。
- 无横向溢出（血缘图 SVG 宽 830 内，容器可滚动）。

---

## Task 5 · 批次文件（BatchFileModule）

**分支** `task/phase3-batch`

### 改动

**`src/modules/BatchFileModule.jsx`** — 两段式（列表 + 详情），`useState(selectedId)`：

- 列表：表格列 = 任务名 / 源表 / 目标系统 / 文件格式 / 调度周期 / 状态。筛选 = 状态（全部/运行中/审批中）。「发起交换申请」= `ComingSoonAction`。
- 详情：`kv-list` 展示任务信息 + 「源表」转跳 M1（`onNavigate('tableDetail', { tableId: sourceTableId, title: 表名 })`）+ **样例审批链**（`.flow-step` 逐步渲染 `applyFlow`，含 step/actor/time/result）+「← 返回列表」。

**`src/components/TabWorkspace.jsx`** — 补 `batchFile` 分支。

### TDD

- 数据引用完整性已在 Task 3 覆盖；本任务以渲染验收为主（e2e 在 Task 7 补）。

### 验收

- 浏览器：列表 → 详情 → 审批链 `.flow-step` 渲染 → 源表转跳 M1 → 占位按钮弹 modal。

---

## Task 6 · 数据服务（DataServiceModule）

**分支** `task/phase3-service`

### 改动

**`src/modules/DataServiceModule.jsx`** — 两段式（列表 + 详情），`useState(selectedId)`：

- 列表：表格列 = 服务名 / 类型 / 时延 / 封装资产 / 安全分级 / 状态。筛选 = 时延（全部/实时/准实时）。「发起服务申请」= `ComingSoonAction`。
- 详情：`kv-list` 展示服务信息 + 调用计量（调用量/消费方数/近24h）+ 封装的资产清单（每条转跳 M1）+ **样例审批链**（`.flow-step`，L4 服务自然多一步「二次审批」）+「← 返回列表」。

**`src/components/TabWorkspace.jsx`** — 补 `dataService` 分支。

### TDD

- 数据引用完整性已在 Task 3 覆盖；本任务以渲染验收为主。

### 验收

- 浏览器：列表 → 详情 → 审批链（L4 服务 4 步）→ 封装资产转跳 M1 → 占位按钮弹 modal。

---

## Task 7 · e2e + 收尾

**分支** `task/phase3-e2e`

### 改动

**`test/page_test.py`** — 改写治理看板相关断言（原「数据治理看板」→ 三看板），追加三看板/血缘图/批次文件/数据服务断言：

1. 质量看板：`.sidebar-item`「数据质量看板」→ `.tab`「数据质量看板」→ `.score-row` 存在 → 问题明细 `.issue .link` 转跳 M1。
2. 标准看板：`.sidebar-item`「数据标准看板」→ `.tab`「数据标准看板」→ `.score-row` 存在 → `.table tbody tr .link`「定位」转跳 M1。
3. 血缘看板：`.sidebar-item`「数据血缘看板」→ 列表 `.table tbody tr` ≥ 10 → 点「查看血缘图」→ `.lineage-node` ≥ 1 → 点节点 → `.field-table` + `.tab.active` 表名。
4. 批次文件：`.sidebar-item`「批次文件」→ `.table tbody tr` ≥ 5 → 点行进详情 → `.flow-step` ≥ 3 → 源表转跳 M1 → 占位按钮弹 modal。
5. 数据服务：`.sidebar-item`「数据服务」→ `.table tbody tr` ≥ 5 → 点行进详情 → `.flow-step` ≥ 3 → 封装资产转跳 M1 → 占位按钮弹 modal。

**`CLAUDE.md`** — 当前状态同步三期（血缘看板 + 数据交换两子菜单上线；M0 拆三看板；分组改名）。

### 验收

- `npm test` 全绿、`npm run build` 成功、`npm run test:page` PASS。
- final review subagent 全分支对抗式审查后 merge。

---

## 执行顺序

Task 2 → 3（可并行，串行 merge 更稳）→ 4 → 5 → 6 → 7。每任务：建分支 → 实现者 subagent（TDD）→ reviewer subagent → merge main。
