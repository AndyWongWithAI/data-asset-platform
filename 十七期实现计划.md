# 十七期 Implementation Plan：标准元数据写闭环（编辑/停用 + 批量导入）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 5 个标准元数据实体（基础术语/值域/参考数据/信息项/质量规则）补齐「编辑」「停用/启用」「批量导入」，形成 新增→改→停 完整写闭环。

**Architecture:** 后端 `store.js` 开 5 实体 `updatable` 闸 + 4 实体加 `status` 枚举与默认值；前端 `EntityForm` 加字段级「编辑时只读」与「引用下拉过滤停用项」（纯函数）；新建 `src/bulkImport.js` 纯函数（CSV 解析/模板/逐条导入）+ `src/components/BulkImport.jsx` UI；5 个模块行内接线。

**Tech Stack:** React 18 + Vite 5；Express 5 后端 JSON 落盘；`node --test` + playwright。

## Global Constraints

- **停用语义**：停用=改 `status` 不删数据、可逆；新建/编辑表单的引用下拉**隐藏停用项**，但**历史已关联的停用项保留回显**（编辑记录时它已引用的停用项仍在下拉中）。
- **批量导入**：CSV 文件上传 + 模板下载；逐条走后端 `create` 校验，汇总「成功 N / 失败 M 行+原因」；**只新增，不支持 upsert**。
- **信息项中文名**：编辑 `infoItems` 时 `nameCn` 只读（改名=重新登记新项）。
- 编辑表单**不显示 `status` 字段**（停用/启用只走行内按钮）；`qualityRules` 表单里原有的 `status` 下拉保留不动。
- 不触碰鉴权/密钥；不碰广发「数据资源目录」术语；不碰门户侧 `src/portal/`；`meta.disclaimer` 不变。
- 后端 `server/data.json` 是 gitignored 运行时缓存：改种子后需 `rm server/data.json` 重种。
- git-guardrails L0：main 只接受 merge 提交，开发走 `git checkout -b task/...` 再 `git merge --no-ff`。
- 每次开发完成后必跑 `npm run test:page`。

---

### Task 1: 后端 —— 4 实体 status 字段 + 5 实体 updatable 开闸

**Files:**
- Modify: `src/data.js`（baseTerms/valueDomains/refDatas/infoItems 各条补 `status: '启用'`）
- Modify: `server/store.js`（SCHEMAS 加 `enum.status` + `default` + `updatable: true`；`create` 注入 default）
- Modify: `test/api.test.mjs`（更新 UPDATABLE 断言 + 加 update/status 测试）
- Modify: `test/data.test.mjs`（加 status 存在断言）

**Interfaces:**
- Produces: 4 实体每条含 `status ∈ {'启用','停用'}`；`UPDATABLE` 变为 6 实体；`create` 未传 status 时默认 `'启用'`。供 Task 2/3/4 消费。

- [ ] **Step 1: data.js 补 status（13 次 replace_all / 精确替换）**

在 `src/data.js` 上执行以下替换（**全部 `replace_all: true`**，次数已核实精确匹配，不会误伤其他实体）：

```
# baseTerms 类词（10 条）
old: isClassWord: true }
new: isClassWord: true, status: '启用' }

# baseTerms 修饰词根（18 条）
old: isClassWord: false }
new: isClassWord: false, status: '启用' }

# valueDomains（precision 0/1/2 各 3/1/2 条）
old: precision: 0 }
new: precision: 0, status: '启用' }
old: precision: 1 }
new: precision: 1, status: '启用' }
old: precision: 2 }
new: precision: 2, status: '启用' }

# infoItems（L2/L3/L4 各 8/1/1 条，精确 ` }` 结尾，不碰 fields 嵌套）
old: securityLevel: 'L2' }
new: securityLevel: 'L2', status: '启用' }
old: securityLevel: 'L3' }
new: securityLevel: 'L3', status: '启用' }
old: securityLevel: 'L4' }
new: securityLevel: 'L4', status: '启用' }
```

refDatas 5 条（`values` 前插入，`name: 'XXX', values: [` 各唯一）：

```
old: name: '电压等级', values: [
new: name: '电压等级', status: '启用', values: [

old: name: '风机机型', values: [
new: name: '风机机型', status: '启用', values: [

old: name: '基础型式', values: [
new: name: '基础型式', status: '启用', values: [

old: name: '海域', values: [
new: name: '海域', status: '启用', values: [

old: name: '海缆类型', values: [
new: name: '海缆类型', status: '启用', values: [
```

- [ ] **Step 2: 加 data.test.mjs 断言（先写失败测试）**

在 `test/data.test.mjs` 末尾追加：

```js
test('4 实体每条含 status ∈ {启用,停用}（qualityRules 除外）', () => {
  for (const key of ['baseTerms', 'valueDomains', 'refDatas', 'infoItems']) {
    for (const it of D[key]) {
      assert.ok(it.status === '启用' || it.status === '停用', `${key} ${it.id} status 缺失/非法`);
    }
  }
});
```

Run: `node --test test/data.test.mjs` → 若 Step 1 未完成则 FAIL；完成后 PASS。

- [ ] **Step 3: store.js 开闸 + status + default**

`server/store.js` 的 `SCHEMAS` 做以下改动：

① 5 实体 `updatable: false` → `updatable: true`（baseTerms / valueDomains / refDatas / infoItems / qualityRules）。

② 4 实体加 `enum.status` + `default`：

```js
  baseTerms: {
    idKey: 'id', idPrefix: 'term', creatable: true, updatable: true,
    required: ['nameCn', 'nameEn', 'isClassWord'],
    unique: ['nameEn'],
    enum: { status: ['启用', '停用'] },
    types: { synonyms: 'array', isClassWord: 'bool' },
    refs: {},
    default: { status: '启用' },
  },
  valueDomains: {
    idKey: 'id', idPrefix: 'vd', creatable: true, updatable: true,
    required: ['code', 'dataType', 'length', 'precision'],
    unique: ['code'],
    enum: { dataType: ['varchar', 'decimal'], status: ['启用', '停用'] },
    types: { length: 'number', precision: 'number' },
    refs: {},
    default: { status: '启用' },
  },
  refDatas: {
    idKey: 'id', idPrefix: 'rd', creatable: true, updatable: true,
    required: ['code', 'name', 'values'],
    unique: ['code'],
    enum: { status: ['启用', '停用'] },
    types: { values: 'array' },
    refs: {},
    default: { status: '启用' },
  },
  infoItems: {
    idKey: 'id', idPrefix: 'ii', creatable: true, updatable: true,
    required: ['code', 'nameCn', 'nameEn', 'type', 'termIds', 'valueDomainId'],
    unique: ['code', 'nameEn'],
    enum: { type: ['技术', '业务'], securityLevel: ['L1', 'L2', 'L3', 'L4'], status: ['启用', '停用'] },
    types: { termIds: 'array' },
    refs: { valueDomainId: 'valueDomains', refDataId: 'refDatas', bizDomainId: 'bizDomains', termIds: 'baseTerms' },
    default: { status: '启用' },
  },
  qualityRules: {
    idKey: 'id', idPrefix: 'qr', creatable: true, updatable: true,
    required: ['name', 'type', 'targetFieldId', 'expr', 'threshold', 'severity', 'status'],
    unique: [],
    enum: { type: ['准确性', '完整性', '一致性', '及时性'], severity: ['严重', '警告', '提示'], status: ['启用', '停用'] },
    types: {},
    refs: { targetFieldId: 'fields' },
  },
```

③ `create` 函数注入 default，把这一行：

```js
  const record = { ...finalPayload, [schema.idKey]: id }; // 服务端生成 id 优先，客户端不可注入
```

改为：

```js
  const record = { ...(schema.default || {}), ...finalPayload, [schema.idKey]: id }; // default 铺底，服务端生成 id 优先，客户端不可注入
```

- [ ] **Step 4: 更新 api.test.mjs 断言 + 加 update 测试**

把 `test/api.test.mjs` 中：

```js
test('CREATABLE 5 实体 / UPDATABLE 仅 security', () => {
  assert.deepEqual(CREATABLE.sort(), ['baseTerms', 'infoItems', 'qualityRules', 'refDatas', 'valueDomains'].sort());
  assert.deepEqual(UPDATABLE, ['security']);
});
```

改为：

```js
test('CREATABLE 5 实体 / UPDATABLE 6 实体', () => {
  assert.deepEqual(CREATABLE.sort(), ['baseTerms', 'infoItems', 'qualityRules', 'refDatas', 'valueDomains'].sort());
  assert.deepEqual(UPDATABLE.sort(), ['baseTerms', 'infoItems', 'qualityRules', 'refDatas', 'security', 'valueDomains'].sort());
});
```

在文件末尾追加：

```js
test('create 未传 status → 默认启用（4 实体）', () => {
  freshStore();
  const r = create('valueDomains', { code: 'VD-TEST', dataType: 'varchar', length: 20, precision: 0 });
  assert.equal(r.ok, true);
  assert.equal(r.record.status, '启用');
});

test('update baseTerms：改名成功 + 落盘', () => {
  const dataFile = freshStore();
  const r = update('baseTerms', 'term_name', { nameCn: '名称（改）' });
  assert.equal(r.ok, true);
  assert.equal(r.record.nameCn, '名称（改）');
  init({ dataFile });
  assert.equal(getState().baseTerms.find((t) => t.id === 'term_name').nameCn, '名称（改）');
});

test('update 停用/启用：status 枚举 + 可逆', () => {
  freshStore();
  const off = update('valueDomains', 'vd_varchar10', { status: '停用' });
  assert.equal(off.ok, true);
  assert.equal(off.record.status, '停用');
  const on = update('valueDomains', 'vd_varchar10', { status: '启用' });
  assert.equal(on.ok, true);
  assert.equal(on.record.status, '启用');
});

test('update status 非法值 → 报错', () => {
  freshStore();
  const r = update('baseTerms', 'term_name', { status: '废弃' });
  assert.equal(r.ok, false);
  assert.ok(r.errors.some((e) => e.includes('status')));
});

test('update 主键不可篡改', () => {
  freshStore();
  const r = update('baseTerms', 'term_name', { id: 'term_hacked', nameCn: '篡改' });
  assert.equal(r.ok, true);
  assert.equal(r.record.id, 'term_name'); // id 仍为原值
});
```

- [ ] **Step 5: 跑全量单测**

Run: `npm test` → Expected: 全绿（含新增 status/update 测试）。

- [ ] **Step 6: Commit**

```bash
git add src/data.js server/store.js test/api.test.mjs test/data.test.mjs
git commit -m "feat(十七期): 4 实体 status 字段 + 5 实体 updatable 开闸 + update/status 测试"
```

---

### Task 2: 引擎 —— EntityForm 编辑只读 + 引用下拉过滤停用项

**Files:**
- Create: `src/entityFilter.js`（纯函数 `filterRefOptions`）
- Create: `test/entityFilter.test.mjs`
- Modify: `src/components/EntityForm.jsx`（readonlyOnUpdate + ref/multiref 下拉接 filterRefOptions）
- Modify: `src/schema.js`（infoItems.nameCn 加 `readonlyOnUpdate: true`）

**Interfaces:**
- Consumes: Task 1 的 `status` 字段。
- Produces: `filterRefOptions(options, record, fieldKey)`；EntityForm 支持 `readonlyOnUpdate`（供 Task 3 的 infoItems 编辑）。

- [ ] **Step 1: 创建 src/entityFilter.js**

```js
// 引用下拉选项过滤：隐藏 status='停用' 的项，但保留当前 record 已引用的停用项（编辑回显）。
// options 为被引用实体的完整数组；fieldKey 为当前表单字段（对应 FORM_SCHEMAS 的 ref/multiref 字段 key）。
export function filterRefOptions(options, record, fieldKey) {
  const current = record ? record[fieldKey] : undefined;
  const currentIds = new Set(Array.isArray(current) ? current : current ? [current] : []);
  return (options || []).filter((o) => o.status !== '停用' || currentIds.has(o.id));
}
```

- [ ] **Step 2: 创建 test/entityFilter.test.mjs（先写测试）**

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { filterRefOptions } from '../src/entityFilter.js';

const opts = [
  { id: 'a', status: '启用' },
  { id: 'b', status: '停用' },
  { id: 'c', status: '启用' },
];

test('过滤停用项', () => {
  assert.deepEqual(filterRefOptions(opts, null, 'ref').map((o) => o.id), ['a', 'c']);
});

test('保留当前 record 已引用的停用项（单值 ref）', () => {
  assert.deepEqual(filterRefOptions(opts, { ref: 'b' }, 'ref').map((o) => o.id), ['a', 'b', 'c']);
});

test('保留当前 record 已引用的停用项（multiref 数组）', () => {
  assert.deepEqual(filterRefOptions(opts, { terms: ['b', 'c'] }, 'terms').map((o) => o.id), ['a', 'b', 'c']);
});

test('无 status 字段的实体不受过滤影响', () => {
  const noStatus = [{ id: 'x' }, { id: 'y' }];
  assert.deepEqual(filterRefOptions(noStatus, null, 'ref').map((o) => o.id), ['x', 'y']);
});
```

Run: `node --test test/entityFilter.test.mjs` → Expected: PASS。

- [ ] **Step 3: EntityForm 支持 readonlyOnUpdate + 下拉过滤**

`src/components/EntityForm.jsx` 改动：

① 顶部 import 加 `filterRefOptions`：

```js
import { filterRefOptions } from '../entityFilter.js';
```

② `buildPayload` 的派生/只读字段跳过条件（约 44 行）：

```js
    if (f.type === 'derived' || f.readonly) continue;
```

改为：

```js
    if (f.type === 'derived' || f.readonly || (mode === 'update' && f.readonlyOnUpdate)) continue;
```

③ `renderField` 的只读判断（约 188 行）：

```jsx
    if (f.readonly) {
      control = <div className="derived-value">{val ?? '—'}</div>;
    } else if (f.type === 'derived') {
```

改为：

```jsx
    if (f.readonly || (mode === 'update' && f.readonlyOnUpdate)) {
      control = <div className="derived-value">{val ?? '—'}</div>;
    } else if (f.type === 'derived') {
```

④ ref 下拉（约 210-219 行），把 `{(data[f.ref] || []).map((item) => (` 改为用过滤后的列表：

```jsx
        case 'ref':
          control = (
            <select className="form-input" value={val ?? ''} onChange={(e) => setValue(f.key, e.target.value)}>
              <option value="">请选择</option>
              {filterRefOptions(data[f.ref], record, f.key).map((item) => (
                <option key={item.id} value={item.id}>{labelOf(f.ref, item)}</option>
              ))}
            </select>
          );
          break;
```

⑤ multiref 下拉（约 220-234 行），把 `{(data[f.ref] || []).map((item) => {` 改为 `{filterRefOptions(data[f.ref], record, f.key).map((item) => {`（其余逻辑不变）。

- [ ] **Step 4: schema.js 信息项中文名只读**

`src/schema.js` 的 `infoItems` 数组第一条：

```js
    { key: 'nameCn', label: '中文名', type: 'text', required: true, placeholder: '如：风机标识', help: '输入后自动拆词翻译，英文名与词根自动派生' },
```

改为（加 `readonlyOnUpdate: true`）：

```js
    { key: 'nameCn', label: '中文名', type: 'text', required: true, readonlyOnUpdate: true, placeholder: '如：风机标识', help: '编辑时只读；输入后自动拆词翻译，英文名与词根自动派生' },
```

- [ ] **Step 5: 构建 + 单测**

Run: `npm run build` → Expected: 构建通过。
Run: `npm test` → Expected: 全绿（含 entityFilter 测试）。

- [ ] **Step 6: Commit**

```bash
git add src/entityFilter.js test/entityFilter.test.mjs src/components/EntityForm.jsx src/schema.js
git commit -m "feat(十七期): EntityForm 编辑只读 + 引用下拉过滤停用项"
```

---

### Task 3: 5 模块编辑/停用接线

**Files:**
- Modify: `src/modules/BaseTermModule.jsx`
- Modify: `src/modules/ValueDomainModule.jsx`
- Modify: `src/modules/RefDataModule.jsx`
- Modify: `src/modules/InfoItemModule.jsx`
- Modify: `src/modules/QualityModule.jsx`

**Interfaces:**
- Consumes: Task 1 的 `updateRecord` + `status`；Task 2 的 `EntityForm mode="update"` + 下拉过滤。
- Produces: 每模块行内「编辑」（打开编辑表单）+「停用/启用」（按 status 切换文案 + PUT）+ 状态列 Tag。

**模式（5 模块一致，以 BaseTermModule 为例给完整代码，其余按同模式改造列）**：

- [ ] **Step 1: BaseTermModule.jsx 完整替换**

```jsx
import { useState } from 'react';
import { useData } from '../DataContext.jsx';
import Tag from '../components/Tag.jsx';
import EntityForm from '../components/EntityForm.jsx';
import ComingSoonAction from '../components/ComingSoonAction.jsx';

export default function BaseTermModule() {
  const { data, updateRecord } = useData();
  const [kind, setKind] = useState('all');
  const [showForm, setShowForm] = useState(false);
  const [editItem, setEditItem] = useState(null);

  const filtered = data.baseTerms.filter((t) =>
    kind === 'all' || (kind === 'classWord' ? t.isClassWord : !t.isClassWord)
  );
  const preview = (t) =>
    t.synonyms.length
      ? t.synonyms.slice(0, 3).join('，') + (t.synonyms.length > 3 ? '…' : '')
      : '—';
  const toggleStatus = (t) => updateRecord('baseTerms', t.id, { status: t.status === '停用' ? '启用' : '停用' });
  return (
    <div>
      <div className="search-bar">
        <select value={kind} onChange={(e) => setKind(e.target.value)} aria-label="按是否类词筛选">
          <option value="all">全部</option>
          <option value="classWord">类词</option>
          <option value="nonClassWord">非类词</option>
        </select>
        <button className="btn-primary" onClick={() => setShowForm(true)}>新增术语</button>
        <ComingSoonAction label="批量导入" />
      </div>
      <table className="table">
        <thead><tr><th>中文名</th><th>英文名</th><th>同义词</th><th>是否类词</th><th>状态</th><th>操作</th></tr></thead>
        <tbody>
          {filtered.map((t) => (
            <tr key={t.id}>
              <td>{t.nameCn}</td>
              <td><code>{t.nameEn}</code></td>
              <td>{preview(t)}</td>
              <td>{t.isClassWord ? <Tag tone="ok">类词</Tag> : '—'}</td>
              <td>{t.status === '停用' ? <Tag tone="warn">停用</Tag> : <Tag tone="ok">启用</Tag>}</td>
              <td><button className="link" onClick={() => setEditItem(t)}>编辑</button> <button className="link" onClick={() => toggleStatus(t)}>{t.status === '停用' ? '启用' : '停用'}</button></td>
            </tr>
          ))}
        </tbody>
      </table>
      {showForm && <EntityForm entity="baseTerms" onClose={() => setShowForm(false)} onSaved={() => setShowForm(false)} />}
      {editItem && <EntityForm entity="baseTerms" mode="update" record={editItem} onClose={() => setEditItem(null)} onSaved={() => setEditItem(null)} />}
    </div>
  );
}
```

- [ ] **Step 2: ValueDomainModule.jsx 同模式改造**

改动点：`const { data, updateRecord } = useData();`；加 `editItem` state；`toggleStatus`；表头加「状态」列；行内「编辑/停用」占位替换为真按钮；末尾加 `editItem` 的 EntityForm（`mode="update" record={editItem}`）。

```jsx
import { useState } from 'react';
import { useData } from '../DataContext.jsx';
import Tag from '../components/Tag.jsx';
import EntityForm from '../components/EntityForm.jsx';
import ComingSoonAction from '../components/ComingSoonAction.jsx';

export default function ValueDomainModule({ onNavigate }) {
  const { data, updateRecord } = useData();
  const [showForm, setShowForm] = useState(false);
  const [editItem, setEditItem] = useState(null);
  const toggleStatus = (v) => updateRecord('valueDomains', v.id, { status: v.status === '停用' ? '启用' : '停用' });
  return (
    <div>
      <div className="search-bar">
        <button className="btn-primary" onClick={() => setShowForm(true)}>新增值域</button>
        <ComingSoonAction label="批量导入" />
      </div>
      <table className="table">
        <thead><tr><th>值域编号</th><th>数据类型</th><th>长度</th><th>精度</th><th>状态</th><th>操作</th></tr></thead>
        <tbody>
          {data.valueDomains.map((v) => (
            <tr key={v.id}>
              <td><button className="link" onClick={() => onNavigate('valueDomainDetail', { valueDomainId: v.id })}>{v.code}</button></td>
              <td><code>{v.dataType}</code></td>
              <td>{v.length}</td>
              <td>{v.precision}</td>
              <td>{v.status === '停用' ? <Tag tone="warn">停用</Tag> : <Tag tone="ok">启用</Tag>}</td>
              <td><button className="link" onClick={() => setEditItem(v)}>编辑</button> <button className="link" onClick={() => toggleStatus(v)}>{v.status === '停用' ? '启用' : '停用'}</button></td>
            </tr>
          ))}
        </tbody>
      </table>
      {showForm && <EntityForm entity="valueDomains" onClose={() => setShowForm(false)} onSaved={() => setShowForm(false)} />}
      {editItem && <EntityForm entity="valueDomains" mode="update" record={editItem} onClose={() => setEditItem(null)} onSaved={() => setEditItem(null)} />}
    </div>
  );
}
```

- [ ] **Step 3: RefDataModule.jsx 同模式改造**

```jsx
import { useState } from 'react';
import { useData } from '../DataContext.jsx';
import Tag from '../components/Tag.jsx';
import EntityForm from '../components/EntityForm.jsx';
import ComingSoonAction from '../components/ComingSoonAction.jsx';

export default function RefDataModule({ onNavigate }) {
  const { data, updateRecord } = useData();
  const [showForm, setShowForm] = useState(false);
  const [editItem, setEditItem] = useState(null);
  const toggleStatus = (r) => updateRecord('refDatas', r.id, { status: r.status === '停用' ? '启用' : '停用' });
  return (
    <div>
      <div className="search-bar">
        <button className="btn-primary" onClick={() => setShowForm(true)}>新增参考数据</button>
        <ComingSoonAction label="批量导入" />
      </div>
      <table className="table">
        <thead><tr><th>编号</th><th>名称</th><th>枚举值数</th><th>状态</th><th>操作</th></tr></thead>
        <tbody>
          {data.refDatas.map((r) => (
            <tr key={r.id}>
              <td><button className="link" onClick={() => onNavigate('refDataDetail', { refDataId: r.id })}>{r.code}</button></td>
              <td>{r.name}</td>
              <td>{r.values.length}</td>
              <td>{r.status === '停用' ? <Tag tone="warn">停用</Tag> : <Tag tone="ok">启用</Tag>}</td>
              <td><button className="link" onClick={() => setEditItem(r)}>编辑</button> <button className="link" onClick={() => toggleStatus(r)}>{r.status === '停用' ? '启用' : '停用'}</button></td>
            </tr>
          ))}
        </tbody>
      </table>
      {showForm && <EntityForm entity="refDatas" onClose={() => setShowForm(false)} onSaved={() => setShowForm(false)} />}
      {editItem && <EntityForm entity="refDatas" mode="update" record={editItem} onClose={() => setEditItem(null)} onSaved={() => setEditItem(null)} />}
    </div>
  );
}
```

- [ ] **Step 4: InfoItemModule.jsx 同模式改造**

```jsx
import { useState } from 'react';
import { useData } from '../DataContext.jsx';
import Tag from '../components/Tag.jsx';
import EntityForm from '../components/EntityForm.jsx';
import ComingSoonAction from '../components/ComingSoonAction.jsx';

export default function InfoItemModule({ onNavigate }) {
  const { data, updateRecord } = useData();
  const [showForm, setShowForm] = useState(false);
  const [editItem, setEditItem] = useState(null);
  const vdCode = (id) => data.valueDomains.find((v) => v.id === id)?.code ?? '—';
  const rdName = (id) => (id ? data.refDatas.find((r) => r.id === id)?.name ?? '—' : '—');
  const toggleStatus = (i) => updateRecord('infoItems', i.id, { status: i.status === '停用' ? '启用' : '停用' });
  return (
    <div>
      <div className="search-bar">
        <button className="btn-primary" onClick={() => setShowForm(true)}>新增信息项</button>
        <ComingSoonAction label="批量导入" />
      </div>
      <table className="table">
        <thead><tr><th>信息项编号</th><th>中文名</th><th>英文名</th><th>类型</th><th>值域</th><th>参考数据</th><th>状态</th><th>操作</th></tr></thead>
        <tbody>
          {data.infoItems.map((i) => (
            <tr key={i.id}>
              <td><button className="link" onClick={() => onNavigate('infoItemDetail', { infoItemId: i.id })}>{i.code}</button></td>
              <td>{i.nameCn}</td>
              <td><code>{i.nameEn}</code></td>
              <td>{i.type}</td>
              <td><code>{vdCode(i.valueDomainId)}</code></td>
              <td>{rdName(i.refDataId)}</td>
              <td>{i.status === '停用' ? <Tag tone="warn">停用</Tag> : <Tag tone="ok">启用</Tag>}</td>
              <td><button className="link" onClick={() => setEditItem(i)}>编辑</button> <button className="link" onClick={() => toggleStatus(i)}>{i.status === '停用' ? '启用' : '停用'}</button></td>
            </tr>
          ))}
        </tbody>
      </table>
      {showForm && <EntityForm entity="infoItems" onClose={() => setShowForm(false)} onSaved={() => setShowForm(false)} />}
      {editItem && <EntityForm entity="infoItems" mode="update" record={editItem} onClose={() => setEditItem(null)} onSaved={() => setEditItem(null)} />}
    </div>
  );
}
```

- [ ] **Step 5: QualityModule.jsx 同模式改造（status 列已有，补「编辑」真按钮 + 停用/启用）**

QualityModule 已有「状态」列显示 `r.status`。改动：`const { data, updateRecord } = useData();`；加 `editItem` state；`toggleStatus`；把状态列 `<td>{r.status}</td>` 改成 Tag；行内「编辑/停用」占位替换为真按钮；末尾加 `editItem` EntityForm。

```jsx
import { useState } from 'react';
import { useData } from '../DataContext.jsx';
import Tag from '../components/Tag.jsx';
import EntityForm from '../components/EntityForm.jsx';
import ComingSoonAction from '../components/ComingSoonAction.jsx';

const SEVERITY_TONE = { 严重: 'danger', 警告: 'warn', 提示: 'default' };

export default function QualityModule({ onNavigate }) {
  const { data, updateRecord } = useData();
  const [type, setType] = useState('all');
  const [showForm, setShowForm] = useState(false);
  const [editItem, setEditItem] = useState(null);
  const filtered = data.qualityRules.filter((r) => type === 'all' || r.type === type);
  const toggleStatus = (r) => updateRecord('qualityRules', r.id, { status: r.status === '停用' ? '启用' : '停用' });
  return (
    <div>
      <div className="search-bar">
        <select value={type} onChange={(e) => setType(e.target.value)} aria-label="按类型筛选">
          <option value="all">全部类型</option>
          {['准确性', '完整性', '一致性', '及时性'].map((t) => <option key={t} value={t}>{t}</option>)}
        </select>
        <button className="btn-primary" onClick={() => setShowForm(true)}>新增规则</button>
        <ComingSoonAction label="批量导入" />
      </div>
      <table className="table">
        <thead><tr><th>规则名</th><th>类型</th><th>绑定字段</th><th>校验表达式</th><th>阈值</th><th>严重级别</th><th>状态</th><th>操作</th></tr></thead>
        <tbody>
          {filtered.map((r) => {
            const f = data.fields.find((x) => x.id === r.targetFieldId);
            return (
              <tr key={r.id}>
                <td><button className="link" onClick={() => onNavigate('qualityDetail', { qualityRuleId: r.id })}>{r.name}</button></td>
                <td>{r.type}</td>
                <td>{f?.business.nameCn}</td>
                <td><code>{r.expr}</code></td>
                <td>{r.threshold}</td>
                <td><Tag tone={SEVERITY_TONE[r.severity]}>{r.severity}</Tag></td>
                <td>{r.status === '停用' ? <Tag tone="warn">停用</Tag> : <Tag tone="ok">启用</Tag>}</td>
                <td><button className="link" onClick={() => setEditItem(r)}>编辑</button> <button className="link" onClick={() => toggleStatus(r)}>{r.status === '停用' ? '启用' : '停用'}</button></td>
              </tr>
            );
          })}
        </tbody>
      </table>
      {showForm && <EntityForm entity="qualityRules" onClose={() => setShowForm(false)} onSaved={() => setShowForm(false)} />}
      {editItem && <EntityForm entity="qualityRules" mode="update" record={editItem} onClose={() => setEditItem(null)} onSaved={() => setEditItem(null)} />}
    </div>
  );
}
```

- [ ] **Step 6: 构建 + 单测**

Run: `npm run build` → Expected: 构建通过。
Run: `npm test` → Expected: 全绿。

- [ ] **Step 7: Commit**

```bash
git add src/modules/BaseTermModule.jsx src/modules/ValueDomainModule.jsx src/modules/RefDataModule.jsx src/modules/InfoItemModule.jsx src/modules/QualityModule.jsx
git commit -m "feat(十七期): 5 模块编辑/停用/启用接线 + 状态列"
```

---

### Task 4: 批量导入纯函数

**Files:**
- Create: `src/bulkImport.js`
- Create: `test/bulkImport.test.mjs`

**Interfaces:**
- Consumes: `FORM_SCHEMAS`（schema.js）+ `api.create`。
- Produces: `buildColumns(entity)`、`serializeCell(field, value)`、`buildTemplateCsv(entity, sample)`、`parseCsv(text)`、`rowToPayload(entity, headers, cells)`、`importRows(entity, rows)`。供 Task 5 的 BulkImport UI 消费。

- [ ] **Step 1: 创建 src/bulkImport.js**

```js
import { FORM_SCHEMAS } from './schema.js';
import { create } from './api.js';

// 批量导入纯函数：列定义 / CSV 解析 / 模板生成 / 逐条导入。不依赖 React，可 node --test。

// 列定义：从 FORM_SCHEMAS 取非 derived/readonly 字段（readonlyOnUpdate 不影响 create，仍导入）
export function buildColumns(entity) {
  return (FORM_SCHEMAS[entity] || []).filter((f) => f.type !== 'derived' && !f.readonly);
}

// 单元格序列化：把种子示例值转成 CSV 单元格文本
export function serializeCell(field, value) {
  if (value == null) return '';
  if (field.type === 'subtable') {
    return (value || []).map((r) => `${r.code}:${r.name}`).join('|');
  }
  if (field.type === 'bool') return value ? 'true' : 'false';
  if (Array.isArray(value)) return value.join(', ');
  return String(value);
}

// 模板 CSV：表头 + 一行示例
export function buildTemplateCsv(entity, sample) {
  const cols = buildColumns(entity);
  const header = cols.map((f) => f.label);
  const row = cols.map((f) => serializeCell(f, sample ? sample[f.key] : undefined));
  return [header, row].map((cells) => cells.map((c) => `"${String(c).replace(/"/g, '""')}"`).join(',')).join('\n');
}

// CSV 解析：支持双引号包裹、引号内逗号、"" 转义。返回 string[][]（不含表头由调用方决定）
export function parseCsv(text) {
  const rows = [];
  let row = [];
  let cell = '';
  let inQuotes = false;
  for (let i = 0; i < text.length; i++) {
    const ch = text[i];
    if (inQuotes) {
      if (ch === '"') {
        if (text[i + 1] === '"') { cell += '"'; i++; }
        else inQuotes = false;
      } else cell += ch;
    } else if (ch === '"') {
      inQuotes = true;
    } else if (ch === ',') {
      row.push(cell); cell = '';
    } else if (ch === '\n' || ch === '\r') {
      if (ch === '\r' && text[i + 1] === '\n') i++;
      row.push(cell); cell = '';
      if (row.some((c) => c !== '')) rows.push(row);
      row = [];
    } else {
      cell += ch;
    }
  }
  row.push(cell);
  if (row.some((c) => c !== '')) rows.push(row);
  return rows;
}

// CSV 行 → payload：按字段 type 解析
export function rowToPayload(entity, headers, cells) {
  const cols = buildColumns(entity);
  const payload = {};
  cols.forEach((f, idx) => {
    const raw = (cells[idx] ?? '').trim();
    if (raw === '') return;
    if (f.type === 'subtable') {
      payload[f.key] = raw.split('|').filter(Boolean).map((seg) => {
        const [code, name] = seg.split(':');
        return { code: (code || '').trim(), name: (name || '').trim() };
      });
    } else if (f.multi) {
      payload[f.key] = raw.split(/[,，]/).map((s) => s.trim()).filter(Boolean);
    } else if (f.type === 'bool') {
      payload[f.key] = raw === 'true' || raw === '是' || raw === '1';
    } else if (f.type === 'number') {
      const n = parseFloat(raw);
      if (!Number.isNaN(n)) payload[f.key] = n;
    } else {
      payload[f.key] = raw;
    }
  });
  return payload;
}

// 逐条导入：headers 为 CSV 表头行（用于校验列序，也可不校验直接按 buildColumns 顺序）；rows 为数据行
export async function importRows(entity, rows) {
  const success = [];
  const errors = [];
  for (let i = 0; i < rows.length; i++) {
    const payload = rowToPayload(entity, null, rows[i]);
    try {
      const record = await create(entity, payload);
      success.push(record);
    } catch (e) {
      errors.push({ row: i + 1, errors: [e.message] });
    }
  }
  return { success, errors };
}
```

> 说明：`rowToPayload` 的 `headers` 参数保留但不用（列序固定按 `buildColumns`），避免 CSV 表头错位导致映射错乱。CSV 第一行（表头）由 UI 层解析后丢弃。

- [ ] **Step 2: 创建 test/bulkImport.test.mjs**

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { buildColumns, serializeCell, buildTemplateCsv, parseCsv, rowToPayload } from '../src/bulkImport.js';

test('buildColumns 排除 derived/readonly', () => {
  const cols = buildColumns('refDatas');
  assert.deepEqual(cols.map((c) => c.key), ['name', 'values']); // code 是 derived 排除
  const ii = buildColumns('infoItems');
  assert.deepEqual(ii.map((c) => c.key), ['nameCn', 'type', 'bizDomainId', 'definition', 'valueDomainId', 'refDataId', 'securityLevel']);
});

test('serializeCell subtable / bool / array', () => {
  assert.equal(serializeCell({ type: 'subtable' }, [{ code: '01', name: '35kV' }, { code: '02', name: '66kV' }]), '01:35kV|02:66kV');
  assert.equal(serializeCell({ type: 'bool' }, true), 'true');
  assert.equal(serializeCell({ type: 'text', multi: true }, ['a', 'b']), 'a, b');
});

test('buildTemplateCsv 生成表头 + 示例行', () => {
  const csv = buildTemplateCsv('valueDomains', { code: 'VD-X', dataType: 'varchar', length: 10, precision: 0 });
  assert.ok(csv.includes('编号'));
  assert.ok(csv.includes('VD-X'));
});

test('parseCsv 支持引号内逗号 + 转义', () => {
  const rows = parseCsv('"a,1",b\n"he said ""hi""",c\n');
  assert.deepEqual(rows, [['a,1', 'b'], ['he said "hi"', 'c']]);
});

test('rowToPayload 解析 multi/subtable/bool/number', () => {
  const p = rowToPayload('refDatas', null, ['电压等级X', '01:35kV|02:66kV']);
  assert.equal(p.name, '电压等级X');
  assert.deepEqual(p.values, [{ code: '01', name: '35kV' }, { code: '02', name: '66kV' }]);
  const bt = rowToPayload('baseTerms', null, ['名称X', 'name_x', '别名1, 别名2', 'true']);
  assert.equal(bt.nameCn, '名称X');
  assert.deepEqual(bt.synonyms, ['别名1', '别名2']);
  assert.equal(bt.isClassWord, true);
});
```

Run: `node --test test/bulkImport.test.mjs` → Expected: PASS。

- [ ] **Step 3: Commit**

```bash
git add src/bulkImport.js test/bulkImport.test.mjs
git commit -m "feat(十七期): 批量导入纯函数（CSV 解析/模板/逐条导入）+ 测试"
```

---

### Task 5: BulkImport UI + 5 模块批量导入接线

**Files:**
- Create: `src/components/BulkImport.jsx`
- Modify: `src/modules/BaseTermModule.jsx`、`ValueDomainModule.jsx`、`RefDataModule.jsx`、`InfoItemModule.jsx`、`QualityModule.jsx`（`<ComingSoonAction label="批量导入" />` → `<BulkImport entity="..." />`）

**Interfaces:**
- Consumes: Task 4 的 `buildColumns` / `buildTemplateCsv` / `parseCsv` / `importRows`。
- Produces: `<BulkImport entity="..." onClose onSaved />` 弹窗（模板下载 + 上传 + 逐条导入 + 汇总）。

- [ ] **Step 1: 创建 src/components/BulkImport.jsx**

```jsx
import { useState, useRef } from 'react';
import { useData } from '../DataContext.jsx';
import { buildColumns, buildTemplateCsv, parseCsv, importRows } from '../bulkImport.js';

// 批量导入弹窗：模板下载 + CSV 文件上传 + 逐条导入 + 结果汇总。纯前端，逐条走后端 create。
export default function BulkImport({ entity, onClose, onSaved }) {
  const { data } = useData();
  const [result, setResult] = useState(null);
  const [busy, setBusy] = useState(false);
  const fileRef = useRef(null);

  const cols = buildColumns(entity);
  const sample = (data[entity] || [])[0];

  const downloadTemplate = () => {
    const csv = buildTemplateCsv(entity, sample);
    const blob = new Blob(['﻿' + csv], { type: 'text/csv;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `${entity}-导入模板.csv`;
    a.click();
    URL.revokeObjectURL(url);
  };

  const handleFile = async (e) => {
    const file = e.target.files?.[0];
    if (!file) return;
    const text = await file.text();
    const rows = parseCsv(text);
    if (rows.length < 2) return; // 至少表头 + 1 数据行
    setBusy(true);
    const res = await importRows(entity, rows.slice(1)); // 丢弃表头
    setBusy(false);
    setResult(res);
    if (res.errors.length === 0) onSaved?.();
  };

  return (
    <div className="modal-mask" onClick={onClose}>
      <div className="modal modal-lg" onClick={(e) => e.stopPropagation()}>
        <h3>批量导入</h3>
        <div className="form-help">
          列顺序：{cols.map((f) => f.label).join(' / ')}
        </div>
        <div className="form-actions" style={{ justifyContent: 'flex-start', gap: 8 }}>
          <button className="btn-secondary" onClick={downloadTemplate}>下载模板</button>
          <button className="btn-secondary" onClick={() => fileRef.current?.click()}>{busy ? '导入中…' : '上传 CSV'}</button>
          <input ref={fileRef} type="file" accept=".csv" style={{ display: 'none' }} onChange={handleFile} />
        </div>
        {result && (
          <div className="form-help" style={{ marginTop: 8 }}>
            {result.errors.length === 0
              ? <span>导入完成：成功 {result.success.length} 条。</span>
              : <span>成功 {result.success.length} 条 / 失败 {result.errors.length} 行：{result.errors.map((e) => `第${e.row + 1}行 ${e.errors.join('；')}`).join('；')}</span>}
          </div>
        )}
        <div className="form-actions">
          <button className="btn-secondary" onClick={onClose}>关闭</button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 5 模块接线**

每个模块把顶部：

```jsx
        <ComingSoonAction label="批量导入" />
```

替换为：

```jsx
        <BulkImport entity="<entity名>" onClose={...} onSaved={...} />
```

并加 import 与 state。以 BaseTermModule 为例：

```jsx
import BulkImport from '../components/BulkImport.jsx';
// ...
  const [showImport, setShowImport] = useState(false);
// ...
        <button className="btn-secondary" onClick={() => setShowImport(true)}>批量导入</button>
// ...
      {showImport && <BulkImport entity="baseTerms" onClose={() => setShowImport(false)} onSaved={() => setShowImport(false)} />}
```

> 更简：可直接 `{showImport && <BulkImport entity="baseTerms" ... />}`，顶部按钮文案保持「批量导入」。5 模块 entity 名依次为：`baseTerms` / `valueDomains` / `refDatas` / `infoItems` / `qualityRules`。注意：替换后 `ComingSoonAction` import 若不再使用可移除（如无其他占位按钮）。

- [ ] **Step 3: 构建 + 单测**

Run: `npm run build` → Expected: 构建通过。
Run: `npm test` → Expected: 全绿。

- [ ] **Step 4: Commit**

```bash
git add src/components/BulkImport.jsx src/modules/BaseTermModule.jsx src/modules/ValueDomainModule.jsx src/modules/RefDataModule.jsx src/modules/InfoItemModule.jsx src/modules/QualityModule.jsx
git commit -m "feat(十七期): BulkImport 批量导入 UI + 5 模块接线"
```

---

### Task 6: e2e + 全量测试

**Files:**
- Modify: `test/page_test.py`（新增 编辑 / 批量导入 断言）

**Interfaces:**
- Consumes: Task 3 的「编辑/停用」按钮 + Task 5 的「批量导入」弹窗。

- [ ] **Step 1: 追加 e2e 断言**

在 `test/page_test.py` 中，找到「新增术语」断言块（约 146 行）之后，追加基础术语的编辑 + 批量导入断言；其余 4 模块在各自「新增X」断言块之后追加同模式断言。核心断言：

```python
        # 十七期：基础术语 编辑 + 批量导入
        # 编辑 → 表单弹窗（.modal-lg）→ 取消
        page.locator('.table tbody tr', has_text='名称').locator('button', has_text='编辑').first.click()
        assert page.locator('.modal-lg').count() == 1
        page.locator('.modal button', has_text='取消').click()
        assert page.locator('.modal').count() == 0
        # 批量导入 → 弹窗（含「下载模板」）→ 关闭
        page.locator('button', has_text='批量导入').click()
        assert page.locator('.modal').count() == 1
        assert page.locator('.modal button', has_text='下载模板').count() == 1
        page.locator('.modal button', has_text='关闭').click()
        assert page.locator('.modal').count() == 0
```

信息项额外断言中文名只读：

```python
        # 信息项编辑：中文名只读（derived-value 而非 input）
        page.locator('.table tbody tr', has_text='II0001').locator('button', has_text='编辑').first.click()
        assert page.locator('.modal-lg').count() == 1
        assert page.locator('.modal .derived-value', has_text='电压等级编码').count() == 1
        page.locator('.modal button', has_text='取消').click()
```

> 注意：`has_text='名称'` 会命中多个术语（名称/…），`.first` 兜底。停用/启用不在此 e2e 断言（真实写库会污染 data.json，由 Task 1 的 api.test.mjs 单测覆盖）。所有「编辑」断言只打开表单 + 取消，不提交不落盘。

- [ ] **Step 2: 起 dev server + 后端（rm 旧缓存重种）**

```bash
rm -f server/data.json       # 清掉旧缓存，让后端按新种子（含 status）重种
npm run server               # 后端 :8078（后台）
npm run dev                  # 前端 :5173（后台）
```

- [ ] **Step 3: 跑 e2e**

Run: `npm run test:page`
Expected: `page_test PASS`（含十七期编辑/批量导入断言）。

- [ ] **Step 4: 全量回归**

Run: `npm test` → 全绿。
Run: `npm run build` → 通过。

- [ ] **Step 5: Commit**

```bash
git add test/page_test.py
git commit -m "test(十七期): 编辑/批量导入 e2e 断言"
```

---

## 完成后

- 6 个任务全部完成后，在 main 上 `git merge --no-ff` 合回，跑一次全量 `npm test` + `npm run test:page` 确认全绿。
- 部署（outward-facing，由用户拍板）：后端 `server/store.js` 与种子 `src/data.js` 有改动，部署时需 **scp 更新后端 + `rm data.json` 重种 + `systemctl restart data-asset-platform`**（沿用十六期踩过的 stale-data.json 坑）；前端走 GH Actions `push main` 自动 build/rsync。
