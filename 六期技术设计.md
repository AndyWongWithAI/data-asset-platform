# 数据资源管理平台 · 六期技术设计

> 展示版（Demo）。六期范围：数据标准板块从「码表+字段标准」两分类，细化为「基础术语 / 值域 / 参考数据 / 信息项」四个独立模块（数据元标准体系）。
> 日期：2026-08-15 · 上游依据：`功能矩阵设计.md`(v4) → `五期技术设计.md` · 状态：已实现

---

## 0. 需求来源（用户原话）

> 当前的数据标准板块，设计过于简单，我需要以下几个更加细化的模块：
> 1. **基础术语**：保存基本词根的中文名、英文名、同义词、是否类词。（类词是用来表示实际存储数据类型的词汇，在极度严格的要求下，仅有下面的词根可以被定义为：名称、编码、量、值、日期、时间、日期时间、标识、百分比、率）
> 2. **值域**：表示数据的合法取值范围。元模型：值域编号、数据类型、长度、精度。
> 3. **参考数据**：通用的企业级枚举值集合。元模型含「编号与名称」（如 CK00021-客户类型），内部枚举值含「编号与名称」（01-个人客户、02-企业客户、03-政府客户、04-其它）。
> 4. **信息项**：字段级数据标准。元模型：信息项编号、信息项中文名、信息项英文名、值域编号（必选）、参考数据编号（可选）。信息项中文名由基础术语构成，必须以类词结尾；信息项英文名由中文名拆分后映射英文名。

**用户确认的两个决策**（2026-08-15 澄清）：
- 类词白名单 = 去重后 10 个（原话「量」重复出现，按原意去重）：**名称、编码、量、值、日期、时间、日期时间、标识、百分比、率**。
- 入口结构 = **拆成 4 个独立菜单项**（非「数据标准」下一个入口的 sub-tab）。

---

## 1. 现状与差距

现状（`src/data.js` `standards` 数组，6 条）：
- `kind: 'code'` 码表 5 条（电压等级/风机机型/基础型式/海域/海缆类型），枚举值内嵌 `values[]` 纯字符串。
- `kind: 'field'` 字段标准 1 条（std_power 有功功率），`fieldStd{name,type,unit,domain}`。
- `fields[].management.standardId` 引用 standards（10 个字段引用 6 种标准）。

差距：缺「词根-类词」体系、缺「值域」独立建模、码表枚举值无「编号」、字段标准未挂值域。要升级为标准的数据元（Data Element）四层体系。

---

## 2. 数据模型（data.js 新增 4 数组，删 standards）

### 2.1 baseTerms（基础术语 / 词根）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | `term_xxx` |
| nameCn | string | 中文名 |
| nameEn | string | 英文名（小写，供信息项英文名拼接） |
| synonyms | string[] | 同义词 |
| isClassWord | boolean | 是否类词 |

**类词白名单（isClassWord=true 的 nameCn 集合，精确等于这 10 个）**：
`名称 / 编码 / 量 / 值 / 日期 / 时间 / 日期时间 / 标识 / 百分比 / 率`

### 2.2 valueDomains（值域）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | `vd_xxx` |
| code | string | 值域编号（如 `VD-DEC52`） |
| dataType | string | 数据类型（varchar/decimal/...） |
| length | number | 长度 |
| precision | number | 精度（小数位） |

### 2.3 refDatas（参考数据）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | `rd_xxx` |
| code | string | 编号（如 `CK0001`） |
| name | string | 名称（如「电压等级」） |
| values | {code:string, name:string}[] | 枚举值（编号+名称） |

### 2.4 infoItems（信息项 / 数据元）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | `ii_xxx` |
| code | string | 信息项编号（如 `II0001`） |
| nameCn | string | 中文名（必须以类词结尾） |
| nameEn | string | 英文名（= termIds 映射 nameEn 下划线拼接） |
| termIds | string[] | 构成词根（有序，最后一个必须 isClassWord=true） |
| valueDomainId | string | 值域编号（必选，非空） |
| refDataId | string \| null | 参考数据编号（可选） |

**命名规则（硬约束，测试兜底）**：
- `nameCn` = 由 termIds 各词根 nameCn 顺序拼接，且以「类词」结尾（最后 term 的 isClassWord=true）。
- `nameEn` = `termIds.map(t => t.nameEn).join('_')`。

---

## 3. 种子数据设计

### 3.1 baseTerms（10 类词 + 18 修饰词根 = 28 条）

类词（10）：
`名称 name / 编码 code / 量 quantity / 值 value / 日期 date / 时间 time / 日期时间 datetime / 标识 identifier / 百分比 percent / 率 rate`

修饰词根（18）：`电压 voltage / 等级 level / 有功 active / 功率 power / 风机 turbine / 机型 model / 基础 foundation / 型式 pattern / 海缆 cable / 类型 type / 海域 sea_area / 预测 forecast / 精度 accuracy / 风速 wind_speed / 温度 temperature / 进度 progress / 项目 project / 主数据 master_data`

> 同义词、英文名以贴合海上风电场景、可读为先，subagent 实现时微调不影响契约。

### 3.2 valueDomains（6 条）

| id | code | dataType | length | precision |
|----|------|----------|--------|-----------|
| vd_varchar10 | VD-VARCHAR10 | varchar | 10 | 0 |
| vd_dec52 | VD-DEC52 | decimal | 7 | 2 |
| vd_dec51 | VD-DEC51 | decimal | 6 | 1 |
| vd_name | VD-NAME | varchar | 64 | 0 |
| vd_code | VD-CODE | varchar | 32 | 0 |
| vd_percent | VD-PERCENT | decimal | 6 | 2 |

### 3.3 refDatas（5 条，旧码表迁移）

| id | code | name | values |
|----|------|------|--------|
| rd_voltage | CK0001 | 电压等级 | 01-35kV, 02-66kV, 03-220kV, 04-500kV, 05-±500kV |
| rd_turbine | CK0002 | 风机机型 | 01-8MW, 02-12MW, 03-16MW, 04-16.6MW漂浮式 |
| rd_foundation | CK0003 | 基础型式 | 01-单桩, 02-导管架, 03-多桩, 04-漂浮式 |
| rd_sea_area | CK0004 | 海域 | 01-粤东, 02-粤西, 03-珠三角 |
| rd_cable | CK0005 | 海缆类型 | 01-集电, 02-送出, 03-三芯, 04-单芯 |

### 3.4 infoItems（10 条）

| id | code | nameCn | termIds | valueDomainId | refDataId |
|----|------|--------|---------|---------------|-----------|
| ii_voltage | II0001 | 电压等级编码 | 电压,等级,编码 | vd_varchar10 | rd_voltage |
| ii_active_power | II0002 | 有功功率值 | 有功,功率,值 | vd_dec52 | null |
| ii_turbine_model | II0003 | 风机机型名称 | 风机,机型,名称 | vd_name | rd_turbine |
| ii_foundation | II0004 | 基础型式名称 | 基础,型式,名称 | vd_name | rd_foundation |
| ii_sea_area | II0005 | 海域编码 | 海域,编码 | vd_code | rd_sea_area |
| ii_cable_type | II0006 | 海缆类型名称 | 海缆,类型,名称 | vd_name | rd_cable |
| ii_accuracy | II0007 | 预测精度百分比 | 预测,精度,百分比 | vd_percent | null |
| ii_wind_speed | II0008 | 风速值 | 风速,值 | vd_dec52 | null |
| ii_temperature | II0009 | 温度值 | 温度,值 | vd_dec51 | null |
| ii_progress | II0010 | 进度百分比 | 进度,百分比 | vd_percent | null |

### 3.5 fields.management.standardId 迁移（15 字段 → 10 信息项）

| 信息项 | 被引用字段 |
|--------|-----------|
| ii_voltage | f_cable_voltage, f_sub_voltage |
| ii_active_power | f_scada_power, f_forecast_power, f_turbine_power |
| ii_turbine_model | f_turbine_model, f_spare_turbine |
| ii_foundation | f_turbine_foundation |
| ii_sea_area | f_topo_coord |
| ii_cable_type | f_cable_type |
| ii_accuracy | f_forecast_accuracy |
| ii_wind_speed | f_wind_speed |
| ii_temperature | f_scada_temp, f_cable_temp |
| ii_progress | f_prog_progress |

> 新增引用（原 standardId=null）：f_forecast_accuracy / f_wind_speed / f_scada_temp / f_cable_temp / f_prog_progress。其余 10 字段按旧标准等价迁移。贯标率从 10/51 升至 15/51。

---

## 4. 引用完整性约束（单测兜底）

新增测试（`test/data.test.mjs`）：
1. baseTerms：id 唯一；`isClassWord=true` 的 nameCn 集合 == 白名单 10 个（精确相等）。
2. valueDomains：id/code 唯一。
3. refDatas：values 每条含 code+name；code 唯一。
4. infoItems：
   - `valueDomainId` 非空且存在；`refDataId` 为 null 或存在。
   - `termIds` 每个存在；**最后一个 term 的 isClassWord=true**。
   - `nameCn` 以「最后一个 term 的 nameCn」结尾（endsWith）。
   - `nameEn === termIds.map(t => t.nameEn).join('_')`。
5. fields：`management.standardId` 若非空则 ∈ infoItems（替换原 `ids(D.standards)` 断言）。

---

## 5. UI / 入口设计

### 5.1 侧边栏（state.js MODULE_GROUPS）

「设计态·定义」组，`standard` 一项拆为 4 项（自底向上依赖顺序）：

```js
{ key: 'baseTerm', title: '基础术语', implemented: true },
{ key: 'valueDomain', title: '值域', implemented: true },
{ key: 'refData', title: '参考数据', implemented: true },
{ key: 'infoItem', title: '信息项', implemented: true },
```

### 5.2 四个模块（每个「列表 + 详情」两段式，占位写按钮 ComingSoonAction，延续 demo 只读定位）

| 模块 | 列表列 | 详情 |
|------|--------|------|
| BaseTermModule 基础术语 | 中文名/英文名/同义词/是否类词（类词 Tag 高亮） | 同义词完整列表 |
| ValueDomainModule 值域 | 值域编号/数据类型/长度/精度 | 被引用信息项（转跳信息项） |
| RefDataModule 参考数据 | 编号/名称/枚举值数 | 枚举值表（编号+名称）+ 被引用信息项 |
| InfoItemModule 信息项 | 编号/中文名/英文名/值域/参考数据 | 完整元模型 + 构成词根链（高亮类词）+ 被引用字段（定位转跳 M1） |

- 信息项详情重点展示「中文名由基础术语构成、以类词结尾」的词根链，以及「英文名 = 词根英文名拼接」。
- 每个模块保留「被引用字段 → 定位 M1 表详情」反向转跳（延续现有模式）。

### 5.3 TabWorkspace 映射

删 `StandardModule` 引用，新增 4 个组件映射（moduleKey → 组件）。旧 `StandardModule.jsx` 删除。

### 5.4 生产态「数据标准看板」（StandardBoardModule）

口径不变（贯标率 = `standardId` 非空字段数 / 总字段数），`standardId` 现在指向信息项，数值自动变为 15/51。可加一行「信息项覆盖 X/10」但不强制。

---

## 6. 改动范围

- `src/data.js`：删 `standards`，新增 `baseTerms/valueDomains/refDatas/infoItems`；`fields[].management.standardId` 重指信息项；`meta.stats` 更新。
- `src/modules/TableDetailModule.jsx`：字段「关联标准」列改读 `data.infoItems`（`std.name` → `std.nameCn`）。
- `src/state.js`：`MODULE_GROUPS` 拆 4 项。
- `src/components/TabWorkspace.jsx`：映射 4 新组件，删 StandardModule。
- `src/modules/BaseTermModule.jsx` / `ValueDomainModule.jsx` / `RefDataModule.jsx` / `InfoItemModule.jsx`：新增。
- `src/modules/StandardModule.jsx`：删除。
- `src/index.css`：复用现有 `.table/.detail-panel/.kv-list/.search-bar`，如需词根链样式少量新增。
- `test/data.test.mjs`：新增 4 数组引用完整性测试，改 `standardId` 断言。
- `test/page_test.py`：重写 M3 数据标准断言（原「数据标准/新增标准」按钮已不存在），新增 4 模块 e2e。
- `六期技术设计.md`（本文件）、`CLAUDE.md`、`功能矩阵设计.md`：文档收尾。

---

## 7. 测试约定

- 单测（node --test）：覆盖 §4 全部约束。
- e2e（`npm run test:page`）：4 个模块各自列表 + 详情 + 占位弹框 + 信息项详情「定位」转跳 M1。
- 开发完成后必跑 `npm test` + `npm run build` + `npm run test:page`，全绿方可合并。

---

## 8. 任务分解（subagent-driven-development）

| 任务 | 分支 | 内容 |
|------|------|------|
| A | task/phase6-data-model | data.js 4 数组 + 迁移 + data.test.mjs 单测 |
| B | task/phase6-modules | state.js + 4 组件 + TabWorkspace + 删 StandardModule |
| C | task/phase6-e2e-doc | StandardBoard 口径 + page_test.py 重写 + 文档收尾 |

> 依赖顺序 A → B → C。每任务：实现者 subagent → reviewer subagent → merge。
