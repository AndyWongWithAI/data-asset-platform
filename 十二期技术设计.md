# 十二期技术设计 · 生产元数据比对

> 目标：① 文件交换入站任务从「采单表」升级「采整库元数据」；② 生产态治理面板新增「元数据比对」看板（差异清单，只读）。

## 一、数据模型

### 1. batchFiles 入站改「库级」

- 每条任务新增 `direction: 'outbound' | 'inbound'`（出站/入站）。
- **出站**（平台表 → 外部系统）字段不变：`sourceSystem / sourceTableId / sourceTableName / targetSystem / targetTableId / targetTableName`。
- **入站**（外部库 → 平台库）字段改为库级：`sourceSystem / sourceDatabaseName / sourceDatabaseType / targetSystem / targetDatabaseId / targetDatabaseName`（去掉 sourceTableId/sourceTableName/targetTableId/targetTableName）。

3 条入站样例（bf_006/007/008）映射：

| id | 来源 | sourceDatabaseName/Type | targetDatabaseId/Name |
|---|---|---|---|
| bf_006 | SCADA采集系统 | scada_telemetry_db / MySQL | db_ops / 运维库 |
| bf_007 | 气象站 | met_station_db / MySQL | db_res / 资源评估库 |
| bf_008 | BIM系统 | bim_model_db / Oracle | db_design / 设计库 |

`meta.stats` 不变（batchFiles 仍 8）。

### 2. 新增实体 `prodMetadatas`（生产元数据快照，只读）

顶层数组，每条 = 一个库级采集，`batchFileId` 关联交换申请。**不进 SCHEMAS（不可写）**，store.js `READABLE` 自动收录。

```js
{
  id: 'pm_scada', batchFileId: 'bf_006', sourceSystem: 'SCADA采集系统',
  databaseName: 'scada_telemetry_db', databaseType: 'MySQL',
  targetDatabaseId: 'db_ops', collectedAt: '2026-08-15 02:00',
  tables: [
    { nameEn: 'scada_telemetry', nameCn: 'SCADA遥测表',
      fields: [ { code, nameCn, type }, ... ] },
    ...
  ],
}
```

字段 `code` 对齐设计态 `fields[].business.code`，表 `nameEn` 对齐设计态 `tables[].nameEn`。

### 3. 生产元数据 mock 数据（制造有意义的差异）

**pm_scada**（→ db_ops，3 表）：
- `scada_telemetry`（已登记，对齐 t_scada），字段 6 个：
  - `TS/datetime`、`TURBINE_ID/varchar(32)`、`temperature_value/decimal(5,1)` 一致
  - `active_power_value/decimal(8,2)/有功功率` ← **漂移**（设计态 decimal(5,2)/有功功率值，类型+中文名双漂）
  - `gearbox_temp/decimal(5,1)/齿轮箱温度` ← **未登记字段**
  - `nacelle_yaw/decimal(6,2)/机舱偏航角` ← **未登记字段**
  - （设计态 t_scada 的 `VIBRATION` 生产没有 → **疑似下线字段**）
- `scada_alarm_raw`（SCADA告警原始表）← **未登记表**（字段 2~3 个随意）
- `scada_event_log`（SCADA事件日志表）← **未登记表**

**pm_met**（→ db_res，2 表）：
- `wind_measurement`（已登记，对齐 t_wind），字段 6 个：
  - `MEASURE_TIME/datetime`、`PROJECT_ID/varchar(32)`、`wind_speed_value/decimal(5,2)`、`TURBULENCE/varchar(10)` 一致
  - `WIND_DIR/int/风向` ← **漂移**（设计态 smallint，类型漂）
  - `wind_gust/decimal(5,2)/阵风风速` ← **未登记字段**
- `met_forecast_raw`（气象预报原始表）← **未登记表**

**pm_bim**（→ db_design，2 表）：
- `turbine_device`（已登记，对齐 t_turbine），字段 7 个：
  - `MD_ID/varchar(32)`、`turbine_model_name/varchar(20)`、`RATED_POWER/decimal(5,2)`、`SUPPLIER_ID/varchar(32)`、`PROJECT_ID/varchar(32)` 一致
  - `FOUNDATION/varchar(20)/基础形式` ← **漂移**（设计态 基础型式，中文名漂）
  - `blade_length/decimal(5,1)/叶片长度` ← **未登记字段**
- `bim_element_attr`（BIM构件属性表）← **未登记表**

**差异汇总（单测断言锚点）**：
- 未登记表 4（scada_alarm_raw/scada_event_log/met_forecast_raw/bim_element_attr）
- 疑似下线表 4（t_forecast/t_spare/t_cable/t_substation —— db_ops/db_design 被采集、清单无；db_res 仅 t_wind 已覆盖；db_survey/db_build 未采集不参与）
- 未登记字段 4（gearbox_temp/nacelle_yaw/wind_gust/blade_length）
- 疑似下线字段 1（VIBRATION）
- 漂移字段 3（active_power_value 类型+中文名 / WIND_DIR 类型 / FOUNDATION 中文名）

## 二、比对逻辑（纯函数 `src/metadataCompare.js`，可 node --test）

```js
export function compareMetadata(prodMetadatas, tables, fields) { ... }
```

- 对齐键：表 `nameEn`、字段 `business.code`。
- 表级：生产表 nameEn 不在设计态 → `unregistered`；设计态表 dbId ∈ 被采集 targetDatabaseId 集合且 nameEn 不在该生产库清单 → `offline`。
- 字段级（仅对已登记表）：生产 code 不在设计态 → `unregistered`；设计态 code 不在生产 → `offline`；都有但 `type` 或 `nameCn` 不一致 → `drift`（记录 drift 维度数组 `['type','nameCn']`）。
- 返回 `{ tableDiffs, fieldDiffs, summary }`，summary 含 unregisteredTables/offlineTables/unregisteredFields/offlineFields/driftedFields 五计数。

## 三、比对看板 UI（差异清单，只读）

新增模块 `metadataCompare`，挂在「生产态·治理看板」下（state.js MODULE_GROUPS 第一组 items 追加）。

- 顶部：5 个计数卡片（未登记表 / 疑似下线表 / 未登记字段 / 疑似下线字段 / 漂移字段）。
- 明细：按「表级差异」「字段级差异」两区块，每行展示差异类型 tag + 生产侧/设计态侧对照。
  - 表级漂移无（漂移仅字段级）。
  - 可点下钻：未登记表→无设计态不可跳；疑似下线表→跳 tableDetail；漂移/未登记字段→跳对应已登记表的 tableDetail。
- 只读，无写操作按钮。

## 四、文件交换 UI 改库级

FileExchangeModule 列表 + Detail：加「方向」列（出站/入站 tag），源/目标对象按方向渲染（出站=表名，入站=库名+库类型）。Detail 入站展示 sourceDatabaseName/Type + targetDatabaseId/Name，并列出关联的 prodMetadatas 快照（表数、采集时间）。

## 五、测试锚点

- `test/data.test.mjs`：batchFiles 入站含 direction/sourceDatabaseName/targetDatabaseId 无 sourceTableId；prodMetadatas 3 条、含 batchFileId 引用完整性。
- 新增 `test/metadataCompare.test.mjs`：比对逻辑五类差异计数精确断言（4/4/4/1/3）+ 漂移维度。
- `test/state.test.mjs`：MODULES 含 metadataCompare。
- `test/page_test.py`：文件交换「方向」列 + 比对看板卡片/明细断言。

## 六、复用与一致性

- 复用 Tag 组件做类型/方向/漂移 tag；复用 `tableDetail` 转跳（`onNavigate('tableDetail', { tableId, title })`）。
- 比对逻辑为纯函数（不依赖 React），与 store.js 领域规则同风格，单测兜底。
- 免责声明不变；不触碰广发「数据资源目录」术语。
