# 十七期技术设计：标准元数据写闭环（编辑 / 停用 + 批量导入）

> 承接九期 P0 后端写操作 + P1 表单引擎。九期打通了「新增」，本期补齐「编辑 / 停用 / 批量导入」，让 5 个标准元数据实体拥有完整生命周期。对应前一轮优先级分析的 **Tier 0（批量导入）+ Tier 1（编辑/停用）**。

## 一、目标与范围

| 项 | 内容 |
|----|------|
| **目标** | 5 个标准元数据实体（基础术语 / 值域 / 参考数据 / 信息项 / 质量规则）支持「编辑」「停用/启用」「批量导入」，形成 新增→改→停 的完整写闭环 |
| **范围内** | `baseTerms` `valueDomains` `refDatas` `infoItems` `qualityRules` 5 实体 + 后端 `store.js` + 前端表单引擎 `EntityForm` + 新批量导入组件 |
| **范围外** | M1 表/字段写操作（Tier 2）、审批流类（服务上架/门户/批次，Tier 3）、主数据同步（masterData 外部同步不可写）、门户侧 `src/portal/` |

## 二、现状盘点（写能力）

九期已打通「新增」：后端 `store.js` 的 `SCHEMAS` 覆盖 5 个 creatable 实体 + `security` 可改；前端 `EntityForm` 已支持 `mode="create"`。缺口：

| 能力 | 现状 | 缺口 |
|------|------|------|
| 新增 | ✅ 后端 `create` + 前端 `EntityForm` | 无 |
| 编辑 | ❌ 5 实体 `updatable: false` | 后端开闸 + 前端接线 |
| 停用/启用 | ❌ 仅 `qualityRules` 有 `status` 字段，其余 4 实体无 | 加 `status` 字段 + 行内按钮 |
| 批量导入 | ❌ 全无 | 新建导入引擎 |

## 三、数据模型变更

**`status` 字段**（枚举 `启用` / `停用`）：

- `baseTerms` `valueDomains` `refDatas` `infoItems` 4 实体**新增** `status` 字段，种子数据全部补 `status: '启用'`（28+6+5+10=49 条）。
- `qualityRules` **已有** `status`，复用不动。
- 后端 `SCHEMAS` 为 4 实体加 `enum.status` + `default.status='启用'`（`create` 时未传则注入默认值）。
- 停用**不做物理删除**，只改状态；停用可逆（「启用」恢复）。

## 四、关键设计决策（已拍板）

1. **停用语义 —— 隐藏停用项 + 旧引用保留**
   新建/编辑表单的下拉**不再列出**停用项；但**历史已关联的停用项照常展示**（编辑某条记录时，它已引用的停用项仍在回显下拉中）。停用=不可新增引用，旧引用不追溯。

2. **批量导入 —— CSV 文件上传 + 模板下载**
   每个实体提供「模板下载」（表头 + 一行真实示例），上传 CSV 后逐条走后端 `create` 校验，汇总「成功 N 条 / 失败 M 行 + 原因」。批量导入**只新增，不支持 upsert**。

3. **信息项中文名只读**
   编辑 `infoItems` 时 `nameCn` 只读（英文名/词根/编号是系统按中文名派生的，改名会脱节）；编辑只改类型 / 业务域 / 定义 / 值域 / 参考数据 / 安全分级。

## 五、架构设计

### 5.1 后端 `store.js`（小改）

- 5 实体 `updatable: false → true`。
- 4 实体 SCHEMAS 加 `enum: { ..., status: ['启用','停用'] }` + `default: { status: '启用' }`。
- `create` 函数注入 default：`record = { ...(schema.default || {}), ...finalPayload, [idKey]: id }`。
- 复用现有 `update`（结构校验 + 主键不可篡改 + 落盘），无需改 update 逻辑。

### 5.2 前端引擎 `EntityForm`（中改）

- 支持字段级 `readonlyOnUpdate`：`mode==='update'` 时该字段只读（用于 infoItems 中文名），渲染只读 + 提交不带上该字段。
- ref/multiref 下拉过滤停用项：抽纯函数 `filterRefOptions(options, record, fieldKey)`，规则为「`status!=='停用'` 或 该 id 是当前 record 已引用的」。纯函数便于 node 单测。
- `schema.js` 的 `infoItems.nameCn` 加 `readonlyOnUpdate: true`。

### 5.3 批量导入（新建）

- **纯函数模块 `src/bulkImport.js`**（可 node 单测）：
  - `buildColumns(entity)` —— 从 `FORM_SCHEMAS[entity]` 取非 derived/readonly 字段生成列定义。
  - `buildTemplateCsv(entity)` —— 表头 + 示例行（取种子第一条真实值）。
  - `parseCsv(text)` —— 支持双引号包裹的 CSV 解析器（手写，不引依赖）。
  - `rowToPayload(entity, headers, cells)` —— CSV 行 → payload（按字段 type 解析：`multi` 逗号分隔、`subtable` 用 `code:name|code:name`、`bool`/`number` 转换、`ref`/`enum` 原样）。
  - `importRows(entity, rows)` —— 逐条 `create`，返回 `{ success[], errors[{row, errors[]}] }`。
- **UI 组件 `src/components/BulkImport.jsx`**：模板下载（Blob）+ 文件上传 + 逐条导入 + 结果汇总展示。

### 5.4 5 个模块接线（小改，模式一致）

每个模块行内「编辑」「停用」占位按钮替换为真按钮：

- **编辑**：`EntityForm mode="update" record={item}`（复用现有表单，预填）。
- **停用/启用**：按当前 `status` 切换文案，点击 `updateRecord(entity, id, { status: '停用'|'启用' })`，状态列用 `Tag` 展示。
- **批量导入**：顶部占位按钮替换为打开 `<BulkImport entity="..." />`。

## 六、任务分解（6 个 task，供 subagent-driven 执行）

| Task | 内容 | 依赖 |
|------|------|------|
| 1 | 后端：`data.js` 补 status + `store.js` updatable/status/default + `api.test.mjs` 更新 | — |
| 2 | 引擎：`EntityForm` readonlyOnUpdate + 下拉过滤 `filterRefOptions` + `schema.js` nameCn 只读 + 单测 | Task 1 |
| 3 | 5 模块编辑/停用接线（状态列 + 编辑表单 + 停用/启用按钮） | Task 1, 2 |
| 4 | 批量导入纯函数 `src/bulkImport.js` + `test/bulkImport.test.mjs` | — |
| 5 | `BulkImport.jsx` UI + 5 模块批量导入接线 | Task 4 |
| 6 | e2e `page_test.py` 新增断言 + 全量 `npm test` + `build` + `test:page` | 全部 |

## 七、红线与约束

- **不触碰鉴权/密钥**：本期不涉及任何 `auth/key/secret/token` 等字段；批量导入 CSV 不含密钥列。
- **不碰广发「数据资源目录」术语**。
- **不碰门户侧 `src/portal/`**。
- `meta.disclaimer` 不变（仍声明「非广东院实际部署、仅供演示」）。
- git-guardrails L0：main 只接受 merge 提交，开发走 `git checkout -b task/...` 再 `git merge --no-ff`。
- 每次开发完成后必跑 `npm run test:page`。
- 后端 `server/data.json` 是 gitignored 运行时缓存：本地开发改种子后需 `rm server/data.json` 重种（生产部署同理）。
