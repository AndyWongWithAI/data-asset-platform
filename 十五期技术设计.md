# 十五期技术设计 · 数据资产门户

> 目标：新增「数据资产门户」（业务视角）作为「数据资产管理平台」（技术视角）的**一体两面**——两者共享同一底层数据与后端，通过右上角按钮互为切换、可不同域名。门户上的资产由「门户管理」模块配置上架，且上架前置「业务方同意（业务数据治理专员审批）」、留痕，故门户资产**默认可共享**。
> 已拍板：资产粒度 = 表 + 服务（指标/报告留后续）；「数据服务」模块并存不合并；两个独立 SPA（Vite 多入口）；门户三页（资产目录 / 资产详情 / 我的申请）。

## 一、数据模型

### 新增顶层实体 `portalAssets`（门户资产，seed 8 条）

每条 `{ id, name, category, desc, dataOwner, govSpecialist, manager, usageType, securityLevel, tableIds, serviceIds, status, featured, listedAt, approval }`。字段语义：

| 字段 | 含义 | 说明 |
|---|---|---|
| `id` | `pa_001` | 主键 |
| `name` | 业务资产名 | 业务友好命名（非技术表名） |
| `category` | 业务分类 | 分类导航值域：风资源 / 海洋勘测 / 风机设备 / 运营监测 / 海域环境 |
| `desc` | 业务描述 | 口径、用途、更新频率，写给业务看的白话 |
| `dataOwner` | 责任业务方 | 谁授权共享 |
| `govSpecialist` | 业务数据治理专员 | 上架审批人（留痕） |
| `manager` | 数据管理人员 | 上架人 |
| `usageType` | `下载` / `申请` | 数据包=自助下载；服务=申请开通 |
| `securityLevel` | 安全分级 | L1-L4，继承底层（≥ 打包对象最高级） |
| `tableIds` | 打包的表 | 可空；与 serviceIds 至少其一非空 |
| `serviceIds` | 打包的服务 | 可空 |
| `status` | `已上架` / `已下架` / `审批中` | 门户只展示「已上架」 |
| `featured` | 是否推荐位 | 布尔 |
| `listedAt` | 上架时间 | 字符串 |
| `approval` | 审批链 | demo 静态：发起上架 → 业务数据治理专员审批 → 上架 |

Seed 8 条（覆盖 5 业务分类，引用现有 `tables` 10 张 + `services` 5 条）：

| id | name | category | usageType | securityLevel | tableIds | serviceIds | status |
|---|---|---|---|---|---|---|---|
| pa_001 | 海上风电场测风数据集 | 风资源 | 下载 | L2 | [t_wind] | [] | 已上架 |
| pa_002 | 风资源评估 API | 风资源 | 申请 | L2 | [] | [svc_001] | 已上架 |
| pa_003 | 海底地形测绘数据包 | 海洋勘测 | 下载 | L4 | [t_topo, t_cable] | [] | 已上架 |
| pa_004 | 地质钻孔数据集 | 海洋勘测 | 下载 | L3 | [t_geo] | [] | 已上架 |
| pa_005 | SCADA 实时运行监测 | 运营监测 | 申请 | L2 | [t_scada] | [svc_002] | 已上架 |
| pa_006 | 风机设备档案数据产品 | 风机设备 | 下载 | L2 | [t_turbine] | [svc_005] | 已上架 |
| pa_007 | 功率预测服务 | 运营监测 | 申请 | L2 | [] | [svc_003] | 已上架 |
| pa_008 | 海域限制因素共享 | 海域环境 | 下载 | L4 | [t_topo] | [svc_004] | 已上架 |

### 新增顶层实体 `portalRequests`（申请记录，demo，seed 4 条）

喂「我的申请」页，业务用户视角。`{ id, portalAssetId, applicant, type, status, requestAt }`：

| id | portalAssetId | applicant | type | status |
|---|---|---|---|---|
| pr_001 | pa_002 | 张工 | 开通 | 已开通 |
| pr_002 | pa_001 | 张工 | 下载 | 已下载 |
| pr_003 | pa_005 | 张工 | 开通 | 审批中 |
| pr_004 | pa_007 | 张工 | 开通 | 已开通 |

### 一致性与只读

- 引用完整性：`tableIds` ∈ `tables`、`serviceIds` ∈ `services`、`tableIds ∪ serviceIds ≠ ∅`。
- 治理一致性（默认可共享但保留分级）：`portalAsset.securityLevel >= max(打包对象的有效分级)`，用 `LEVEL_RANK` 断言。其中「打包对象的有效分级」：服务取 `service.securityLevel`；表取 `max(该表各字段 securityLevel)`（表无表级分级，字段分级已在十四期定级）。
- 两个实体均**只读**（不进 SCHEMAS，`store.js` READABLE 按 `Array.isArray(seed[k])` 自动收录 GET）；`meta.stats` 加 `portalAssets: 8, portalRequests: 4`。
- 后端部署依赖 seed 更新：**scp `src/data.js` + 删 `server/data.json` + 重启**（同十四期手法）。

## 二、门户侧（新 SPA，业务视角）

- 独立 SPA：Vite 多入口 `portal.html`，base `/data-asset-portal/`，入口 `src/portal/main.jsx`。
- 根组件 `PortalApp`：无侧边栏 tab 体系，改「顶部导航 + 三页切换」；右上角「**资产管理**」按钮 → 跳 `/data-asset-platform/`。
- 复用共享层：`src/data.js`（种子）、`src/api.js`、`src/DataContext.jsx`、`fieldSecurity` 等纯函数；**不复用**后台密集表格组件，另建业务化组件。
- 三页：
  1. **资产目录**（`PortalCatalog`）：顶部「分类导航 + 搜索框」，下方资产卡片/列表（资产名、分类、业务描述一句话、安全分级 Tag、`下载/申请` 标签）。仅展示 `status === 已上架`。
  2. **资产详情**（`PortalAssetDetail`）：业务介绍（口径/用途/更新频率）+ 责任业务方 / 业务数据治理专员 / 数据管理人员 + 打包的表与服务（点开看业务化字段口径，转跳后台 `tableDetail`）+ `下载`/`申请` 按钮（占位）。
  3. **我的申请**（`PortalMyRequests`）：`portalRequests` 列表（状态 Tag）。

## 三、管理平台侧

- `HeaderBar` 右上角加「**资产门户**」按钮 → 跳 `/data-asset-portal/`。
- 新增「**门户管理**」模块（设计态·定义 分组下的叶子项，`state.js` MODULE_GROUPS + `TabWorkspace` 分发）：
  - `PortalManagementModule`：上架资产列表（资产名/分类/责任业务方/状态/上架时间/推荐位）。
  - 详情（`PortalAssetManageDetail`）：审批链（demo 静态）+ 打包的表/服务。
  - 「发起上架 / 上架 / 下架」按钮**占位**（沿用 `ComingSoonAction`，本次不做真实流转）。

## 四、后端 & 部署

- 后端：`portalAssets` / `portalRequests` 只读 GET（无需新增写接口）。
- 前端 Vite 多入口：
  - `index.html` → 管理平台（base `/data-asset-platform/`，dist）
  - `portal.html` → 门户（base `/data-asset-portal/`，dist-portal）
- nginx 新增 `location /data-asset-portal/` → alias `/opt/demo/data-asset-portal/`（与 `/data-asset-platform/` 并存）。
- GH Actions `deploy.yml` 增加门户 build（`vite build --base=/data-asset-portal/ --outDir=dist-portal`）+ rsync 到 `/opt/demo/data-asset-portal/`。

## 五、测试锚点

- `test/data.test.mjs`：`portalAssets` 长度 8 / id 唯一 / `tableIds`、`serviceIds` 引用存在且至少其一非空 / `usageType` ∈ {下载,申请} / `status` ∈ {已上架,已下架,审批中} / `category` ∈ 5 分类 / `securityLevel >= max(打包对象分级)` / `approval` 非空含审批步骤；`portalRequests` 引用 `portalAssets`；`stats.portalAssets === 8`。
- `test/state.test.mjs`：MODULE_GROUPS 含 `portalManagement`。
- `test/page_test.py`（管理平台）：右上角「资产门户」链接存在 + 门户管理模块可达 + 上架列表 + 审批链详情 + 占位写按钮。
- 新增 `test/portal_page_test.py`（门户冒烟）：三页可达 + 分类导航筛选 + 搜索过滤 + 详情（打包表/服务 + 下载/申请占位）+ 我的申请列表 + 右上角「资产管理」回跳。

## 六、约束

- 内容红线：门户资产仍基于广东院公开业务信息，`meta.disclaimer` 声明不变；不虚构内部运营数据（业务方/专员/管理人员用人名/角色占位，非真实人名）。
- 审批流与申请动作本次均 **demo 化**（静态数据 + 占位按钮），不做真实写流转。
- 不触碰广发「数据资源目录」术语；指标/报告记为后续扩展，本次不实现。
- 门户资产默认只读，与「默认可共享」语义一致；安全分级仍透出（业务用户可感知数据敏感度）。
