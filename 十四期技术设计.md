# 十四期技术设计 · 数据安全分类目录

> 目标：把「分级明细」升级为「数据安全分类目录」——四列 `一级分类 / 二级分类 / 数据类型 / 数据分级`，一二级分类参考 business-architecture 能力地图（5 能力域 / 27 能力项），目录条目下钻到表/字段（承接十三期「分级定位」）。
> 用户已拍板：覆盖全 5 能力域抽样 ~13 条；下钻接线到表/字段。

## 一、数据模型

### 新增顶层实体 `securityCatalog`（数据安全分类目录，13 条）

每条 `{ id, category1, category2, dataType, level, tableIds }`（`tableIds` 为表 id 数组，支持跨多表定位；空数组 = 无平台内表定位）：

| id | category1(一级分类) | category2(二级分类) | dataType(数据类型) | level(数据分级) | tableIds(定位) |
|---|---|---|---|---|---|
| sc_001 | 项目经营域 | 投标管理 | 投标书 | L2 | [] |
| sc_002 | 项目经营域 | 合同管理 | 合同 | L3 | [] |
| sc_003 | 设计研发域 | 资源评估 | 测风数据 | L2 | [t_wind] |
| sc_004 | 设计研发域 | 勘测 | 地质钻孔数据 | L3 | [t_geo] |
| sc_005 | 设计研发域 | 勘测 | 海底地形测绘数据 | L4 | [t_topo, t_cable, t_substation] |
| sc_006 | 设计研发域 | 专业设计 | 风机设备数据 | L2 | [t_turbine] |
| sc_007 | 设计研发域 | 专业设计 | 海缆参数数据 | L3 | [t_cable] |
| sc_008 | 设计研发域 | 专业设计 | 升压站主变数据 | L2 | [t_substation] |
| sc_009 | 工程交付域 | 施工管理 | 施工作业进度数据 | L2 | [t_progress] |
| sc_010 | 工程交付域 | 运营管理 | SCADA遥测数据 | L2 | [t_scada, t_forecast, t_spare] |
| sc_011 | 工程交付域 | 运营管理 | 功率预测结果数据 | L2 | [t_forecast] |
| sc_012 | 专业技术域 | 新能源发电设计 | 发电量测算报告 | L2 | [] |
| sc_013 | 支撑服务域 | 供应链 | 备品备件数据 | L2 | [t_spare] |

- `category1` 值域 = 能力地图 5 能力域：项目经营域/设计研发域/工程交付域/专业技术域/支撑服务域。
- `level` 用 enum L1-L4（沿用 security 分级定义，security 主键是 `level` 无 id，故用 enum 兜底而非 refs——同 infoItems.securityLevel 的既有坑）。
- `tableIds` 引用 `tables`，可为空数组（投标书/合同/发电量测算报告 3 条无平台表定位）。
- 跨多表定位：`sc_005` 海底地形测绘数据 → t_topo（测绘原始）+ t_cable（海缆路由设计依据）+ t_substation（站址选址依据），沿血缘 lg_005/lg_006；`sc_010` SCADA遥测数据 → t_scada + t_forecast（模型训练）+ t_spare（备件需求驱动），沿血缘 lg_002/lg_003。
- 一致性：`tableIds` 非空时，`level` === 全部定位表字段 securityLevel 的最高级（LEVEL_RANK 取跨表 max）。已核对 13 条对齐。
- `meta.stats` 加 `securityCatalog: 13`。
- 后端：`securityCatalog` 只读（不进 SCHEMAS，`store.js` READABLE 按 `Array.isArray(seed[k])` 自动收录，GET 可读、不可写）。**部署时后端依赖 seed 更新，需 scp src/data.js + 删 data.json 重启。**

## 二、UI

### 1. SecurityModule 改造
- 子 tab「分级明细」→ 改名「分类目录」。
- 「分级总览」（L1-L4 定义）保留，每级加「查看定位」→ `securityDetail`（既有按级定位详情），保留「调整」按钮。
- 「分类目录」tab：一级分类下拉筛选 + 表格四列（一级分类/二级分类/数据类型/数据分级）+「操作」列（查看明细 → `securityCatalogDetail`）。**无「定位」列**（表级定位已并入明细页下钻）。

### 2. 新增 SecurityCatalogDetailModule（数据安全分类详情，下钻）
- `state.js` DETAIL_MODULES 加 `{ key: 'securityCatalogDetail', title: '数据安全分类详情', implemented: true }`；`TabWorkspace.jsx` 接分发。
- 接收 `assetId.catalogId`，展示分类/数据类型/分级（kv，`定位表` 显示「N 个 · 表名列表」）；遍历 `tableIds`，每个定位表一个 `h4` 区块 + 该表字段表（字段名/英文码/安全分级/来源 Tag，复用 `fieldSecuritySource` + Tag）+「定位」跳 `tableDetail`（带 fieldId 高亮）；`tableIds` 空时给「该数据类型暂无平台内表定位（目录覆盖范围大于已登记资产）」空态。

## 三、测试锚点

- `test/data.test.mjs`：securityCatalog 长度 13；id 唯一；level ∈ L1-L4；category1 ∈ 5 能力域；`tableIds` 数组且每元素存在于 tables；**至少 1 条跨多表定位**；**`tableIds` 非空时 level === 全部定位表字段最高分级（跨表 max）**；stats.securityCatalog === 13。
- `test/state.test.mjs`：DETAIL_MODULES 含 securityCatalogDetail。
- `test/page_test.py`：改「分级明细」→「分类目录」断言（四列数据表头 + 操作列 + **无定位列** + 13 行 + 点查看明细进详情 + 跨 3 表定位 `h4` 数 + 详情字段列表）；分级总览「查看定位」仍可进按级详情（保留 13 期断言）。

## 四、约束

- 目录只读，不新增写操作；security L1-L4 定义仍可 PUT（不变）。
- 不触碰广发「数据资源目录」术语；免责声明不变。
- 复用 Tag / tableDetail 转跳 / fieldSecuritySource / 一级分类筛选范式。
