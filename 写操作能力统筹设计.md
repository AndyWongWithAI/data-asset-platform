# 数据资产管理平台 · 元数据登记表单设计

> 展示版（Demo）。范围：**仅实现「一张内部表填一个表单」的元数据登记写操作**；审批流与导出明确排除。
> 日期：2026-08-15 · 上游依据：`功能矩阵设计.md`(v4) + `REQUIREMENTS.md` · 状态：**P0 后端 + P1 表单引擎 + P2 领域规则已实现**

---

## 0. 需求来源（用户原话）

> 1. 目前有多少个占位组件？→ 占位写按钮 10 处
> 2. 整体统筹这些占位功能，哪些统一建设、哪些单独设计？
> 3. 只需要实现「一张内部表填一个表单」的部分，审批流和导出不做。
> 4. **可交互登记，后端 API，但后端设计从简。**

---

## 1. 范围界定

| 类别 | 内容 |
|------|------|
| ✅ **做** | 7 个元数据登记写操作：新增规则 / 新增术语 / 新增值域 / 新增参考数据 / 新增信息项 / 新增实体 / 分级调整 |
| ❌ **不做** | 审批流（发起交换申请、发起服务申请）、导出（批量导出血缘） |

> 本质：7 个操作都是「对一张内部 ILF 填一个表单 → 校验 → 落库」。其中 6 个是 insert 新记录，1 个（分级调整）是 update L1-L4 定义。

---

## 2. 现状与差距

- **现状**：`data.js` 静态 import，组件直接读；7 个写按钮复用 `ComingSoonAction`（点击弹「二期后完善」）。
- **差距**：缺 ①后端持久化 ②表单与校验 ③引用完整性交互层强制。其中 ① 是前置硬依赖。

---

## 3. 统一建设（3 个横切组件，做一次复用 7 次）

### 3.1 后端 API（地基，最先建）

- `data.js` 由「前端静态 import」改为「后端种子数据 + REST API 读写」。
- 写操作只做「登记/维护」，不承担数据加工（符合本平台定位）。
- 从简设计见 §7。

### 3.2 Schema 驱动表单引擎

- 每个实体只写一份 schema（字段/类型/必填/校验/是否关联下拉），引擎统一渲染表单 + 校验 + 提交 + 反馈。
- 7 个实体差异全部落在 schema 配置，不写重复表单代码。

### 3.3 引用完整性校验器 + 关联选择器

- 关联字段统一用「从目标 ILF 拉选项」的下拉选择器 + 提交前「id 必须存在于目标 ILF」的存在性校验。
- 覆盖引用对：信息项→值域/参考数据/业务域/词根、规则→字段。
- **校验分工**：后端做结构性校验（必填/类型/引用存在/唯一），前端做交互层体验 + 领域规则校验（见 §5）。

---

## 4. 实体 Schema（7 个，核心落地）

### 4.1 基础术语 baseTerms（新增术语）

| 字段 | 类型 | 必填 | 引用 | 校验/规则 |
|------|------|------|------|-----------|
| nameCn 中文名 | text | ✅ | — | 非空 |
| nameEn 英文名 | text | ✅ | — | 非空、唯一 |
| synonyms 同义词 | list | ⬜ | — | — |
| isClassWord 是否类词 | bool | ✅ | — | — |

### 4.2 值域 valueDomains（新增值域）

| 字段 | 类型 | 必填 | 引用 | 校验/规则 |
|------|------|------|------|-----------|
| code 编号 | text | ✅ | — | 唯一，格式 `VD-XXX` |
| dataType 数据类型 | enum | ✅ | — | varchar/decimal 等 |
| length 长度 | int | ✅ | — | > 0 |
| precision 精度 | int | ✅ | — | ≥ 0 |

### 4.3 参考数据 refDatas（新增参考数据）

| 字段 | 类型 | 必填 | 引用 | 校验/规则 |
|------|------|------|------|-----------|
| code 编号 | text | ✅ | — | 唯一 |
| name 名称 | text | ✅ | — | — |
| values 枚举值 | 子表（动态行） | ✅ | — | 每行 `{code, name}` |

### 4.4 信息项 infoItems（新增信息项，最复杂）

| 字段 | 类型 | 必填 | 引用 | 校验/规则 |
|------|------|------|------|-----------|
| code 编号 | text | 自动 | — | `II0001` 递增 |
| termIds 词根 | 多选 | ✅ | baseTerms | 末位必须是类词（见 §5.1） |
| nameCn 中文名 | text | 自动派生 | termIds | 词根顺序拼接、以类词结尾（见 §5.1） |
| nameEn 英文名 | text | 自动派生 | termIds | 词根 nameEn 下划线拼接（见 §5.1） |
| type 类型 | enum | ✅ | — | 技术 / 业务 |
| bizDomainId 业务域 | 下拉 | 业务必选 | bizDomains | type=业务 时必填 |
| definition 定义 | text | 业务必填 | — | type=业务 时必填 |
| valueDomainId 值域 | 下拉 | ✅ | valueDomains | — |
| refDataId 参考数据 | 下拉 | ⬜ | refDatas | 可空 |

### 4.5 质量规则 qualityRules（新增规则）

| 字段 | 类型 | 必填 | 引用 | 校验/规则 |
|------|------|------|------|-----------|
| name 规则名 | text | ✅ | — | — |
| type 类型 | enum | ✅ | — | 准确性/完整性/一致性/及时性 |
| targetFieldId 绑定字段 | 下拉 | ✅ | fields | — |
| expr 校验表达式 | text | ✅ | — | 表达式编辑器（见 §5.2） |
| threshold 阈值 | text | ✅ | — | — |
| severity 严重级别 | enum | ✅ | — | 严重/警告/提示 |
| status 状态 | enum | ✅ | — | 启用/停用 |

### 4.6 主数据 masterData（新增实体）

| 字段 | 类型 | 必填 | 引用 | 校验/规则 |
|------|------|------|------|-----------|
| code 唯一编码 | text | ✅ | — | 唯一，如 `WTG-0001` |
| entityType 实体类型 | enum | ✅ | — | 风机/海缆/升压站/项目/供应商（决定 attrs 模板） |
| name 名称 | text | ✅ | — | — |
| attrs 权威字段 | 动态键值 | ✅ | — | 按 entityType 动态字段集（见 §5.3） |

### 4.7 安全分级 security（分级调整，update）

| 字段 | 类型 | 必填 | 引用 | 校验/规则 |
|------|------|------|------|-----------|
| level | 固定 | — | — | L1-L4 不可增删 |
| name 名称 | text | ✅ | — | — |
| desc 描述 | text | ✅ | — | — |
| mask 脱敏策略 | text | ⬜ | — | 变更影响分析（见 §5.4） |

---

## 5. 领域规则（4 处，单独设计，不能通用化）

### 5.1 信息项命名规则校验器

- **硬约束**（六期已立，单测兜底）：`nameCn` 由 `termIds` 词根顺序拼接、以类词结尾；`nameEn` = 词根 nameEn 下划线拼接。
- 新增信息项时：用户只选词根（多选），中文名/英文名由词根自动派生；校验「末位词根必须是类词」。

### 5.2 规则表达式编辑器

- `expr` 是伪代码式表达式（如 `0 <= active_power <= 16`），需一个轻量编辑器（含字段名提示），非普通文本框。

### 5.3 主数据动态属性 schema

- `attrs` 随 `entityType` 变化（风机→额定功率/叶轮直径；海缆→电压等级/截面…）。表单引擎需支持「按 entityType 切换字段集」。

### 5.4 数据安全分级变更影响分析

- 分级调整是 update L1-L4 定义，会连带影响所有已分级字段的脱敏策略。需「改定义 → 提示受影响字段清单 → 确认」流程。

---

## 6. 建序

| 阶段 | 交付物 | 覆盖 | 前置 |
|------|--------|------|------|
| P0 后端 | REST API + JSON 持久化（从简） | 全部 7 个 | 无 |
| P1 表单 | schema 驱动表单引擎 + 引用校验器 + 关联选择器 | 全部 7 个 | P0 |
| P2 领域规则 | 命名校验器、表达式编辑器、动态属性、变更影响分析 | 按实体 | P1 |

---

## 7. 后端设计（从简）

### 7.1 技术选型

- **框架**：Node.js + Express（最小依赖），不引入数据库。
- **存储**：`data.js` 作为种子数据，后端启动读入内存；写操作落盘 JSON 文件（`server/data.json`）。
- **部署**：Node 服务跑在 #1，nginx `location /api/` 反代到后端端口；静态资源仍由 nginx 托管。

### 7.2 接口设计（通用 CRUD，按实体）

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/v1/:entity` | 读实体列表 |
| GET | `/api/v1/:entity/:id` | 读单条 |
| POST | `/api/v1/:entity` | 新增（insert，6 个登记） |
| PUT | `/api/v1/:entity/:id` | 修改（分级调整 update） |

### 7.3 校验分工

- **后端（结构校验，唯一数据源）**：必填、类型、引用存在性、唯一性。
- **前端（体验 + 领域规则）**：下拉即时提示、命名规则、动态属性、变更影响提示。

**P0 已落地（结构校验）**：7 实体 schema（必填/唯一/枚举/类型/引用存在）已在 `server/store.js` 实现，含主键防篡改、id 服务端生成、局部更新不误报、坏文件容错、HTTP 状态码语义（404/405/400）、读全实体（P1 关联下拉可用）。

**P2 已落地（领域规则）**：信息项 code 服务端自增（II0001，客户端不可注入）+ nameCn/nameEn 词根派生 + 末位类词校验 + nameCn/nameEn 一致性校验 + 业务项 bizDomainId/definition 条件必填 + 值域 length>0/precision≥0；前端对应交互（词根多选实时派生/表达式编辑器/attrs 动态字段集/分级变更影响确认）。

**P1 已落地（表单引擎）**：`src/schema.js` 定义 7 实体表单 schema，`src/components/EntityForm.jsx` 通用渲染（text/number/bool/enum/ref/multiref/subtable/dynamic/expr/derived），`src/DataContext.jsx` 数据层（种子兜底 + 启动拉后端覆盖 + 写后全局刷新），7 个写按钮接入、审批流与导出仍占位。

### 7.4 从简边界（明确不做）

- 不引入数据库（JSON 文件落盘）
- 不做用户认证/权限（单机 demo）
- 不做审批流（已排除）、不做导出（已排除）
- 不做并发/事务（单进程内存 + 文件持久化）

---

## 8. 已拍板决策

| 决策点 | 结论 |
|--------|------|
| Demo 定位 | **可交互登记**（不再只读，写操作真正落库） |
| 持久化 | **后端 API，后端从简**（Express + JSON 文件，无数据库） |
