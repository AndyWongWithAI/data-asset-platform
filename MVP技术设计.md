# 数据资产管理平台 · MVP 技术设计

> 展示版（Demo）。本文件是「功能矩阵设计」到「前端实现」之间的桥梁 —— 固化 MVP 范围、工程形态、数据契约、组件划分、跨模块交互与测试部署。
> 日期：2026-08-13 · 上游依据：`REQUIREMENTS.md` → `功能矩阵设计.md`(v4) · 状态：SDLC Phase 1 需求细化产出（已评审确认）

---

## 0. 已确认决策（需求细化结论）

| 维度 | 结论 |
|------|------|
| 首要目标 | **架构评审 · 逻辑闭环**（把两态分层 / 唯一锚点 / 数据加工边界讲清楚，而非视觉或数据海量） |
| MVP 范围 | M1 资产目录 + M0 治理看板（**质量子看板 + 标准子看板**） |
| 血缘看板 | 后置二期（§5 旅程 4 不在 MVP） |
| 数据规模 | 精而全 · 生命周期覆盖（~10 表，每表 5–15 字段，三类元数据配齐） |
| 工程形态 | React SPA + 左侧固定导航 + 右侧多 tab 工作区（点导航不转跳页面，tab 追加/切换/关闭） |
| 部署 | demo.intelab.cn/data-asset-platform（子路径） |
| 占位模块呈现 | 侧栏 7 模块全列，占位模块点击显示「定位说明 + 二期建设中」页 |

---

## 1. 整体架构与模块范围

### 1.1 壳（Shell）

```
┌─────────────────────────────────────────────────────┐
│ 顶部栏：数据资产管理平台 · GDEPDI 海上风电     [免责声明] │
├──────────────┬──────────────────────────────────────┤
│ 左侧导航      │ 右侧多 tab 工作区                     │
│              │  ┌─[资产目录][治理看板][数据质量] ✕──┐ │
│ 生产态·监控   │  ├──────────────────────────────────┤ │
│  数据治理看板 │  │                                   │ │
│ ──────────── │  │  当前激活 tab 的模块内容（组件渲染）  │ │
│ 设计态·定义   │  │                                   │ │
│  数据资产目录 │  │                                   │ │
│  数据质量     │  └──────────────────────────────────┘ │
│  数据标准     │                                        │
│  数据安全     │                                        │
│  主数据       │                                        │
│ ──────────── │                                        │
│ 价值输出      │                                        │
│  数据服务     │                                        │
└──────────────┴──────────────────────────────────────┘
```

**两态分层（架构评审的核心表达）**：左侧导航分三组 —— **生产态·监控**（治理看板）→ **设计态·定义**（资产目录/质量/标准/安全/主数据）→ **价值输出**（数据服务）。导航分组直接把 `功能矩阵设计.md` §4 的两态洞察画在界面上。

### 1.2 MVP 实现范围

- ✅ **完整实现**：M1 数据资产目录 + M0 治理看板（质量子看板 + 标准子看板）。
- ⏳ **占位（二期）**：M2 数据质量规则登记、M3 数据标准定义、M4 安全分级、M5 主数据、M6 数据服务、M0 血缘子看板。

> 模块名易混淆点：侧栏「数据治理看板」= M0（生产态，看评分/贯标），其子看板是**治理看板 tab 内的子视图**；侧栏「数据质量」「数据标准」= M2/M3（设计态，登记规则/定义标准）。二者是「生产态展示」vs「设计态登记」的不同模块。

> 占位模块：M4/M5 的**独立页**占位，但 `security[]`、`masterData[]` **数据保留**（M1 字段要引用）——占位的是「登记/维护页」，不是「数据」。

---

## 2. 数据契约（data.js）

复用兄弟案例 `data.js` IIFE 双环境范式（浏览器 `<script>` 引入 + Node `await import()`）。MVP 收缩后的数组：

```
meta            // 平台标题/免责声明/统计
applications[]  // 应用（技术视角编目起点）
databases[]     // 库
tables[]        // 表（应用/库/业务域/主题域/主数据引用）
fields[]        // 字段 = 三类元数据（业务/技术/管理）
bizDomains[]    // 业务域 + 主题域（业务视角编目）
qualityRules[]  // 质量规则（M1 引用 + M0 质量子看板）
qualityResults[]// 质量评分 + 问题明细（生产态，外部加工结果）
standards[]     // 码表 + 字段标准（M1 引用 + M0 标准看板）
security[]      // 分级 + 脱敏策略（M1「安全分类分级」引用）
masterData[]    // 主数据实体（M1「主数据引用」）
// 二期：lineage[]、services[]
```

### 2.1 引用完整性（逻辑闭环骨架，Node 断言兜底）

- `tables[].appId / dbId / bizDomainId / masterDataId` → 必须指向存在的实体
- `fields[].technical.qualityRuleIds[]` → `qualityRules[]`
- `fields[].management.standardId` → `standards[]`；`securityLevel` → `security[]`
- `fields[].business.masterDataId` → `masterData[]`
- `qualityResults[].issues[].fieldId / ruleId` → `fields[]` / `qualityRules[]`

改任何 id 必须同步改引用；`test/data.test.mjs` 引用完整性测试兜底。

### 2.2 精而全 ~10 张代表表（覆盖全生命周期）

| 生命周期 | 代表表 |
|---------|--------|
| 资源评估 | 测风数据表 |
| 勘测 | 地质钻孔表、海底地形测绘表 |
| 设计 | 风机设备表、海缆参数表、升压站主变表 |
| 施工 | 作业进度表 |
| 运维 | SCADA 遥测表、功率预测结果表、备品备件表 |

每表 5–15 字段，字段三类元数据（业务/技术/管理）配齐 —— 「字段级治理」的展示载体。

---

## 3. 组件划分 + 数据流

### 3.1 组件树

```
App（壳，持有全局状态，不引入 Redux——useState/useReducer 足够）
├─ HeaderBar          顶部：平台名 + 免责声明
├─ Sidebar            左侧导航（三组：生产态/设计态/价值输出）
│   └─ NavItem × 7    占位项带「二期」badge
└─ TabWorkspace       右侧工作区
    ├─ TabBar         多 tab 条（追加 / 激活 / 关闭）
    └─ TabPanel       渲染激活 tab 的模块组件
        ├─ CatalogModule     M1 资产目录（检索 + 列表 + 资产详情）
        ├─ GovernanceModule  M0 治理看板（质量子看板 + 标准子看板）
        └─ PlaceholderModule 通用占位页（定位说明 + 二期 badge，M2–M6 复用）
```

共享组件（`components/`）：`SearchInput`、`DataTable`、`Tag/Badge`、`ScoreGauge`（评分/贯标率）、`FieldMetaCard`（字段三类元数据卡片）、`AssetBreadcrumb`（应用→库→表→字段 面包屑）。

### 3.2 状态模型（跨模块转跳 = 逻辑闭环机制核心）

```js
tabs:     [{ id, moduleKey, title, assetId? }]   // assetId 记录「当前选中资产」
activeTabId
navigate(moduleKey, { tableId?, fieldId? })      // 唯一转跳入口
```

- **点左侧导航**：该模块 tab 已存在 → 激活它；不存在 → 追加 + 激活。
- **跨模块转跳**（`navigate`）：从 M0 质量子看板「问题明细」点「转跳资产目录」→ 找到/新建 `catalog` tab → 写入 `assetId`（定位到具体表/字段）→ 激活。看板与目录靠同一份 `assetId` 联动，杜绝两套数据 —— 「唯一锚点 M1」在交互层落地。
- **数据只读单向流**：所有数据来自 `data.js`，模块组件用 `moduleKey + assetId` 取数渲染，无运行时写操作。

### 3.3 两条核心闭环（对应 `功能矩阵` §5 旅程）

- **质量闭环**：治理看板（质量子看板·看评分）→ 问题明细 → `navigate('catalog', {fieldId})` 定位字段 → 看「关联质量规则」→ `navigate('governance')` 回看板。
- **标准闭环**：治理看板（标准子看板·看贯标率）→ 筛「应贯未贯」→ 定位字段 → 看「关联标准」→ 回看板。

> 血缘旅程（§5 旅程 4）因血缘看板后置，MVP 不实现。

---

## 4. 错误处理 / 测试 / 部署

### 4.1 错误处理（静态 demo，无 async/API）

- 引用完整性错误 → **测试期拦截**（`node --test` 断言失败即失败），不进运行时。
- 空态优雅降级：搜索无结果 → 空状态提示；占位模块 → 定位说明页（非 404）。
- 无网络/异步 → 无网络错误分支。

### 4.2 测试（沿用兄弟案例范式）

```bash
npm test          # data.js 引用完整性测试（node --test 自动发现）
npm run test:page # 页面渲染冒烟测试（playwright + 系统 Chrome）
```

> ⚠ Node 22 下 `node --test test/`（带目录参数）会报 MODULE_NOT_FOUND，必须用 `node --test` 自动发现。
> `navigate`/tab 状态机先由 `test:page` 冒烟覆盖；如需可加 vitest 单测（可选，非 MVP 必需）。

### 4.3 部署（demo.intelab.cn/data-asset-platform 子路径）

- Vite 配 `base: '/data-asset-platform/'`；`npm run build` → `dist/`。
- tab 导航不走 URL 路由 → **无需 SPA history fallback**，nginx 静态托管 `location /data-asset-platform/` → `dist/`。
- 走 GH Actions 部署到 #1 服务器（`consolidated-deploy-rules` 模式），HTTPS 复用 demo.intelab.cn 证书。

---

## 5. 二期清单（非 MVP）

| 项 | 说明 |
|----|------|
| 血缘看板（M0 子看板） | 卡片式血缘图（测风→评估→设计→施工→运维），§5 旅程 4 |
| M2 数据质量独立页 | 规则登记/编辑（MVP 以静态引用值内嵌 M1） |
| M3 数据标准独立页 | 码表/字段标准定义（MVP 静态引用） |
| M4 数据安全分级页 | 分级规则 + 脱敏策略维护 |
| M5 主数据管理页 | 主数据实体黄金记录维护 |
| M6 数据服务/共享 | 数据产品/API 封装 + 申请审批 |
