# 数据资源管理平台 · 二期技术设计

> 展示版（Demo）。本文件是「功能矩阵设计 → 二期前端实现」之间的桥梁 —— 固化二期范围、页面形态、组件划分、跨模块转跳与数据契约补充。
> 日期：2026-08-14 · 上游依据：`REQUIREMENTS.md` → `功能矩阵设计.md`(v4) · 状态：SDLC Phase 2 设计产出（待实现计划）

---

## 0. 已确认决策

| 维度 | 结论 |
|------|------|
| 二期范围 | **M2 数据质量 + M3 数据标准 + M4 数据安全分级 + M5 主数据管理**（4 个设计态独立页） |
| 不在二期 | 血缘看板（M0 子看板，卡片式血缘图渲染最复杂，单独排期）、M6 数据服务（P2 三期） |
| 写操作边界 | **全部只读展示**，前端保留「新增/编辑」占位按钮，点击弹「该功能二期后完善」提示，**不引入运行时写操作**（延续 MVP「只读单向流」架构） |
| 首要目标 | 延续一期「架构评审 · 逻辑闭环」——把「设计态登记」四个模块的独立页形态 + 与 M1 唯一锚点的引用转跳跑通 |

---

## 1. 模块范围与统一页面形态

四个模块数据在 MVP 已全部就位（作为 M1 字段元数据的「引用来源」内嵌于 `data.js`），二期只是把这些数据**从「内嵌」升级为「独立页」**，并打通「设计态登记 → 转跳 M1 锚点」的反向引用。

**统一形态（四个模块一致）**：

```
模块组件（模块内两段式，非独立 tab）
├─ 列表视图（默认）  顶部：筛选/检索 + 「新增/编辑」占位按钮
│                    中部：表格（该模块的数据对象清单）
└─ 详情视图（点行进入） 顶部：「← 返回列表」
                        中部：对象完整信息 + 被引用字段清单（每条可转跳 M1 定位字段）
```

- 列表/详情切换用模块内 `useState`（`selectedId` 为 null 显示列表，非 null 显示详情），**不新增 tab**。
- 「被引用字段」转跳沿用现有 `onNavigate('tableDetail', { tableId, fieldId, title })`，落到 M1 字段级详情 tab（`TableDetailModule` 高亮该字段）——「唯一锚点 M1」在反向引用侧落地。
- 占位写按钮统一用共享组件 `ComingSoonAction`（点击弹 modal 提示，见 §3）。

---

## 2. 各模块详述

### M2 数据质量（QualityModule）

- **定位**：设计态·规则登记处，M1「关联质量规则」取值来源，运行结果由 M0 质量子看板监控。
- **列表**：表格列 = 规则名 / 类型 / 绑定字段 / 校验表达式 / 阈值 / 严重级别 / 状态。筛选 = 按类型下拉（全部/准确性/完整性/一致性/及时性）。
- **详情**：规则完整信息（名称/类型/表达式/阈值/严重级别/状态）+「绑定字段」卡片（字段名 + 所属表 + 转跳 M1 定位字段）。
- **数据**：`qualityRules[]`（8 条，字段：id/name/type/targetFieldId/expr/threshold/severity/status）。
- **占位按钮**：`新增规则`。

### M3 数据标准（StandardModule）

- **定位**：设计态·标尺，码表与字段标准定义处，M1「关联标准」与 M0 标准看板「贯标」的对标尺。
- **列表**：表格列 = 标准编码 / 名称 / 类型（码表|字段标准）/ 内容预览（码表→枚举值前几项，字段标准→单位/取值域）。筛选 = 按类型（全部/码表/字段标准）。
- **详情**：标准完整信息（码表 → 枚举值全量列表；字段标准 → name/type/unit/domain）+「被引用字段」清单（反查 `fields[]` 中 `standardId` 指向该标准者，转跳 M1）。
- **数据**：`standards[]`（6 条：5 码表 + 1 字段标准）。
- **占位按钮**：`新增标准`。

### M4 数据安全分级（SecurityModule）

- **定位**：设计态·治理配置，分级规则与脱敏策略定义处，M1「安全分类分级」取值来源。
- **分级总览**：L1–L4 四级卡片（名称/说明/脱敏策略 mask）+ 各级字段数统计（反查 `fields[].management.securityLevel`）。
- **高风险清单**：L3/L4 字段清单（字段/所属表/分级/转跳 M1 定位）。
- **脱敏前后对比**：`maskExamples[]`（新增，见 §4）表格 —— 字段/级别/原值/脱敏后值/策略（静态演示样本，不做真实坐标计算）。
- **占位按钮**：`分级调整`。

### M5 主数据管理（MasterDataModule）

- **定位**：设计态·共享对象，主数据实体黄金记录定义处，M1「主数据引用」来源。
- **列表**：表格列 = 唯一编码 / 实体类型 / 名称 / 权威字段预览。筛选 = 按实体类型（全部/风机/海缆/升压站/项目/供应商）。
- **详情**：唯一编码 + 实体类型 + 名称 + 权威字段（`attrs` 键值对）+「被引用字段」清单（反查 `fields[]` 中 `masterDataId` 指向该实体者，转跳 M1）。
- **数据**：`masterData[]`（5 实体，字段：id/code/entityType/name/attrs）。
- **占位按钮**：`新增实体`。

---

## 3. 组件划分 + 数据流

### 3.1 组件树（二期新增部分）

```
src/
├─ components/
│  └─ ComingSoonAction.jsx   # 共享占位写按钮（label prop，点击弹 modal「二期后完善」）
└─ modules/
   ├─ QualityModule.jsx      # M2 数据质量（列表 + 详情）
   ├─ StandardModule.jsx     # M3 数据标准（列表 + 详情）
   ├─ SecurityModule.jsx     # M4 数据安全分级（分级总览 + 高风险清单 + 脱敏对比）
   └─ MasterDataModule.jsx   # M5 主数据管理（列表 + 详情）
```

### 3.2 改动文件

- `src/state.js`：`MODULE_GROUPS` 中 `quality/standard/security/masterdata` 的 `implemented` 改 `true`（`service` 保持 `false`，继续占位）。
- `src/components/TabWorkspace.jsx`：加 4 个路由分支（`quality/standard/security/masterdata` → 对应组件），`service` 继续走 `PlaceholderModule`。
- `src/data.js`：新增 `maskExamples[]`（M4 脱敏对比样本）。
- `src/index.css`：新增 `.btn-primary`、`.modal-mask`、`.modal`、详情视图（`.detail-panel`、`.kv-list`）等样式。

### 3.3 数据流（沿用一期「只读单向流」）

- 所有数据来自 `data.js`（`export default` 单例），模块组件用 `moduleKey + selectedId` 取数渲染，**无运行时写操作**。
- 「被引用字段」反向查询 = `data.fields.filter(f => f.management.standardId === selectedId)`（M3）/ `f.business.masterDataId === selectedId`（M5）/ 规则 `f.technical.qualityRuleIds.includes(selectedId)`（M2，可选）。
- 跨模块转跳统一走 `dispatch({ type: 'NAVIGATE', moduleKey: 'tableDetail', assetId: {...} })`，状态机零改动。

---

## 4. 数据契约补充（data.js）

新增一个数组，供 M4 脱敏对比展示（静态演示样本，非真实坐标计算）：

```js
maskExamples: [
  { field: '场址精确坐标', level: 'L4', original: '21.5021°N, 111.3047°E', masked: '21.5°N, 111.3°E', strategy: '坐标模糊化' },
  { field: '海缆路由坐标', level: 'L3', original: '21.4832°N, 111.2891°E', masked: '21.48°N, 111.29°E', strategy: '坐标偏移' },
  { field: '岩土钻孔坐标', level: 'L3', original: '21.4618°N, 111.2716°E', masked: '21.46°N, 111.27°E', strategy: '坐标偏移' },
  { field: 'SCADA 机组编号', level: 'L2', original: 'WTG-A12-07', masked: 'WTG-****-07', strategy: '字段掩码' },
],
```

`meta.stats` 同步补 `maskExamples: 4`（可选，保持统计口径一致）。

---

## 5. 测试

- **单测（node --test）**：
  - `test/state.test.mjs`：断言 `quality/standard/security/masterdata` 四模块 `implemented === true`（`service` 仍 false）。
  - `test/data.test.mjs`：`maskExamples[]` 每条 `level` 必须存在于 `security[]`；引用完整性已有测试不变。
- **e2e（playwright `test:page`）**：四个模块逐个打开 → 列表渲染断言 → 点行进详情 → 「被引用字段」转跳 M1（`.tab.active` 落在表详情）→ 占位按钮点击弹 modal。
- **约定**：二期开发完成后必跑 `npm test` + `npm run test:page`，全绿方可合并。

---

## 6. 部署

沿用一期 GH Actions：push `main` → `npm ci` → `npm test` → `npm run build` → rsync `dist/` 到 #1 `/opt/demo/data-asset-platform/`。无新增 secret、无 nginx 改动。

---

## 7. 待确认

1. 本二期技术设计（4 模块只读 + 占位写按钮 + M4 脱敏静态样本 + 两段式页面）是否通过？
2. 通过后即产出「二期实现计划」（任务清单，任务分支开发再 merge）。
