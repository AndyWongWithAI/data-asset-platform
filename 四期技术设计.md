# 数据资源管理平台 · 四期技术设计

> 展示版（Demo）。本文件固化四期范围：血缘看板「交换方式标注移位」+「字段级血缘（UML 类图风格）」两项改造。
> 日期：2026-08-14 · 上游依据：`功能矩阵设计.md`(v4) → `三期技术设计.md` · 状态：已实现并合入 main

---

## 0. 需求来源（用户原话要点）

1. 离线批次 / 数据服务等交换方式说明，应标注在**血缘链接线**上（而非节点内）。
2. 字段级血缘需以**类似 UML 图**的形式，同时表示表级血缘与字段级血缘：
   - 默认状态：展示**表级血缘**，连线从**实体名称**出发，连到下一个实体名称（默认**蓝色**）。
   - 点击具体属性（字段）：表级血缘颜色从蓝变**灰**，再用新的**蓝色**线条，从点击的属性链接到与它相关的**上下游实体的属性**。
   - 切换属性：清空已选字段级血缘，重新基于新选中的属性绘制。

---

## 1. 数据契约：lineage 增加 fieldMapping

字段级血缘**不另建独立数组**，而是作为表级边的细化，挂在每条 `lineage` 边下 —— 字段映射天然属于某条表级血缘，引用完整性可沿「字段 ∈ 所属表 ∈ 边端点」一条链验证，避免两套数据漂移。

### 1.1 结构

```js
lineage: [
  { id, up, down, relation, mode, desc,
    fieldMapping: [ { up: 'f_xxx', down: 'f_yyy' }, ... ] },
]
```

- `fieldMapping[].up` —— 上游字段 id，必须 `∈ fields[]` 且 `fields[id].tableId === 该边 up`。
- `fieldMapping[].down` —— 下游字段 id，必须 `∈ fields[]` 且 `fields[id].tableId === 该边 down`。
- 每条边 1–3 个映射；「应用内」边同样给映射（内部加工脚本也有字段级流向，仅不显式标注交换方式）。

### 1.2 完整映射表（9 边 / 22 映射）

| 边 id | up 表 → down 表 | fieldMapping (up → down) |
|-------|-----------------|--------------------------|
| lg_001 | t_wind → t_forecast | f_wind_speed→f_forecast_power；f_wind_time→f_forecast_time；f_wind_dir→f_forecast_power |
| lg_002 | t_scada → t_forecast | f_scada_power→f_forecast_power；f_scada_time→f_forecast_time |
| lg_003 | t_scada → t_spare | f_scada_turbine→f_spare_turbine；f_scada_vibration→f_spare_qty |
| lg_004 | t_geo → t_turbine | f_geo_bearing→f_turbine_foundation；f_geo_stratum→f_turbine_model；f_geo_project→f_turbine_project |
| lg_005 | t_topo → t_cable | f_topo_route→f_cable_route；f_topo_depth→f_cable_type |
| lg_006 | t_topo → t_substation | f_topo_coord→f_sub_md；f_topo_depth→f_sub_capacity；f_topo_project→f_sub_project |
| lg_007 | t_turbine → t_scada | f_turbine_md→f_scada_turbine；f_turbine_power→f_scada_power |
| lg_008 | t_cable → t_substation | f_cable_temp→f_sub_oil_temp；f_cable_md→f_sub_md |
| lg_009 | t_forecast → t_progress | f_forecast_power→f_prog_seastate；f_forecast_time→f_prog_task；f_forecast_project→f_prog_project |

> 字段 id 均取自现有 `fields[]`，字段→表归属见 `src/data.js`。映射方向与表级边方向一致（up 表字段 → down 表字段）。

---

## 2. UML 类图布局（LineageGraph 重构）

### 2.1 类框（取代原 170×50 小矩形）

每个表渲染为一个**类框**，结构：

```
┌───────────────────┐  ← 表名条（类名区，表级连线锚点）
│ 测风数据表  wind_measurement │
├───────────────────┤  ← 分隔线
│ · 风速   WIND_SPEED    │  ← 字段行（可点击）
│ · 风向   WIND_DIR      │
│ · ...                  │
└───────────────────┘
```

尺寸参数：

- 类框宽 `BOX_W = 180`
- 表名条高 `NAME_H = 30`（含表名中文 + 英文小字）
- 字段行高 `FIELD_H = 22`（每字段显示「业务名 + code」或仅业务名，可加小字 code）
- 类框高 = `NAME_H + 1（分隔线）+ n_fields × FIELD_H`

### 2.2 三列布局（沿用，垂直堆叠改按类框实际高度累计）

- 上游列 x=0，中心列 x=`BOX_W + GAPX`，下游列 x=`2*(BOX_W + GAPX)`；`GAPX = 200`（给连线留标注空间）。
- 每列内多表**按各自类框高度**纵向累加，列间留 `COL_GAP = 24` 间距。
- 画布高 = `max(上游列总高, 下游列总高, 中心框高)`（中心单表）；中心框垂直居中。
- 画布宽 = `3*BOX_W + 2*GAPX`。

### 2.3 表级连线（默认蓝色，锚定表名条）

- 起点/终点锚在**表名条**（类框顶部区域）：上游表名条右缘中点 → 中心表名条左缘中点；中心表名条右缘中点 → 下游表名条左缘中点。
- 颜色：默认 `#165dff`（主题蓝）；选中字段后变 `#c9cdd4`（灰）。
- 连线旁标注 `relation`（沿用），下方标注 `mode`（仅非「应用内」）。

### 2.4 交换方式标注（移到连线）

- 原节点内 `lineage-node-mode` 文本**删除**。
- 在每条表级连线的 `relation` 标签**下方**加 `mode` 标签（`.lineage-edge-mode`）：`离线批次`（灰 tag 色）/ `数据服务`（绿 tag 色）；「应用内」不标注。
- 位置取连线中点、`relation` 文本 y 下移 `+14`。

### 2.5 字段级连线（点击后蓝色）

- 选中字段 `selFieldId` 后，遍历全部 `lineage[].fieldMapping`，凡 `up === selFieldId` 或 `down === selFieldId` 的映射，若其**两端字段所在表均在当前图内**（中心/上游/下游），画一条字段级连线：
  - `up 字段行右缘中点` → `down 字段行左缘中点`（跨列折线或贝塞尔，蓝色 `#165dff`）。
- 方向：字段作为 `down` 时线从上游字段流入；作为 `up` 时线流向下游字段。

---

## 3. 交互（LineageGraph 内 useState）

- `const [selFieldId, setSelFieldId] = useState(null)`
- **默认** `selFieldId = null`：表级线蓝色，无字段级线。
- **点击字段行**：`setSelFieldId(field.id)` —— 表级线变灰，绘制以该字段为端点的字段级线（蓝）。
- **切换字段**：`setSelFieldId` 直接替换（React 重绘，天然「清空旧的、按新选中重绘」）。
- **再次点击同一字段**：`setSelFieldId(null)` 清空回到表级视图（toggle）。
- **点击表名条**：仍 `onNavigate('tableDetail', {tableId})` 转跳 M1（保留）；字段行点击只做字段级选中，不转跳。
- 字段级选中态：选中的字段行加高亮背景（`.lineage-field-active`）。

> 表级线变灰 + 字段级线变蓝需两套箭头 marker（`arrow-blue` / `arrow-gray`），SVG marker 颜色不能由 path stroke 继承。

---

## 4. 样式（src/index.css 增改）

```css
.lineage-table-box rect { fill:#fff; stroke:#165dff; stroke-width:1.5; rx:6; }
.lineage-table-box .table-title { font-weight:600; font-size:13px; fill:#1f2329; }
.lineage-table-box .table-title-en { font-size:10px; fill:#86909c; }
.lineage-table-box .sep { stroke:#e5e6eb; stroke-width:1; }
.lineage-field-row { cursor:pointer; font-size:12px; fill:#4e5969; }
.lineage-field-row:hover text { fill:#165dff; }
.lineage-field-active text { fill:#165dff; font-weight:600; }
.lineage-field-active rect { fill:#e8f1ff; }
.lineage-edge-table { stroke:#165dff; stroke-width:1.6; fill:none; }      /* 默认蓝 */
.lineage-edge-table.is-dimmed { stroke:#c9cdd4; stroke-width:1.4; }        /* 点字段后灰 */
.lineage-edge-field { stroke:#165dff; stroke-width:1.8; fill:none; }        /* 字段级蓝 */
.lineage-edge-label { font-size:11px; fill:#86909c; }
.lineage-edge-mode { font-size:10px; }
.lineage-edge-mode.mode-ok { fill:#00b42a; }
.lineage-edge-mode.mode-default { fill:#86909c; }
.lineage-empty-col { color:#c9cdd4; font-size:12px; }
```

> 删除旧的 `.lineage-node` / `.lineage-node-mode` 相关（不再用节点内 mode 标注）。`.lineage-svg` 保留。

---

## 5. 文件改动

- `src/data.js`：`lineage[]` 每条加 `fieldMapping[]`（§1.2）。
- `src/modules/LineageModule.jsx`：`LineageGraph` 重构为 UML 类框 + 表级/字段级双视图 + `selFieldId` 交互；交换方式标注移到连线。
- `src/index.css`：§4 样式增改。
- `test/data.test.mjs`：追加 fieldMapping 引用完整性测试。
- `test/page_test.py`：追加字段级交互断言。
- `CLAUDE.md`：当前状态同步（血缘看板升级字段级）。

---

## 6. 测试

- **单测**：`lineage[].fieldMapping` 每条边非空；每个映射 `up`/`down` 均存在于 `fields[]`；`up` 字段归属 `up` 表、`down` 字段归属 `down` 表。
- **e2e**：血缘看板 → 查看血缘图 → 点字段行 → 断言 `.lineage-edge-field` 出现（字段级线）且表级线 `.lineage-edge-table` 带 `.is-dimmed`；再点同字段 → 字段级线消失。
- **约定**：开发完成后必跑 `npm test` + `npm run test:page`，全绿方可合并。

---

## 7. 已确认并实现

1. 字段级血缘数据模型（`fieldMapping` 挂在 `lineage` 边下，§1）已实现：9 边 / 22 映射，`test/data.test.mjs` 引用完整性测试兜底。
2. UML 类框布局 + 「点字段表级变灰/字段级变蓝、切换清空」交互（§2/§3）已实现，`test/page_test.py` 字段级交互 e2e 断言兜底。
3. 四期实现计划已完成，改动合入 `main`（血缘看板 UML 类图双视图 + 字段级交互 + 交换方式标注移位）。
