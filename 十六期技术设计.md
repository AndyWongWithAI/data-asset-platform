# 十六期技术设计 · 数据服务接入指南

> 目标：数据服务从「治理视角」补上「技术接入视角」——技术人员打开一条数据服务，能直接看懂「怎么调用 API、怎么拿数据表、怎么订阅推送」。
> 已拍板：接入信息放在「数据服务详情页」新增的「接入指南」区块，按服务类型（API / 订阅 / 数据包）分支渲染，附「一键复制」按钮；**不含鉴权/密钥**（鉴权属安全管控，demo 不展示、不复制密钥）；纯前端 mock，示例占位，不真实调用。

## 一、数据模型

### `services` 每条新增 `access` 字段（接入契约）

`access` 按 `service.type` 分为三种异构结构（UI 按 `type` 分支渲染，无需额外 `kind` 标记）。**所有地址 / 主题 / 连接地址均为示例占位，非真实可调用**；**不含任何鉴权 / 密钥字段**。

#### API 类型（svc_001 / svc_003）

```js
access: {
  endpoint: 'https://demo.intelab.cn/api/v1/wind/query',  // 接口地址（示例）
  method: 'POST',                                          // GET / POST
  params: [                                                // 请求参数表
    { name: 'turbineId', type: 'string', required: true, desc: '风机编号' },
    { name: 'metric', type: 'string', required: true, desc: '指标：wind_speed / wind_dir / turbulence' },
    { name: 'startTime', type: 'string', required: true, desc: '起始时间（ISO8601）' },
  ],
  requestExample: 'curl -X POST "..." -H "Content-Type: application/json" -d \'{"turbineId":"W-001"}\'',  // 请求示例
  responseExample: '{ "code": 0, "data": [ { "ts": "...", "wind_speed": 8.4 } ] }',  // 响应示例
  rateLimit: '1000 次/分钟',
  version: 'v1.2',
}
```

#### 订阅类型（svc_002）

```js
access: {
  protocol: 'MQTT',                                        // MQTT / Kafka
  endpoint: 'mqtt://demo.intelab.cn:1883',                 // 连接地址（示例）
  topic: 'wind/scada/{turbineId}/telemetry',               // 订阅主题
  qos: 1,                                                  // QoS 等级
  messageFormat: 'JSON',                                   // 消息格式
  messageFields: [                                         // 消息字段
    { name: 'turbineId', type: 'string', desc: '风机编号' },
    { name: 'activePower', type: 'float', desc: '有功功率（kW）' },
    { name: 'windSpeed', type: 'float', desc: '风速（m/s）' },
    { name: 'temperature', type: 'float', desc: '机舱温度（℃）' },
    { name: 'ts', type: 'string', desc: '采集时间戳' },
  ],
}
```

#### 数据包类型（svc_004 / svc_005）

```js
access: {
  format: 'CSV / Parquet',                                 // 文件格式
  downloadUrl: 'https://demo.intelab.cn/api/v1/packages/sea-restrict/download',  // 下载地址（示例）
  updateFreq: '按季度更新',                                 // 更新频率
  size: '1.8 GB',                                          // 数据量
  partition: '按海域分区',                                  // 分区方式
}
```

**数据包的字段清单不重复存**：直接复用 `tableIds → tables → fields`，在接入指南里渲染「包含字段」简表（表名 + 字段数，点开转跳表详情看字段）。

### 5 条 seed 具体值

| id | type | access 要点 |
|---|---|---|
| svc_001 风资源评估 API | API | POST `/wind/query`，params 3 项，rateLimit 1000/min，v1.2 |
| svc_002 SCADA 实时数据订阅 | 订阅 | MQTT，topic `wind/scada/{turbineId}/telemetry`，qos 1，messageFields 5 项 |
| svc_003 功率预测服务 | API | GET `/forecast/power`，rateLimit 500/min，v1.0 |
| svc_004 海域限制因素共享 | 数据包 | CSV/Parquet，按季度更新，1.8 GB，按海域分区 |
| svc_005 设备档案数据产品 | 数据包 | CSV，按月更新，420 MB，按机型分区 |

- `services` 仍是**只读**（不进 SCHEMAS，`store.js` READABLE 按 `Array.isArray(seed[k])` 自动收录）；`access` 是内嵌对象，不需要后端改动。
- `meta.stats.services` 不变（5）。

## 二、UI

### 数据服务详情页（`DataServiceDetailModule.jsx`）

在「审批链」之前新增「**接入指南**」区块（`<h4>接入指南</h4>`），按 `s.type` 分支渲染：

- **API**：接口地址（`<code>`）+ 请求方法（Tag）+ 限流 + 版本；请求参数表（参数名/类型/必填/说明）；「请求示例」「响应示例」两个代码块（`<pre><code>`），各带「复制」按钮。
- **订阅**：协议 + 连接地址 + 订阅主题（`<code>`）+ QoS + 消息格式；消息字段表（字段名/类型/说明）；「订阅配置」代码块（topic + endpoint + qos 拼接），带「复制」按钮。
- **数据包**：文件格式 + 下载地址（`<code>`）+ 更新频率 + 数据量 + 分区；「包含字段」简表（复用 `tableIds`，表名 + 字段数，点表名转跳 `tableDetail`）；「下载地址」带「复制」按钮。

### 新增 `src/components/CopyButton.jsx`

复用组件：`<CopyButton text="..." />`，点击 `navigator.clipboard.writeText(text)` 复制，成功后按钮短暂变「已复制」（约 1.5s 回退）。失败时静默忽略（不弹错）。纯前端，无后端。

### 列表页（`DataServiceModule.jsx`）不改

列表保持现状（服务名/类型/时延/封装资产/安全分级/状态/操作），接入信息集中在详情页，避免列表变重。

## 三、范围与语义

- 只改技术后台「数据服务」模块（详情页为主 + 新增 CopyButton 组件），**门户侧不动**（业务视角不需要技术接入信息）。
- 接入契约**公开可见**（接口文档公开是常态）；现有「申请/订阅」「申请订阅」占位按钮**保留不动**，语义仍是「申请调用授权」（占位）。
- **不含鉴权 / 密钥**：`access` 无任何鉴权字段，界面无密钥展示、无复制密钥按钮。

## 四、测试锚点

- `test/data.test.mjs`：
  - `services` 每条有 `access` 对象。
  - 结构一致性：`type==='API'` → `access` 必有 `endpoint`/`method`/`params`（数组）/`requestExample`/`responseExample`；`type==='订阅'` → 必有 `protocol`/`topic`/`qos`；`type==='数据包'` → 必有 `format`/`downloadUrl`/`updateFreq`。
  - 无鉴权/密钥：`access` 不含 `auth`/`key`/`secret`/`token` 等键（防回潮）。
  - 数据包的 `tableIds` 引用仍存在于 `tables`（沿用既有断言）。
- `test/state.test.mjs`：不变（不改 MODULE_GROUPS / MODULES）。
- `test/page_test.py`：
  - 数据服务详情页出现「接入指南」区块。
  - API 详情（svc_001）：断言接口地址/请求方法/请求示例/响应示例/「复制」按钮存在。
  - 订阅详情（svc_002）：断言协议 MQTT/订阅主题/消息字段表存在。
  - 数据包详情（svc_004）：断言文件格式/下载地址/「包含字段」表存在。
  - 断言页面**不出现**「密钥 / API Key / 鉴权」字样。

## 五、约束

- 内容红线：所有接口地址 / 主题 / 连接地址均为**示例占位**（demo.intelab.cn 前缀），非真实可调用，`meta.disclaimer` 声明不变；不虚构内部运营数据。
- 鉴权 / 密钥完全不涉及（用户明确拍板）。
- 本次不实现真实调用 / 真实下载 / 真实订阅，全部为静态展示 + 前端复制。
- 不触碰广发「数据资源目录」术语。
