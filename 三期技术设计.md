# 数据资源管理平台 · 三期技术设计

> 展示版（Demo）。本文件是「功能矩阵设计 → 三期前端实现」之间的桥梁 —— 固化三期范围、页面形态、侧边栏重构、数据契约补充。
> 日期：2026-08-14 · 上游依据：`REQUIREMENTS.md` → `功能矩阵设计.md`(v4) · 状态：SDLC Phase 2 设计产出（待实现计划）

---

## 0. 已确认决策

| 维度 | 结论 |
|------|------|
| 三期范围 | **血缘看板（M0 子看板）+ 数据服务 M6 + 治理看板拆三菜单 + 分组改名** |
| M6 落地 | **「数据交换」模块，下含两子菜单**：① 批次文件（离线批次文件交换）② 数据服务（实时/准实时服务） |
| 审批链 | 两子菜单均**静态展示样例审批链**（申请→安全/合规审批→授权；L4 涉密标注「二次审批」） |
| 血缘交换方式 | **跨应用才标注，且只分两种**：离线批次（批次文件）vs 数据服务（实时/准实时）；**应用内部为普通定时加工脚本，不显式标注** |
| 血缘图渲染 | **纯 SVG 手绘**（零依赖，延续二期「依赖最小化」红线，package.json 不动） |
| 写操作边界 | 延续「全部只读 + 占位写按钮弹 modal」；血缘「批量导出」、批次文件/数据服务「发起申请」均为占位按钮 |

---

## 1. 侧边栏重构（核心结构性改动）

「数据治理看板」原为 1 个一级菜单（内部 3 子 tab），拆为 **3 个独立一级菜单**，替换原位置；「数据服务」提升为「数据交换」模块，下含 2 个一级菜单。

**新 `MODULE_GROUPS`（3 分组 · 10 菜单）**：

```
生产态·治理看板          ← 原「生产态·监控」改名
├─ 数据质量看板   qualityBoard
├─ 数据血缘看板   lineageBoard
└─ 数据标准看板   standardBoard
设计态·定义
├─ 数据资产目录   catalog
├─ 数据质量       quality
├─ 数据标准       standard
├─ 数据安全       security
└─ 主数据         masterdata
数据交换                 ← 原「价值输出」改名
├─ 批次文件       batchFile        （离线批次文件交换）
└─ 数据服务       dataService      （实时/准实时数据服务）
```

- 原 `governance`、`service` 两个 key **移除**（被 5 个新 key 替代）。
- 全部 10 个菜单 `implemented: true`，侧边栏不再有「二期」角标。
- `tableDetail` 仍为多实例非侧边栏模块，不变。

---

## 2. 生产态三看板拆分

原 `GovernanceModule.jsx` 内嵌 3 子 tab，拆为 3 个独立模块文件：

| 新模块 | 来源 | 内容 |
|--------|------|------|
| `QualityBoardModule.jsx` | 原 QualityBoard 抽出 | 企业整体质量评分 + 问题明细「定位字段」转跳 M1 |
| `StandardBoardModule.jsx` | 原 StandardBoard 抽出 | 企业整体贯标率 + 应贯未贯字段清单「定位」转跳 M1 |
| `LineageModule.jsx` | 全新开发 | 见 §3 |

- 两个看板抽出后**行为不变**（仍走 `onNavigate('tableDetail', …)` 落到 M1）。
- `GovernanceModule.jsx` 删除。

---

## 3. 数据血缘看板（LineageModule，纯 SVG）

### 3.1 功能点（对齐功能矩阵 §3.0.2）

1. 顶检索（表名子串 + 交换方式筛选）+ 下方结果清单
2. 批量导出选中资产的血缘 —— **占位按钮弹 modal**
3. 点击单一资产 → 卡片式血缘关系图（纯 SVG）
4. 点击血缘图节点 → 转跳 M1 表详情（字段级看表内字段，闭环「字段粒度」）

> **字段粒度取舍**：血缘关系建模为**表→表**（`lineage[]` 边）。字段粒度血缘通过「血缘图节点 → 转跳 M1 表详情 → 字段列表」闭环，不单独建字段级血缘数据（避免过度建模）。

### 3.2 页面形态（两段式，模块内 useState）

```
列表视图（默认）
├─ 顶检索：交换方式下拉（全部/离线批次/数据服务/应用内）+ 表名输入
├─ 结果清单表：勾选 / 表名 / 所属应用 / 上游数 / 下游数 / 交换方式 / 操作「查看血缘图」
└─ 「批量导出血缘」占位按钮
血缘图视图（点「查看血缘图」进入）
├─ 「← 返回列表」
└─ SVG 卡片式血缘图（中心表 + 左侧上游 + 右侧下游，节点点击转跳 M1）
```

### 3.3 交换方式语义（关键）

- `mode: '离线批次'` —— 跨应用、批次文件交换，标签「离线批次」
- `mode: '数据服务'` —— 跨应用、实时/准实时服务，标签「数据服务」
- `mode: '应用内'` —— 同一应用内部、普通定时加工脚本，**不显式标注交换方式**（列表/图中仅展示 relation，不挂交换方式标签）

### 3.4 SVG 血缘图布局（纯手绘，无库）

- 画布：水平三列 —— 左列直接上游、中心选中表、右列直接下游。
- 节点：圆角矩形（表名 + 交换方式 tag，`mode='应用内'` 不渲染 tag），尺寸 ~170×50，纵向排布。
- 边：箭头折线（上游右缘 → 中心左缘 / 中心右缘 → 下游左缘），边旁标注 `relation`。
- 交换方式着色：离线批次 `default` / 数据服务 `ok`（复用 `.tag-*` 色系）；应用内无边 tag。
- 节点 `onClick` → `onNavigate('tableDetail', { tableId })`。
- 无上游/无下游时该列显示「无上游」「无下游」占位文案。

### 3.5 血缘数据（`lineage[]`，表→表边）

9 条边覆盖全部 10 张表；`mode` 三值（离线批次 4 / 数据服务 2 / 应用内 3）齐全：

```js
lineage: [
  { id: 'lg_001', up: 't_wind',    down: 't_forecast',   relation: '功率预测输入',   mode: '离线批次', desc: '测风数据离线批次同步至功率预测模型' },
  { id: 'lg_002', up: 't_scada',   down: 't_forecast',   relation: '模型训练数据',   mode: '应用内',   desc: '运维应用内定时脚本加工训练预测模型' },
  { id: 'lg_003', up: 't_scada',   down: 't_spare',      relation: '备件需求驱动',   mode: '应用内',   desc: '运维应用内定时脚本加工驱动备件需求' },
  { id: 'lg_004', up: 't_geo',     down: 't_turbine',    relation: '机型选址依据',   mode: '离线批次', desc: '地质钻孔数据离线批次支撑机型/基础选型' },
  { id: 'lg_005', up: 't_topo',    down: 't_cable',      relation: '海缆路由依据',   mode: '离线批次', desc: '海底地形测绘离线批次支撑海缆路由设计' },
  { id: 'lg_006', up: 't_topo',    down: 't_substation', relation: '站址选址依据',   mode: '离线批次', desc: '海底地形测绘离线批次支撑升压站选址' },
  { id: 'lg_007', up: 't_turbine', down: 't_scada',      relation: '采集配置',       mode: '数据服务', desc: '风机设备参数经实时数据服务同步为 SCADA 采集配置' },
  { id: 'lg_008', up: 't_cable',   down: 't_substation', relation: '海缆监测汇聚',   mode: '应用内',   desc: '设计应用内定时脚本汇聚海缆监测至升压站主变表' },
  { id: 'lg_009', up: 't_forecast',down: 't_progress',   relation: '作业窗口调度',   mode: '数据服务', desc: '功率预测经准实时数据服务支撑施工/运维作业窗口调度' },
]
```

---

## 4. 数据交换模块（两子菜单）

### 4.1 批次文件（BatchFileModule，离线批次文件交换）

- **定位**：管理离线批次文件交换信息（源表 → 目标系统、文件格式、调度周期、样例审批链）。
- **列表**：表格列 = 任务名 / 源表 / 目标系统 / 文件格式 / 调度周期 / 状态。筛选 = 按状态（全部/运行中/审批中）。
- **详情**：任务完整信息（kv-list）+ **样例审批链**（申请→安全/合规审批→授权，逐步展示 actor/time/result）+「源表」转跳 M1。
- **占位按钮**：`发起交换申请`。
- **数据**：`batchFiles[]`（5 条）。

```js
batchFiles: [
  { id: 'bf_001', name: '测风数据批次交换', sourceTableId: 't_wind', targetSystem: '大数据开发平台·功率预测', fileFormat: 'CSV', schedule: '每日 02:00', securityLevel: 'L2', status: '运行中',
    applyFlow: [
      { step: '申请', actor: '资源评估组', time: '2026-05-12', result: '通过' },
      { step: '安全合规审批', actor: '数据安全组', time: '2026-05-14', result: '通过' },
      { step: '授权', actor: '平台管理员', time: '2026-05-16', result: '已授权' },
    ] },
  // bf_002 地质勘测数据批次交换 / bf_003 海底地形批次交换 / bf_004 备件库存月度对账 / bf_005 施工进度批次交换
]
```

### 4.2 数据服务（DataServiceModule，实时/准实时服务）

- **定位**：管理实时/准实时数据服务（API/订阅/数据包），原 M6 数据对象。
- **列表**：表格列 = 服务名 / 类型 / 时延 / 封装资产 / 安全分级 / 状态。筛选 = 按时延（全部/实时/准实时）。
- **详情**：服务完整信息 + 封装的资产清单（每条转跳 M1）+ **样例审批链**（L4 服务多一步「二次审批」）+ 调用计量（调用量/消费方数/近24h）。
- **占位按钮**：`发起服务申请`。
- **数据**：`services[]`（5 条，对齐功能矩阵 M6 数据对象）。

```js
services: [
  { id: 'svc_001', name: '风资源评估 API', type: 'API', latency: '准实时', tableIds: ['t_wind'], desc: '对外提供测风数据查询与统计', securityLevel: 'L2', status: '已上架',
    metrics: { calls: 12340, consumers: 3, last24h: 1280 },
    applyFlow: [ { step: '申请', actor: '资源评估组', time: '2026-05-12', result: '通过' }, { step: '安全合规审批', actor: '数据安全组', time: '2026-05-14', result: '通过' }, { step: '授权', actor: '平台管理员', time: '2026-05-16', result: '已授权' } ] },
  // svc_002 SCADA 实时数据订阅（订阅/实时/L2）
  // svc_003 功率预测服务（API/准实时/L2）
  // svc_004 海域限制因素共享（数据包/准实时/L4，applyFlow 含「二次审批」）
  // svc_005 设备档案数据产品（数据包/准实时/L2）
]
```

---

## 5. 组件划分 + 文件改动

### 5.1 新增文件

```
src/modules/QualityBoardModule.jsx    # 质量看板（从 GovernanceModule 拆）
src/modules/StandardBoardModule.jsx   # 标准看板（从 GovernanceModule 拆）
src/modules/LineageModule.jsx         # 血缘看板（列表 + SVG 血缘图）
src/modules/BatchFileModule.jsx       # 批次文件（离线批次文件交换 + 审批链）
src/modules/DataServiceModule.jsx     # 数据服务（实时/准实时服务 + 审批链）
```

### 5.2 改动文件

- `src/modules/GovernanceModule.jsx`：**删除**。
- `src/state.js`：`MODULE_GROUPS` 重构（3 分组 10 菜单），`governance`/`service` key 替换为 5 个新 key。
- `src/components/TabWorkspace.jsx`：加 5 个路由分支（qualityBoard/lineageBoard/standardBoard/batchFile/dataService），移除 governance 分支。
- `src/data.js`：新增 `lineage[]`（9）、`batchFiles[]`（5）、`services[]`（5）；`meta.stats` 补 `lineage/batchFiles/services` 计数。
- `src/index.css`：新增血缘图样式（`.lineage-svg`、`.lineage-node`、`.lineage-edge`、`.flow-step` 审批链样式等）。
- `src/modules/PlaceholderModule.jsx`：`POSITIONING` 更新（移除 service 占位，保留兜底）。
- `CLAUDE.md`：当前状态同步三期。

### 5.3 数据流（延续「只读单向流」）

- 血缘图节点 / 批次文件源表 / 服务封装资产 → 统一 `onNavigate('tableDetail', { tableId })` 落到 M1。
- 无运行时写操作；`发起交换申请`/`发起服务申请`/`批量导出血缘` 全部 `ComingSoonAction` 占位弹 modal。

---

## 6. 测试

- **单测（node --test）**：
  - `test/state.test.mjs`：`MODULE_GROUPS` 含 10 模块；`qualityBoard/lineageBoard/standardBoard/batchFile/dataService` 均 `implemented === true`；`governance`/`service` 不再存在。
  - `test/data.test.mjs`：`lineage[]` 每条 `up`/`down` 均存在于 `tables[]`，`mode ∈ {离线批次,数据服务,应用内}`；`batchFiles[].sourceTableId` 存在于 `tables[]`；`services[].tableIds` 每个存在于 `tables[]`，`securityLevel` 合法，两数组 `applyFlow` 每步含 `step`/`result`。
- **e2e（playwright `test:page`）**：
  - 三看板逐个打开（质量/血缘/标准）→ 渲染断言；
  - 血缘看板：检索 → 查看血缘图 → SVG `.lineage-node` 存在 → 点节点转跳 M1（`.field-table` + `.tab.active` 表名）；
  - 批次文件 / 数据服务：列表渲染 → 详情看审批链（`.flow-step` 存在）→ 占位按钮弹 modal；
  - 原「数据治理看板」相关断言改写为三看板。
- **约定**：开发完成后必跑 `npm test` + `npm run test:page`，全绿方可合并。

---

## 7. 部署

沿用 GH Actions（push main → test → build → rsync）。无新增 secret、无 nginx 改动、无 package.json 改动。

---

## 8. 待确认

1. 侧边栏重构方案（3 分组 10 菜单，`governance`/`service` 替换为 5 新 key，`批次文件`/`数据服务` 命名）是否通过？
2. 血缘交换方式语义（跨应用只分「离线批次/数据服务」两种，应用内不显式标注）与数据契约样例是否通过？
3. 通过后即产出「三期实现计划」（任务清单，subagent-driven-development）。
