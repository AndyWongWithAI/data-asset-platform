# 数据资源管理平台 · 功能矩阵设计（v4）

> 展示版（Demo）。本文件是「功能矩阵设计」的正式产出，是前端实现的上游唯一依据。
> 日期：2026-08-13 · 领域：GDEPDI 海上风电全生命周期数据 · 技术：React + Vite + 静态 JSON

---

## 0. 版本说明 · 设计演进

| 版本 | 变更 |
|------|------|
| v1 | 8 模块平铺（功能域并列型 + 嫁接资产聚合/生命周期维度） |
| v2 | 用户修订 M1 + 新增生产态治理看板 → 引入「设计态 / 生产态」分层 |
| v3 | 数据质量恢复为独立模块（登记质量规则），与生产态质量子看板分离 |
| v4 | 备注数据加工定位边界（大数据开发平台）+ 数据服务 M6 → 资产目录 M1 关系箭头 |

**核心洞察（贯穿 v2–v4）**：数据治理平台分两态——
- **设计态**（定义/编目）：M1 元数据目录 + M2 数据质量 + M3 数据标准 + M4 数据安全分级 + M5 主数据。
- **生产态**（监控/展示）：M0 生产态治理看板，对质量/血缘/标准做运行态展示。

---

## 1. 信息架构（IA）

```
数据资源管理平台（GDEPDI 海上风电）
├── 01 生产态治理看板（门户）★ 生产态·监控
│   ├── 数据质量子看板
│   ├── 数据血缘看板
│   └── 数据标准看板
├── 02 数据资产目录 / 元数据 ★ 设计态·锚点
├── 03 数据质量（规则登记）★ 设计态
├── 04 数据标准（标尺）      ★ 设计态
├── 05 数据安全分级（治理配置）★ 设计态
├── 06 主数据管理（共享对象）  ★ 设计态
└── 07 数据服务 / 共享（价值输出 · 可选）
```

- 一级导航 7 项 = 1 门户（生产态看板）+ 1 锚点（元数据目录）+ 4 设计态治理配置 + 1 价值输出（可选）。
- 模块间只通过「资产/表/字段」「质量规则」「标准」「主数据编码」锚点关联，不直接互依赖。

---

## 2. 模块清单 + 开发优先级（总表）

| 模块 | 层级 | 优先级 | 定位（一句话） |
|------|------|--------|--------------|
| 生产态治理看板（门户） | 生产态·监控 | **P0** | 质量/血缘/标准 三子看板，治理效果的统一监控出口 |
| 数据资产目录 / 元数据 | 设计态·锚点 | **P0** | 数据资产统一编目与元数据管理，唯一数据源 |
| 数据质量 | 设计态·规则登记 | P1 | 登记数据质量规则，是元数据「关联质量规则」的取值来源 |
| 数据标准 | 设计态·标尺 | P1 | 码表与字段标准定义，是元数据「关联标准」与「贯标」的对标尺 |
| 数据安全分级 | 设计态·治理配置 | P1 | 分级规则与脱敏策略，是元数据「安全分类分级」的取值来源 |
| 主数据管理 | 设计态·共享对象 | P1 | 主数据实体黄金记录，是元数据「主数据引用」的来源 |
| 数据服务 / 共享 | 价值输出 | P2 | 治理达标资产封装为产品/API 对外共享（可选） |

---

## 3. 各模块详述

### M0 生产态治理看板（门户）【P0 · 用户锚点】

整合进门户界面中，是「设计态元数据」在生产态的运行效果监控出口。

> **定位边界（备注）**：数据标准看板内容、数据质量看板中数据的**加工**（规则执行、质量评分计算、贯标统计等），由专门团队在**大数据开发平台**上完成，**不在本数据资产管理平台定位内**。本平台只负责「设计态登记规则/标准」与「生产态展示加工结果」，不承担数据加工职责。

#### 3.0.1 数据质量子看板

- **功能点**
  1. 支持查看企业整体质量评分。
  2. 支持搜索应用并查看应用维度质量评分。
  3. 支持定位问题数据明细、转跳资产目录。

#### 3.0.2 数据血缘看板

- **功能点**
  1. 顶检索（可选表或字段粒度）+ 下方检索结果清单（表或字段）。
  2. 支持批量导出选中资产的血缘。
  3. 支持点击单一资产，跳转具体卡片式血缘关系图。
  4. 支持点击单一资产，跳转具体资产详情页。

#### 3.0.3 数据标准看板

- **功能点**
  1. 支持查看企业整体贯标情况。
  2. 支持搜索应用并查看应用维贯标情况。
  3. 支持筛选应贯未贯数据明细、转跳资产目录。

---

### M1 数据资产目录 / 元数据（核心承重墙）【P0 · 用户锚点】

- **定位**：数据资产的统一编目与检索入口，是其余治理模块共用的「数据资产」锚点对象。
- **核心逻辑**
  1. **技术视角编目**：应用 → 库 → 表或视图 → 字段；**业务视角编目**：业务域 → 主题域 → 数据集（映射为表）。
  2. **设计态数据**（后台数据存储 + 前端展示）：
     1. 每个字段挂三类元数据：
        - **业务**：字段编号 / 名称 / 定义 / 主数据编号（如有）/ 主数据类型（如有）
        - **技术**：类型 / 长度 / 主键 / 外键 / 关联质量规则
        - **管理**：关联标准 / 安全分类分级 / 责任人 / 更新频率
     2. 每个表存储：应用名、逻辑模型名、库名、表中文名、表英文名、表类型（业务表/技术表）、业务域（仅业务表）、主题域（仅业务表）、主数据引用（如有）。
- **功能点**：检索（应用名 / 业务域下拉筛选 + 表名子串匹配）、资产详情（字段级元数据列表）、元数据编辑、资产变更记录。
- **数据对象**：测风数据表、地质钻孔表、岩土数据库、风机设备表、海缆参数表、升压站主变表、SCADA 遥测表、功率预测结果表、BIM 模型、图纸计算书。
- **关键界面**：①资产目录总览（顶检索 + 下方检索结果清单，表粒度）；②资产详情页（字段元数据）。
- **DEMO 不做**：批量导入、版本历史、审批流。

---

### M2 数据质量（规则登记）【P1 · 设计态】

- **定位**：数据质量规则的登记/定义处（设计态），是 M1 元数据「关联质量规则」字段的取值来源，其运行结果由 M0 质量子看板监控。
- **核心逻辑**
  1. **规则登记**：规则名、规则类型（完整性/准确性/一致性/及时性）、绑定对象（表/字段）、校验表达式/阈值、严重级别（严重/警告/提示）。
  2. 规则与 M1 字段的「关联质量规则」建立引用关系（一个字段可挂多条规则）。
  3. 规则运行结果（质量评分、问题明细）供 M0 质量子看板展示，实现「设计态登记 → 生产态监控」闭环。
- **功能点**：规则登记（新增/编辑）、规则检索（按类型/绑定对象）、规则-字段引用关系、规则模板、规则启停状态。
- **数据对象**：SCADA 测点缺失（完整性）、测风取值越界（准确性）、海缆监测温度阈值（一致性）、功率预测延迟（及时性）、地质钻孔地层字段缺失（完整性）。
- **关键界面**：①质量规则列表（规则名/类型/绑定对象/状态）；②规则详情（校验表达式 + 绑定字段 + 引用）。

---

### M3 数据标准【P1 · 标尺】

> **六期演进（2026-08-15）**：数据标准从「码表 + 字段标准」两分类升级为数据元四层体系——基础术语 / 值域 / 参考数据 / 信息项（详见 `六期技术设计.md`）。

- **定位**：字段标准与码表的定义处，是 M1 元数据「关联标准」字段的取值来源，也是 M0 标准看板「贯标」的对标尺。
- **核心逻辑**
  1. 两类标准：码表（枚举值：电压等级 / 风机机型 / 基础型式 / 海域编码 / 海缆类型）+ 字段标准（命名 / 类型 / 单位 / 取值域）。
  2. 贯标 = M1 中某字段「关联标准」指向一条标准；M0 标准看板据此统计贯标率与应贯未贯清单。
  3. 标准带版本（DEMO 简化：只读展示，不做版本历史）。
- **功能点**：码表查看、字段标准查看、标准检索、标准-字段引用关系（被哪些字段引用）。
- **数据对象**：电压等级（35/66/220/500kV/±500kV 柔直）、风机机型（8/12/16MW/16.6MW 漂浮式）、基础型式（单桩/导管架/多桩/漂浮式）、海域编码（粤东/粤西/珠三角）、海缆类型（集电/送出/三芯/单芯）。
- **关键界面**：①标准列表；②标准详情（枚举值 + 引用字段清单）。

### M4 数据安全分级【P1 · 治理配置】

- **定位**：分级规则与脱敏策略的定义处，是 M1 元数据「安全分类分级」字段的取值来源。
- **核心逻辑**
  1. 四级：L1 公开 / L2 内部 / L3 敏感 / L4 涉密。
  2. 分级规则（数据内容 × 来源 × 业务影响）+ 脱敏策略（坐标模糊化 / 偏移 / 字段掩码）。
  3. M1 字段「安全分类分级」引用此处结果；涉密导出受管控。
- **功能点**：分级规则、脱敏策略、分级清单、脱敏前后对比预览。
- **数据对象**：场址精确坐标（L4）、航道/锚地/生态红线坐标（L4）、海洋军事区（L4）、海缆路由（L3）、岩土钻孔（L3）、SCADA 遥测/功率预测（L2）、公开气象（L1）。
- **关键界面**：①分级总览（分级分布 + 高风险清单）；②脱敏前后对比（原坐标 vs 模糊化坐标）。

### M5 主数据管理【P1 · 共享对象】

- **定位**：主数据实体黄金记录的定义处，是 M1 元数据「主数据编号 / 类型 / 引用」的来源。
- **核心逻辑**
  1. 主数据实体 = 风机 / 海缆 / 升压站 / 项目 / 供应商，各持唯一编码。
  2. M1 业务表字段通过「主数据编号 / 主数据类型」引用主数据实体，不冗余存属性。
  3. 重复检测合并（DEMO 简化：只读展示）。
- **功能点**：主数据实体清单、唯一编码、引用关系（哪些字段引用它）、主数据质量看板。
- **数据对象**：风机（机型/额定功率/叶轮直径）、海缆（电压等级/截面/长度）、升压站（主变容量/接线方式）、项目（编码/规模/阶段）、供应商（资质/供货范围）。
- **关键界面**：①主数据实体列表（按类型分页）；②实体详情（权威字段 + 引用字段清单）。

### M6 数据服务 / 共享【P2 · 价值输出 · 可选】

- **定位**：把治理达标的数据资产封装为数据产品/API 对外共享（对齐省大数据中心「一平台、两服务」）。
- **核心逻辑**
  1. 数据产品 = 资产 + 服务方式（API/数据包/订阅）+ 申请流程；上架前校验安全分级与脱敏。
  2. 申请 → 安全/合规审批 → 授权 → 调用计量 闭环；L3/L4 二次审批。
  3. 全程留痕可审计。
- **功能点**：数据产品/API 目录、服务申请与审批、授权与调用计量、服务健康监控。
- **数据对象**：风资源评估 API、SCADA 实时数据订阅、功率预测服务、海域限制因素共享（政府监管）、设备档案数据产品。
- **关键界面**：①数据产品目录；②申请审批流。

---

## 4. 设计态 / 生产态分层与模块关系

```
生产态（监控·展示）
   ┌────────────────────────────────────┐
   │  生产态治理看板 M0（门户）            │
   │  质量子看板 / 血缘看板 / 标准看板      │
   └──────────────────┬─────────────────┘
                      │ 对标 · 转跳
                      ▼
设计态（定义·编目）
   ┌────────────────────────────────────────────────────────────┐
   │           数据资产目录 / 元数据 M1（设计态锚点）              │
   │           编目 + 字段/表级元数据                            │
   └──┬──────────┬──────────┬──────────┬──────────┬─────────────┘
      │ 引用      │ 引用      │ 引用      │ 引用      ▲ 封装资产
      ▼          ▼          ▼          ▼          │
  ┌─────────┐┌─────────┐┌─────────┐┌─────────┐┌───┴──────┐
  │ 质量 M2  ││ 标准 M3  ││ 安全 M4  ││ 主数据 M5 ││数据服务 M6 │
  │ 登记规则  ││ 码表/字段 ││ 分级/脱敏 ││ 实体黄金   ││ 产品/API   │
  └────┬────┘└─────────┘└─────────┘└─────────┘└──────────┘
       │ 运行结果（外部大数据开发平台加工）→ 供 M0 质量子看板展示
```

- **设计态**（M1 + M2/M3/M4/M5）：定义「有什么数据、字段是什么、按什么规则校验、按什么标准、什么密级、引用什么主数据」。
- **生产态**（M0）：监控展示「数据跑起来后质量如何、血缘如何、贯标如何」。
- **质量闭环**：M2 登记规则 → M1 字段「关联质量规则」引用 → 规则运行（外部大数据开发平台）→ M0 质量子看板展示评分/问题 → 转跳回 M1 定位字段。
- **锚点一致性**：M0 三个看板全部转跳回 M1（资产目录），以 M1 为唯一数据源，杜绝「看板与目录两套数据」。
- **数据服务 M6 非孤岛**：M6 封装 M1 资产目录中「治理达标」的资产为产品/API，箭头 **M6 → M1**（引用/封装资产）。
- **血缘归属**：血缘关系为静态数据（DEMO 不做采集/维护），由 M0 血缘看板展示。

### 定位边界（备注）

> **数据加工不在本平台定位内**：数据标准看板内容、数据质量看板中数据的**加工**（规则执行、质量评分计算、贯标统计等），由专门团队在**大数据开发平台**上完成。本数据资产管理平台只负责：
> - 设计态：登记/定义元数据、质量规则、标准、安全分级、主数据；
> - 生产态：**展示**加工结果（质量评分、贯标情况、血缘关系），不承担数据加工职责。

---

## 5. 界面交互流程（关键用户旅程）

**旅程 1 · 设计态 → 生产态（主线）**
资产目录（编目检索）→ 打开「青洲一二 500kV 海上升压站主变表」资产详情 → 看字段元数据（关联标准 / 质量规则 / 安全分级 / 主数据引用）→ 转跳生产态治理看板 → 质量子看板看该应用质量评分 / 标准看板看贯标 → 闭环。

**旅程 2 · 质量规则登记 → 评分监控（质量闭环）**
数据质量模块（登记一条「SCADA 有功功率取值范围」准确性规则，绑定字段）→ 转跳资产目录该字段 → 看「关联质量规则」已挂上该规则 → 转跳生产态治理看板质量子看板 → 定位问题数据明细 → 闭环。

**旅程 3 · 质量子看板 → 定位问题 → 转跳目录**
生产态治理看板（质量子看板，看企业整体评分）→ 搜索「SCADA 采集应用」→ 看应用维质量评分 → 定位问题数据明细 → 转跳资产目录该字段 → 看字段元数据与关联质量规则 → 闭环。

**旅程 4 · 血缘看板 → 卡片式血缘图 → 跳详情**
生产态治理看板（血缘看板）→ 顶检索选「表粒度」搜「功率预测结果表」→ 检索结果清单勾选 → 批量导出血缘 → 点击进入卡片式血缘关系图（上游测风/SCADA → 下游运维诊断）→ 点节点跳资产详情页 → 闭环。

**旅程 5 · 标准看板 → 筛应贯未贯 → 转跳目录**
生产态治理看板（标准看板，看企业整体贯标）→ 搜索某应用 → 看应用维贯标情况 → 筛选「应贯未贯」明细 → 转跳资产目录对应字段 → 补「关联标准」→ 闭环。

---

## 6. 前端实现映射（静态 JSON 结构设想）

单一 `data.js`（或 `data.json`）承载全部 mock 数据，页面数据驱动渲染：

```
meta          // 平台标题/免责声明/统计
applications[]// 应用（技术视角编目起点）
databases[]   // 库
tables[]      // 表：应用名/逻辑模型名/库名/表中文名/表英文名/表类型(业务表|技术表)/业务域/主题域/主数据引用
fields[]      // 字段：业务(编号/名称/定义/主数据编号/主数据类型) 技术(类型/长度/主键/外键/关联质量规则) 管理(关联标准/安全分类分级/责任人/更新频率)
bizDomains[]  // 业务域 + 主题域（业务视角编目）
qualityRules[]// 质量规则（供 M2 规则登记 + M1 关联质量规则引用）
qualityResults[]// 质量评分 + 问题明细（外部大数据开发平台加工结果，供 M0 质量子看板展示）
standards[]   // 码表 + 字段标准（供 M3 + M0 标准看板）
security[]    // 分级 + 脱敏策略（供 M4）
masterData[]  // 主数据实体（供 M5）
lineage[]     // 血缘节点 + 边（供 M0 血缘看板）
services[]    // 数据产品/API（供 M6，引用资产 ID）
```

数据完整性测试用 Node 断言引用完整性（表↔字段↔规则↔标准↔主数据↔血缘↔服务 跨模块一致，杜绝孤岛，含 M6 服务引用资产 ID 一致）。

---

## 7. 开发优先级排序

| 优先级 | 模块 | 理由 |
|--------|------|------|
| **P0（首期·MVP）** | **M1 资产目录/元数据** | 唯一数据源、设计态锚点，所有模块的地基 |
| **P0（首期·MVP）** | **M0 生产态治理看板（3 子看板）** | 价值展示出口。子看板内顺序：质量子看板（最简）→ 标准看板 → 血缘看板（血缘图渲染最复杂，可后置） |
| **P1（二期·补齐设计态配置）** | **M2 数据质量、M3 数据标准、M4 数据安全分级、M5 主数据管理** | M1 元数据「关联质量规则/关联标准/安全分类分级/主数据引用」字段的取值来源。P0 阶段用静态 mock 值内嵌占位，二期再做独立登记/维护页 |
| **P2（三期·延伸）** | **M6 数据服务/共享** | 封装 M1 治理达标资产（箭头 M6→M1），对齐省大数据中心「两服务」；可选，不影响主线 |

**首期 MVP 范围**：M1（编目 + 字段/表元数据 + 检索 + 资产详情）+ M0（质量/标准/血缘三子看板），跑通「编目 → 治理监控」主线；质量规则/标准/分级/主数据以静态引用值内嵌于 M1，暂不做独立模块页。

---

## 8. 待确认

1. v4 功能矩阵（7 模块 + 5 旅程 + P0/P1/P2 优先级 + 定位边界备注 + M6→M1 箭头）是否通过？
2. 首期 MVP 按「M1 + M0（三子看板）」推进是否同意？
