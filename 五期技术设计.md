# 数据资产管理平台 · 五期技术设计

> 展示版（Demo）。五期范围：血缘看板字段级血缘从「一级」升级为「N 级多跳遍历」。
> 日期：2026-08-14 · 上游依据：`功能矩阵设计.md`(v4) → `四期技术设计.md` · 状态：已实现并合入 main

---

## 0. 需求来源（用户原话）

> 当一个血缘画布中有多个实体时，点击属性，不只是显示一级字段级血缘，还要遍历整个画布，把它的 N 级血缘也画出来。

---

## 1. 现状与差距

四期已实现：点字段 `selFieldId` 后，只画「以该字段为端点」的字段级线（`m.up === selFieldId || m.down === selFieldId`），即**一级**血缘。

差距：当画布内有多个实体、字段级映射形成链（如 `f_turbine_md → f_scada_turbine → f_spare_turbine`）时，点上游字段 `f_turbine_md` 只画第一跳，看不到它一路流到下游的完整链路。

## 2. 语义边界（关键）

- **表级画布不变**：仍是「中心表 + 直接上游 + 直接下游」三列，不扩到 N 级表血缘。
- **N 级仅作用于字段级血缘遍历**：在现有画布的字段集合内，从选中字段出发做**双向多跳可达**遍历，直到画布内无更多可达字段（BFS 自然终止，不设固定 N 上限）。
- **方向双向**：上游方向（沿 `down→up` 逆边走）+ 下游方向（沿 `up→down` 正边走）。

## 3. 算法（双向有向可达闭包）

选中字段 `F` 后：

1. **收集画布内字段级映射边** `inGraphEdges`：遍历 `lineage[].fieldMapping`，仅保留「up/down 字段所属表都 ∈ inGraph」的映射。
2. **建有向邻接表**（仅画布内边）：
   - 正边 `upToDown: upField -> [downField...]`（下游方向）
   - 逆边 `downToUp: downField -> [upField...]`（上游方向）
3. **BFS 双向可达**：从 `F` 出发，`queue` 里每个字段分别沿 `upToDown`（下游）和 `downToUp`（上游）扩展，得到可达字段集 `reachable`。
4. **画边**：`fieldLines = inGraphEdges.filter(m => reachable.has(m.up) && reachable.has(m.down))`。

> 注意用**双向有向可达**而非「无向连通」：避免在分叉结构（一个上游字段同时流向两个下游字段）时，把「兄弟字段」之间的边误画进去。字段级图继承表级 DAG 结构（无环），双向有向可达 = 上游传递闭包 ∪ 下游传递闭包。

### 示例（现有数据，t_scada 画布）

t_scada 画布 = 上游 t_turbine + 中心 t_scada + 下游 t_forecast/t_spare。字段链：`f_turbine_md → f_scada_turbine → f_spare_turbine`。

- 点 `f_turbine_md`（上游表字段）：
  - 一级（旧）：`f_turbine_md→f_scada_turbine`（1 条）
  - N 级（新）：下游可达 = {f_turbine_md, f_scada_turbine, f_spare_turbine}，画 `f_turbine_md→f_scada_turbine` + `f_scada_turbine→f_spare_turbine`（2 条）

## 4. 改动范围

- `src/modules/LineageModule.jsx`：`LineageGraph` 内 `fieldLines` 计算逻辑替换（§3 算法），其余（类框布局、表级线、toggle 交互、mode 标注）不动。
- `src/index.css`：无需新增（`.lineage-edge-field` 已存在）。
- `test/page_test.py`：追加 N 级断言（点上游表字段 → 字段级线延伸 ≥ 2 条）。
- `五期技术设计.md`（本文件）状态收尾。
- `CLAUDE.md`：当前状态补五期一笔。

## 5. e2e 断言设计

现有血缘 e2e 用「点中心表 t_wind 的『风速』字段」测一级。为测 N 级，需换一个「有上游+下游」的中心表：

- 打开血缘看板 → 点 `t_scada`（用 `has_text='SCADA 遥测表'` 定位该行）行的「查看血缘图」。
- 点上游表字段「主数据编码」（`f_turbine_md`，t_scada 画布内唯一，业务名唯一无 strict 冲突）。
- 断言 `.lineage-edge-field` count **>= 2**（含二级 `f_scada_turbine→f_spare_turbine`）。
- 再点同字段 toggle 清空，`.lineage-edge-field` == 0。

> 若 `.nth` 定位列表行不稳，改用 `page.locator('.table tbody tr', has_text='SCADA 遥测表')` 定位该行再点「查看血缘图」。

## 6. 测试约定

- 单测（node --test）不改：fieldMapping 引用完整性已覆盖，N 级是纯渲染逻辑，用 e2e 兜底。
- 开发完成后必跑 `npm test` + `npm run build` + `npm run test:page`，全绿方可合并。

## 7. 已确认并实现

1. N 级遍历采用「双向有向可达」（上游 N 跳 + 下游 N 跳）语义，已实现。
2. 表级画布不变、仅字段级多跳遍历，已实现。
3. 实现完成并合入 main。
