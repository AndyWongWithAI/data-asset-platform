# 数据资源管理平台 · 七期技术设计

> 展示版（Demo）。七期范围：数据标准板块的 6 项修正与增强——可收缩目录、基础术语去下钻、信息项去词根链/加类型业务域定义、关联标准点击转跳、贯标对齐判定。
> 日期：2026-08-15 · 上游依据：`功能矩阵设计.md`(v4) → `六期技术设计.md` · 状态：已实现

---

## 0. 需求来源（用户原话）

> 1. 左侧菜单栏应该做出可收缩的目录结构，把基础术语、值域、参考数据、信息项放入数据标准目录中。
> 2. 基础术语不用提供下钻明细的操作。
> 3. 信息项明细中不需要构成词根链。
> 4. 信息项增加类型（技术\业务）、业务域（业务信息项必选）、定义（业务信息项必填）。
> 5. 数据资产目录中的字段明细-关联标准，要提供点击标准转跳标准明细的能力。
> 6. 当字段与关联信息项中英文名称不完全一致时，在数据标准看板中应该被列为应贯未贯。

**用户确认（2026-08-15 澄清）**：需求 6 的种子数据策略 = **调整字段名对齐信息项标准名，形成「已对齐=已贯标」vs「未对齐=应贯未贯」对比**。

---

## 1. 现状与差距

六期已把「数据标准」拆为 4 个平铺菜单（baseTerm/valueDomain/refData/infoItem），信息项有词根链展示，基础术语有下钻详情，贯标率 = `standardId` 非空字段数（15/51）。

差距：①侧边栏 4 项平铺、无目录收合；②基础术语/信息项详情过重；③信息项缺类型/业务域/定义；④字段明细的关联标准不可点；⑤贯标只判「是否挂标准」、不判「名称是否对齐标准」。

---

## 2. 需求逐条设计

### 2.1 可收缩目录（state.js + Sidebar.jsx）

`MODULE_GROUPS` 支持 item 带 `children`。设计态「数据标准」恢复为父级目录，4 子项挂其下：

```js
{ key: 'standard', title: '数据标准', implemented: true, children: [
  { key: 'baseTerm', title: '基础术语', implemented: true },
  { key: 'valueDomain', title: '值域', implemented: true },
  { key: 'refData', title: '参考数据', implemented: true },
  { key: 'infoItem', title: '信息项', implemented: true },
]},
```

- **父级 `standard` 是「目录」不是「模块」**：点击展开/折叠子项，不打开 tab。
- **`MODULES` 递归收集叶子模块**（有 children 的 item 不进 MODULES），`TABLE_DETAIL` 照旧。
- **Sidebar**：item 有 children → 渲染目录标题（带展开/折叠箭头，内部 useState 记录折叠态）；无 children → 渲染模块按钮。默认「数据标准」目录**展开**（用户可见 4 子项）。

### 2.2 基础术语不下钻（BaseTermModule.jsx）

删除详情面板（`selectedId` 相关逻辑与「返回列表」），只保留：列表（筛选「全部/类词/非类词」+ 列：中文名/英文名/同义词/是否类词）+ 「新增术语」占位按钮。列表第一列（中文名）由 `button.link` 改为纯文本（不可点下钻）。

### 2.3 信息项去掉词根链（InfoItemModule.jsx）

详情删除「构成词根链」块（`.term-chain` + 「中文名由基础术语顺序拼接构成」hint）。**保留**「英文名映射」块（英文名 + 词根拼接式）。

### 2.4 信息项加类型/业务域/定义（data.js + InfoItemModule）

`infoItems` 元模型新增 3 字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| type | `'技术' \| '业务'` | 信息项类型 |
| bizDomainId | string \| null | 业务域（业务信息项必选，引用 `bizDomains`） |
| definition | string \| null | 定义（业务信息项必填） |

**约束（单测兜底）**：`type ∈ {'技术','业务'}`；`type === '业务'` → `bizDomainId` 非空且存在 + `definition` 非空。

**类型分配**（10 信息项，8 业务 + 2 技术）：
- 业务（8）：ii_voltage(设计域) / ii_active_power(生产运营域) / ii_turbine_model(设计域) / ii_foundation(设计域) / ii_sea_area(勘测域) / ii_cable_type(设计域) / ii_accuracy(生产运营域) / ii_progress(工程建设域)。
- 技术（2）：ii_wind_speed / ii_temperature（无业务域、无定义）。

**UI**：信息项列表加「类型」列；详情 kv-list 加类型/业务域/定义（技术项业务域、定义显示「—」）。

### 2.5 关联标准点击转跳（TableDetailModule + InfoItemModule + TabWorkspace）

- `TableDetailModule` 字段「关联标准」列：由纯文本 `${nameCn}（code）` 改为 `button.link`，onClick `onNavigate('infoItem', { infoItemId: std.id })`。
- `InfoItemModule` 改为**受控于 assetId**：`const selectedId = assetId?.infoItemId ?? null`；列表项点击 → `onNavigate('infoItem', { infoItemId: id })`；「返回列表」→ `onNavigate('infoItem', null)`。删除内部 `useState(selectedId)`。
- `TabWorkspace` 给 `InfoItemModule` 传 `assetId={activeTab.assetId}`。

> 说明：`infoItem` 非 multi 模块，`navigate('infoItem', {infoItemId})` 复用同一 tab 并更新 assetId，tab 标题保持「信息项」。

### 2.6 贯标对齐判定（StandardBoardModule + 字段名对齐）

**判定规则**：字段「已贯标」需同时满足：
1. `f.management.standardId` 非空；
2. `f.business.nameCn === 信息项.nameCn`（中文名精确一致）；
3. `f.business.code === 信息项.nameEn`（英文名精确一致，含大小写）。

否则列「应贯未贯」。`StandardBoardModule` 贯标率 = 已贯标字段数 / 总字段数；「应贯未贯」明细 = 无标准字段 + 有标准但名称未对齐字段（可加「原因」列：无关联标准 / 名称未对齐）。

**字段名对齐清单**（调整 6 字段对齐，9 字段保持未对齐，形成对比）：

| 字段 | 原名 nameCn / code | 对齐为（=信息项） | 信息项 |
|------|------|------|------|
| f_wind_speed | 风速 / WIND_SPEED | 风速值 / wind_speed_value | ii_wind_speed |
| f_scada_temp | 温度 / TEMP | 温度值 / temperature_value | ii_temperature |
| f_prog_progress | 进度百分比 / PROGRESS | 进度百分比 / progress_percent | ii_progress |
| f_cable_voltage | 电压等级 / VOLTAGE | 电压等级编码 / voltage_level_code | ii_voltage |
| f_scada_power | 有功功率 / ACTIVE_POWER | 有功功率值 / active_power_value | ii_active_power |
| f_turbine_model | 机型 / MODEL | 风机机型名称 / turbine_model_name | ii_turbine_model |

未对齐（保持原名，作为「应贯未贯」示例）：f_sub_voltage / f_forecast_power / f_turbine_power / f_spare_turbine / f_turbine_foundation / f_topo_coord / f_cable_type / f_forecast_accuracy / f_cable_temp。

> 改名仅影响展示（nameCn/code），不影响 `fieldId` 引用（血缘/质量规则/fieldMapping 均按 id 引用）。血缘图字段名随之显示标准名（如「风速值」），可接受。

---

## 3. 改动范围

- `src/data.js`：`infoItems` 加 type/bizDomainId/definition；6 字段名对齐（§2.6 表）。
- `src/state.js`：`MODULE_GROUPS` 嵌套 children；`MODULES` 递归收集叶子。
- `src/components/Sidebar.jsx`：可折叠目录渲染。
- `src/modules/BaseTermModule.jsx`：去详情。
- `src/modules/InfoItemModule.jsx`：去词根链 + 加类型/业务域/定义展示 + assetId 受控。
- `src/modules/TableDetailModule.jsx`：关联标准列加转跳按钮。
- `src/modules/StandardBoardModule.jsx`：贯标对齐判定 + 原因列。
- `src/components/TabWorkspace.jsx`：InfoItemModule 传 assetId。
- `src/index.css`：折叠目录样式（箭头/缩进）如有需要少量新增。
- `test/data.test.mjs`：infoItems 类型/业务域/定义约束。
- `test/state.test.mjs`：MODULE_GROUPS 嵌套 + MODULES 叶子收集断言（模块数变）。
- `test/page_test.py`：折叠目录 + 基础术语不下钻 + 信息项新字段 + 关联标准转跳 + 贯标对齐 e2e。
- 文档：`七期技术设计.md`（本文件）、`CLAUDE.md`、`功能矩阵设计.md`。

---

## 4. 测试约定

- 单测：infoItems 类型枚举 + 业务必选业务域/定义约束；MODULES 叶子收集正确（standard 父级不在 MODULES）。
- e2e：①「数据标准」目录可展开/折叠；②基础术语列表无下钻（点中文名不进详情）；③信息项详情无 `.term-chain`、有类型/业务域/定义；④M1 字段明细点「关联标准」转跳信息项详情；⑤数据标准看板贯标率与「应贯未贯」含「名称未对齐」条目。
- 开发完成后必跑 `npm test` + `npm run build` + `npm run test:page`。

---

## 5. 任务分解（subagent-driven-development）

| 任务 | 分支 | 内容 | 状态 |
|------|------|------|------|
| A | task/phase7-data | data.js infoItems 加字段 + 字段名对齐 + data.test.mjs 约束 | ✅ 已完成 |
| B | task/phase7-ui | state 嵌套 + Sidebar 折叠 + BaseTerm 去详情 + InfoItem 去词根链/加字段/assetId 受控 + TabWorkspace | ✅ 已完成 |
| C | task/phase7-standard-e2e | StandardBoard 对齐判定 + TableDetail 转跳 + page_test.py + 文档 | ✅ 已完成 |

> 依赖 A → B → C。每任务：实现者 → reviewer → merge。
