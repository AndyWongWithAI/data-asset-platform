# 数据资产管理平台 · 说明文档

> 用途：面试 / 架构评审时的口头说明底稿。
> 线上地址：https://demo.intelab.cn/data-asset-platform/ （管理平台）+ https://demo.intelab.cn/data-asset-portal/ （数据资产门户）
> 仓库：github.com/AndyWongWithAI/data-asset-platform（公开）

---

## 1. 一句话定位

面向**数据中心液冷温控全生命周期数据治理**的数据资产管理演示平台（Demo），由申菱环境（301018.SZ，专业特种空调与温控设备制造商）的公开业务信息构建，覆盖「元数据登记 → 质量 / 标准 / 安全治理 → 资产封装发布 → 增量管控」一条完整治理闭环。它是展示数据治理方法论与工程能力的作品级项目，不是真实生产系统。

---

## 2. 业务背景：为什么做、给谁用

数据中心液冷温控从研发设计、生产制造、营销服务、集成实施到运营维护，数据分散在多个业务系统（PLM 研发设计、MES 生产制造、CRM 营销服务、PM 集成实施、DCIM 智慧运维），存在典型的「数据孤岛 + 元数据缺失 + 分级不明」问题。

平台要解决的治理命题是：

1. **资产看不全** —— 没有统一的资产目录，不知道有哪些表、哪些字段、谁负责；
2. **标准不统一** —— 同一业务概念在不同系统里命名、口径不一致；
3. **安全分级缺继承** —— 字段级分级靠人工记忆，没有「分级定义 → 信息项 → 字段」的继承链；
4. **存量失控** —— 已积累的数据没盘清，登记的和实际的元数据对不上（表没登记、字段漂移）。

平台的三类目标读者：**架构评审**（看设计逻辑）、**求职作品集**（看工程能力）、**方案演示**（看治理闭环的可视化表达）。

---

## 3. 核心设计逻辑

### 3.1 存量盘点 / 增量管控

工业企业的数据治理，首先关心「已经有什么数据」（存量），存量盘清之后才谈「新数据怎么管控」（增量）。平台据此分成两层逻辑：

| 层 | 定位 | 内容 |
|---|---|---|
| **存量盘点** | 把已有的数据资产盘清、登记、定标准、定分级 | 表结构、数据质量、数据标准、数据安全、主数据、数据交换 |
| **增量管控** | 存量盘清后，新数据进来怎么管控 | 数据质量看板、数据标准看板、元数据比对 |

存量盘点把已有数据登记清楚，增量管控通过「元数据比对」这一功能桥接——比对实际采集的元数据快照与登记数据，自动产出「未登记表 / 疑似下线表 / 未登记字段 / 漂移字段」的差异清单。这是「先解决存量，再管控增量」的可视化表达。

### 3.2 唯一锚点：表结构（M1）

**M1 资产目录是全平台唯一的数据源锚点**。所有其它模块都指向它，而不是各自维护一份数据：

- 质量规则 → 关联到 M1 的字段；
- 数据标准（信息项）→ 被 M1 字段「贯标」引用；
- 安全分级 → 通过信息项继承到 M1 字段；
- 主数据 → 被 M1 表 / 字段引用；
- 数据服务 → 封装 M1 里治理达标的表。

这个设计杜绝了「看板一套数据、目录另一套数据」的治理通病——所有看板最终都转跳回 M1 定位到具体字段，靠**引用完整性**这一硬约束保证跨模块一致（有 Node 断言测试兜底，改任何 id 必须同步改引用）。

### 3.3 数据元四层体系（数据标准的骨架）

数据标准没有做成一张扁平码表，而是拆成**四层**，由下到上：

```
基础术语（词根库，28 个）  →  值域（数据类型口径，6 条）  →  参考数据（码表，5 条）  →  信息项（可复用的字段标准，10 条）
```

关键点：**信息项的命名是"派生"出来的，不是手填的**。输入中文名（如"供液温度值"），系统按词根库做最大正向匹配拆词 → 翻译成英文名（supply_temperature_value）→ 自动挂词根 id，且**末位必须是类词**（如"值/编号/名称"），否则硬校验拦截。这保证了命名口径的机器可执行，而不是靠人工约定。

### 3.4 数据安全分级：继承链

安全分级不是给每个字段单独打标，而是建立一条继承链：

```
分级定义（L1 公开 / L2 内部 / L3 敏感 / L4 涉密）  →  信息项声明分级  →  字段继承信息项分级（可自定义升级）
```

字段的分级来源有五种状态：继承自信息项 / 继承自分类目录 / 自定义 / 自定义升级 / 冲突（字段分级低于信息项或分类目录等级时高亮告警）。再往上是「数据安全分类目录」——字段自己关联所属的数据安全分类实例（字段侧 `securityCatalogId` 引用，而非分类侧持有字段清单），治理规则是**自上而下：定位字段的分级不得低于分类等级**（否则分类失去管控价值）。这体现了「分级要能落地到字段」的治理思路，而不是停留在分类目录层面。

### 3.5 一体两面：管理平台 + 数据资产门户

同一套数据、同一个后端，长出两个「皮肤」：

- **数据资产管理平台** —— 技术侧的治理工作台（存量盘点 + 增量管控 + 系统管理）；
- **数据资产门户** —— 业务侧的消费入口（资产目录、资产详情、我的申请），面向业务人员检索、申请、订阅数据。

两个 SPA 共享同一份种子数据与后端 API，右上角互跳。治理一致性用一条断言保证：**门户资产的 securityLevel 不得低于其打包对象的有效分级**。

---

## 4. 领域模型（数据层全貌）

全部数据承载在单一 `src/data.js` 种子文件中，共 **21 个顶层实体**，一条 `meta` 元信息（含 `disclaimer` 声明 + `schemaVersion` 结构版本 + `stats` 计数）：

| 实体 | 数量 | 定位 |
|---|---|---|
| applications | 5 | 业务系统（研发设计 / 生产制造 / 营销服务 / 集成实施 / 智慧运维） |
| databases | 5 | 数据库（含类型 PostgreSQL / Oracle / MySQL / SQL Server / 时序库） |
| bizDomains | 5 | 业务域 |
| tables | 10 | 数据表（含分区 / 索引 / 版本历史内嵌数组） |
| fields | 51 | 字段（唯一数据源锚点，关联标准 / 规则 / 主数据 / 分级 / 分类目录） |
| baseTerms | 28 | 基础术语（词根库，含类词白名单） |
| valueDomains | 6 | 值域 |
| refDatas | 5 | 参考数据（码表，编号服务端自增） |
| infoItems | 10 | 信息项（命名由拆词派生） |
| qualityRules | 8 | 质量规则 |
| qualityResults | 2 | 质量评分结果 |
| lineage | 9 | 数据血缘边（表级 + 字段级） |
| security | 4 | 安全分级定义 L1–L4 |
| securityCatalog | 13 | 数据安全分类目录（字段级定位，由字段侧 securityCatalogId 引用） |
| capabilityMap | 5 | 能力地图（5 能力域 + 27 能力项，安全分类一二级分类依据，只读） |
| masterData | 5 | 主数据（液冷设备 / 冷源设备 / 数据中心客户 / 液冷项目 / 供应商，元数据级定义） |
| batchFiles | 8 | 文件交换任务（5 出站 + 3 入站库级采集） |
| prodMetadatas | 3 | 采集元数据快照（比对用，关联 batchFileId） |
| services | 5 | 数据服务（含三类接入契约：API / 订阅 / 数据包） |
| portalAssets | 8 | 门户资产（上架封装） |
| portalRequests | 4 | 门户申请 |

> **内容依据红线**：领域模型基于申菱环境**公开业务信息**（年报/公告/技术资料）构建，`meta.disclaimer` 声明「非申菱环境实际部署、仅供演示」，不虚构内部运营数据。

---

## 5. 技术架构

### 5.1 总体

```
┌─────────────────────────┐      ┌──────────────────────────┐
│  管理平台 SPA (React)    │      │  门户 SPA (React)         │
│  base=/data-asset-platform/ │  │  base=/data-asset-portal/  │
└────────────┬────────────┘      └────────────┬─────────────┘
             │      共享 DataContext / api        │
             └───────────────┬───────────────────┘
                             │ REST  /api/v1/*
                    ┌────────▼─────────┐
                    │  Express 5 (:8078)│
                    │  store.js 校验层   │
                    │  data.json 落盘    │
                    └──────────────────┘
```

### 5.2 前端：React 18 + Vite 5（两个独立 SPA）

- **技术栈**：React 18.3 + Vite 5，纯 SPA，无状态管理库（用 `useReducer` 做 tab 状态机）、无 UI 组件库（手写 CSS）。
- **双 SPA**：管理平台（`vite.config.js`）与门户（`vite.portal.config.js`，独立 `root` 与 `outDir`），各自构建、各自部署，但**共享同一套数据层**（`data.js` / `api.js` / `DataContext.jsx`）。
- **架构分层**（前端内部刻意做的分层，保证可测性）：

  | 层 | 文件 | 职责 | 是否依赖 React |
  |---|---|---|---|
  | 状态机 | `state.js` | tab 打开 / 关闭 / 切换 / 导航，纯逻辑 | ❌ |
  | 数据层 | `DataContext.jsx` | 种子兜底 + 启动拉后端 + 写后刷新 | ✅ |
  | 纯函数 | `infoItemNaming.js` / `fieldSecurity.js` / `metadataCompare.js` / `bulkImport.js` / `entityFilter.js` | 命名派生 / 分级来源 / 元数据比对 / CSV 导入 / 引用过滤 | ❌ |
  | 表单引擎 | `schema.js` + `EntityForm.jsx` | schema 驱动的通用表单（引用下拉 / 多选 / 子表 / 表达式编辑器 / 派生回显） | ✅ |
  | 视图 | `modules/*` / `components/*` | 各模块页面与通用组件 | ✅ |

  「纯逻辑与 React 解耦」是刻意的工程选择——命名、分级、比对、导入这些领域规则全部写成不依赖 React 的纯函数，能用 `node --test` 直接单测，这是整个项目测试能跑到 138 个而不臃肿的关键。

### 5.3 后端：Express 5 + JSON 文件持久化（刻意从简）

- **技术选型**：Express 5.2，**无数据库**，数据落盘到 `server/data.json`（纯种子缓存，可随时重种不丢真实数据，因为本就是 demo）。
- **分层**：`app.js`（HTTP 路由薄封装，只做 HTTP ↔ store 映射，无业务逻辑）→ `store.js`（校验 + 领域规则 + 持久化，纯 Node 可单测）。
- **路由**：`GET/POST /api/v1/:entity` + `GET/PUT /api/v1/:entity/:id` + `GET /api/v1/_entities`（实体元信息）。
- **结构校验**（store.js 的 `SCHEMAS`）：必填 / 唯一 / 枚举 / 类型 / 引用完整性，每实体声明式配置。
- **服务端强制派生**（防客户端注入，是本项目的设计亮点）：
  - 信息项 `code`（II0001 自增）、`nameEn` / `termIds`（拆词派生）由服务端生成，客户端传了也无效；
  - 参考数据编号 `CK0001` 服务端自增；
  - 主键 `id` 服务端生成；
  - `update` 时剥离不可篡改字段（`IMMUTABLE_ON_UPDATE`），堵住 PUT 篡改派生字段的缺口。

### 5.4 可靠性与演进（三个关键机制）

1. **种子兜底降级**：前端启动先加载种子数据，再尝试拉后端，后端挂了自动降级为只读展示，不白屏。
2. **坏文件容错**：`data.json` 解析失败自动回退种子重种。
3. **schemaVersion 版本迁移**：种子结构带版本号，后端启动时若持久化数据版本落后 / 缺失，自动重种。**改种子结构只 bump 版本号，不再手动删文件**——这是消除「stale data 掩盖新字段导致白屏」根因的机制。

### 5.5 部署：GH Actions + nginx + systemd

- **前端**：`deploy.yml`（push main → npm ci → 单测 → 双 build → 双 rsync 到 `/opt/demo/`）。
- **后端**：systemd 服务 `data-asset-platform.service`（`User=www-data`，`Restart=on-failure`，:8078）。
- **nginx**：`/data-asset-platform/` 与 `/data-asset-portal/` 静态 alias；`/api/v1/` 反代到 8078（用更长的前缀匹配，与同域名下另一项目占用的 `/api/` → 8077 并存不串线）。

---

## 6. 工程实践

### 6.1 测试策略（138 个单测 + 2 套端到端）

- **单测**（`node --test` 自动发现）：`data.test.mjs`（引用完整性 / 数据一致性，51 字段 × 跨实体引用全断言）、`api.test.mjs`（后端 CRUD + 派生保护 + 版本迁移）、`bulkImport.test.mjs`（CSV 表头对齐）、`fieldSecurity / metadataCompare / infoItemNaming / entityFilter / state` 各纯函数单测。
- **端到端**（Playwright）：`test:page`（管理平台）、`test:portal`（门户），含清单自适应宽度、跨模块转跳、复制按钮、密钥反断言等。

### 6.2 对抗式审查（验证原则落地）

每一次非平凡决策都要过「对抗式审查」：用**不同血统的模型**对实现做独立审查，抓出的缺陷回灌修复。典型成果包括——BFS 多跳血缘写成无向连通导致误画兄弟字段（reviewer 抓出后改两条独立闭包）、`security` 主键是 `level` 无 `id` 导致 refs 校验误报（改 enum 兜底）、批量导入按固定列序静默错位（改表头名对齐）。这些都是在审查环节被拦下的真实缺陷。

### 6.3 开发范式：subagent-driven development

每期功能拆成独立任务，走「实现者子 Agent → 任务审查 → 合并」的流水线，每个任务一个分支、独立测试、独立 review，最终 merge 回 main。`main` 受 git-guardrails 保护（只接受 merge 提交）。

---

## 7. 开发过程（18 期能力递进）

平台不是一次做成的，而是**沿着治理闭环逐期递进**，每一期都建立在前一期之上：

| 阶段 | 主题 | 能力跃迁 |
|---|---|---|
| 一期 | M1 资产目录 + M0 看板 | 确立「存量盘点/增量管控 + 唯一锚点」骨架 |
| 二~五期 | 质量 / 标准 / 安全 / 主数据 + 血缘看板 | 补齐治理四要素，血缘从手绘 SVG 升级 UML 类图 + 字段级 N 级多跳 |
| 六~八期 | 数据元四层体系 + 贯标对齐 + 详情 tab 拆分 | 数据标准从码表升级为可执行的四层体系 |
| 九期 | **写操作**（P0 后端 + P1 表单引擎 + P2 领域规则） | 从「只读 Demo」转「可交互登记」，引入后端 + 服务端强制派生 |
| 十期 | 表结构五 tab + 主数据元数据化 | 表详情细化到分区 / 索引 / 版本历史 |
| 十一期 | 平台改名 + 文件交换双向化 + 服务消费门户 | 交换任务从「采单表」到「双向 + 库级」 |
| 十二期 | **元数据比对** | 打通「存量 vs 增量」鸿沟 |
| 十三~十四期 | 数据安全分级继承链 + 分类目录 | 分级从字段维度升级为「定义 → 信息项 → 字段 → 分类目录」完整链路 |
| 十五期 | **一体两面**（管理平台 + 门户） | 治理成果对业务侧开放消费 |
| 十六期 | 数据服务接入指南 | 补齐技术人员接入视角（API / 订阅 / 数据包三类契约） |
| 十七期 | 标准元数据写闭环（编辑 / 停用 + 批量导入） | 5 实体补齐「新增 → 改 → 停」完整生命周期 |
| 十八期 | 三处严重问题修复 | schemaVersion 迁移 / update 派生保护 / 批量导入表头校验 |

---

## 8. 客观评估

### 8.1 设计上站得住的点

1. **存量盘点/增量管控 + 唯一锚点**：解决了治理平台「数据多源不一致」的头号通病，所有模块靠引用完整性收敛到 M1。
2. **命名可执行化**：信息项命名用「拆词 + 类词硬校验」变成机器规则，而非人工约定，这是数据治理里最难标准化的一环。
3. **服务端强制派生 + 防篡改**：code / nameEn / termIds / 主键全部服务端生成，堵住客户端注入与 PUT 篡改，体现了「信任边界放在服务端」的正确认知。
4. **纯函数解耦 + 高测试覆盖**：领域规则全部是纯函数，138 单测 + 2 套 e2e，可维护性、可验证性拉满。
5. **演进驱动 + 对抗式审查**：18 期能力递进有清晰的治理逻辑主线，每期过对抗式审查，缺陷在 merge 前被拦下。

### 8.2 边界与限制（如实说明）

1. **这是演示版，不是生产系统**：数据是 mock（基于公开信息），无真实数据库、无鉴权、无多用户隔离、单进程 JSON 落盘。
2. **数据加工不在本平台定位内**：规则执行、质量评分、贯标统计等由大数据开发平台完成，本平台只做「登记 + 展示」，不承担加工职责——这是明确的定位边界，不是缺功能。
3. **写操作范围有限**：覆盖 6 个元数据实体的登记 / 修改（术语 / 值域 / 参考数据 / 信息项 / 质量规则 / 分级调整）+ 表结构的新建 / 批量导入 / 编辑（表级元数据，分区 / 索引 / 版本历史服务端保护）+ 字段的新增 / 批量导入 / 编辑（业务定义 / 技术类型 / 键 / 责任人 / 更新频率 / 关联标准 / 关联主数据 / 关联数据安全分类 / 安全分级，安全分级带继承链校验，序号 / 主数据类型 / 质量规则服务端派生）+ 数据安全分类目录的新增 / 批量导入 / 编辑 / 停用（一二级分类级联下拉对齐能力地图，dataType 唯一，字段级定位由字段侧维护，停用可逆不物理删）；表下线、审批流、导出仍是「开发中」占位按钮。
4. **数据血缘看板当前导航已隐藏**（功能代码保留，入口临时摘除）。

### 8.3 一句话总结能力

这个项目能证明的是：**把一套完整的数据治理方法论（分层 + 锚点 + 继承链 + 比对 + 门户），用干净的工程手段（纯函数 + 服务端派生 + 版本迁移 + 对抗式审查 + 高覆盖测试）落地成一个可演示、可扩展的平台**。

---

## 9. 技术栈清单汇总

| 类别 | 技术 |
|---|---|
| 前端框架 | React 18.3 |
| 构建工具 | Vite 5（双 SPA 配置） |
| 后端 | Node.js + Express 5.2 |
| 数据持久化 | JSON 文件落盘（`server/data.json`，schemaVersion 版本迁移） |
| 测试 | Node 内置 `node --test`（138 单测）+ Playwright（2 套 e2e） |
| 部署 | GitHub Actions（前端）+ systemd（后端）+ nginx（反代 / 静态） |
| 开发范式 | subagent-driven development（子 Agent 实现 + 独立审查） + 对抗式审查（异血统模型交叉验证） |
| 版本控制 | Git（main 只接受 merge 提交，git-guardrails 保护） |
